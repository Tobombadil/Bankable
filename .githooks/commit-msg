#!/bin/sh
# Blocks commit messages carrying a model identifier (CLAUDE.md guardrail). Install with `make hooks`.
exec "$(git rev-parse --show-toplevel)/.venv/bin/python" "$(git rev-parse --show-toplevel)/scripts/check_commit_messages.py" "$1"
