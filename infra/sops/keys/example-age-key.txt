# *** THROWAWAY DEMONSTRATION KEY — NEVER USE FOR A REAL ENVIRONMENT ***
#
# Generated with `age-keygen` solely so that infra/sops/secrets.dev.example.enc.yaml can be
# decrypted by anyone who clones this repo, to prove the SOPS/age plumbing actually works
# end-to-end (see infra/sops/README.md "Try it"). It protects nothing: the "secrets" it decrypts
# are placeholder strings, and this file itself is committed, which a real age private key must
# never be (docs/04 E-19). The real per-environment keys are generated by
# infra/scripts/bootstrap_age_key.sh and live only in the operator's password manager and in each
# CI environment's encrypted secret store — never in git.
#
# Public key: age1zmfzuzlqkan95eslq627aa743ka46nz6zyaa3njktl8zdwa94shq68tnw4
AGE-SECRET-KEY-1KCNJNRG0CCMNK5KJACGDAFQ8D4A6PSH46X9LJ42UHZKA94DNSQSSRKQD6N
