# PLAINTEXT SOURCE for infra/sops/secrets.dev.example.enc.yaml — kept only so the example is
# reproducible (`sops -e` this file to regenerate the encrypted one). Every value below is a
# placeholder; none of them work against anything real. A real environment's plaintext is never
# written to disk outside `sops edit` (which decrypts to a tmpfile, opens $EDITOR, re-encrypts on
# save, and never leaves the plaintext behind) — see infra/sops/README.md.

POSTGRES_PASSWORD: replace-me-not-a-real-password
DATABASE_URL: postgresql+psycopg://infraqueue:replace-me@localhost:5432/infraqueue
R2_ACCESS_KEY_ID: replace-me
R2_SECRET_ACCESS_KEY: replace-me
SENTRY_DSN: https://replace-me@example.ingest.sentry.io/0000000
MODEL_PROVIDER_API_KEY: replace-me
BSKY_APP_PASSWORD: replace-me
