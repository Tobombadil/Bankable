"""Public-tier FastAPI service (docs/23-api-spec-outline.md via api/openapi.yaml).

Scope this sprint (per the backend-developer brief): public-tier read operations only. Pro,
API-key and admin surfaces are not implemented — every route here is `x-tier: public` in
`api/openapi.yaml`.
"""
