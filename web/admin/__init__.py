"""Admin panel pages (Sprint 3 item 3, docs/20 §8, docs/30-design-ia.md §1.3): server-rendered
screens over `/admin/v1`, one module per nav group. `shell.py` is the coordinator-owned
contract every page module builds on: the operator guard, the cookie-forwarding API helper,
the chrome template and its stylesheet.
"""
