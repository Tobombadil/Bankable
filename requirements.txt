# python -m venv .venv && .venv/bin/pip install -r requirements.txt
gridstatus>=0.36
pandas>=2.2
pyarrow>=15
requests>=2.31
pyyaml>=6
openpyxl>=3.1
rapidfuzz>=3.9
pytest>=8
pyshp>=2.3               # pipeline/context/regions.py: Census/Natural Earth shapefiles, pure-Python (no geopandas, ADR 0008)
websocket-client>=1.8    # pipeline/connectors/us_epa_class_vi: EPA's Class VI tracker is a public
                         # Qlik Sense app whose only machine route is the Engine JSON API over a
                         # websocket. Imported lazily inside fetch(); parser tests need it not.
pdfplumber>=0.11         # pipeline/connectors/us_tx_rrc_class_vi: the RRC's Class VI list is a vector-table PDF (ADR 0002, docs/20 §15)

# services/db, services/ingest, services/api (Sprint 2 backend)
sqlalchemy>=2.0,<2.1
alembic>=1.13,<1.14
geoalchemy2>=0.15,<0.16       # canonical Postgres migration only; the SQLite test target has a
                               # pure-Python fallback (services/db/types.py) and does not need it
fastapi>=0.141,<0.142         # raised from 0.115 on 2026-09-15: starlette 0.46 carried seven advisories; 0.141 accepts starlette 1.x
starlette>=1.3.1               # PYSEC-2026-248/249 fixed in 1.3.1 (pip-audit, first CI run)
pydantic>=2.7,<3
uvicorn>=0.30,<0.31
httpx>=0.27,<0.28             # FastAPI TestClient
jsonschema>=4.22,<5           # tests/test_api_contract.py

# services/api/auth.py, services/alerts/ (Pro tier and alerts)
argon2-cffi>=25.1,<26         # password hashing (services/api/auth.py)
itsdangerous>=2.2,<3          # signed session cookies (services/api/auth.py)

# web/ (Sprint 2 public-site prototype: FastAPI + Jinja2 + htmx, docs/20 §15)
jinja2>=3.1,<4
python-multipart>=0.0.9,<0.1  # form parsing for FastAPI/Starlette
playwright>=1.47              # web/test_e2e.py smoke path (docs/04 E-10); browsers pre-installed

# lint and type gates (docs/04 E-5)
procrastinate==3.9.0          # infra/scheduler (ADR 0004); CI mypy --strict and the cadence tests import it
coverage>=7.6                 # ci.yml "pytest with branch coverage, 80% floor" (docs/04 E-7)
sentry-sdk==2.69.2            # infra/observability.py: env-gated (SENTRY_DSN) error tracking, docs/60 §7
ruff==0.16.7                  # pinned 2026-09-15: unpinned upgrades turned the base branch red (docs/00-PLAN.md Sprint 3 close)
mypy==2.3.1                   # pinned 2026-09-15, same reason
