# python -m venv .venv && .venv/bin/pip install -r requirements.txt
gridstatus>=0.36
pandas>=2.2
pyarrow>=15
requests>=2.31
pyyaml>=6
openpyxl>=3.1
rapidfuzz>=3.9
pytest>=8

# lint and type gates (docs/04 E-5)
ruff>=0.6
mypy>=1.11
