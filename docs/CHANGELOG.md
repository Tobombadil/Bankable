# Changelog (one line per material doc/code change, newest first)

- 2026-09-15 legal-compliance: added `docs/13-legal-data-rights.md` §2.9 (OurGridFuture — NOT RETRIEVED: 403 Cloudflare on ourgridfuture.org, 429 Vercel checkpoint on opengridworks.com/attribution), §2.10 (OSM-derived Protomaps basemap tiles — ODbL §4.4–4.6 and OSMF Produced Work/Attribution Guidelines quoted; position that self-hosted tile rendering is a Produced Work with no share-alike duty on Bankable's own database, open question 7(b) on an OSM data layer left open), §2.11 (EIA-860M copyright/reuse page quoted for the context layer); updated the §6 register table; added `data/sources.yaml` entry `us.ourgridfuture.transmission_projects` (`reuse: unknown`, tier 2, NOT RETRIEVED).
- 2026-09-13 backend-developer: Pro tier and alerts. `services/db/models.py` + migration `0003`: `account`, `user` (role vocab now includes `legal`, matching docs/21 §3.12's 2026-09-12 note — `api/openapi.yaml`'s `UserRole` enum was stale on it and is corrected here too), `session`, `api_key`, `saved_search`, `alert`, `webhook_endpoint` (secret stored in the clear, not hashed — signing an outbound delivery needs the actual value, unlike a bearer key), `webhook_delivery`. `services/api/auth.py`: argon2 password hashing, signed-cookie sessions backed by a revocable server row, API-key generation/hashing/resolution, a dry-run `EmailPort`/`ResendEmailAdapter` (real send only with `RESEND_API_KEY` set), and `AuthContext`/`require_*` FastAPI dependencies resolving `scopes ∩ plan_tier` per docs/23 §5 (a deliberate addition to docs/20 §7's passwordless design, recorded as a decision — no public register/login endpoint added since none exists in `api/openapi.yaml`). `services/api/visibility.py` extended (not rewritten) with an `entitlement` parameter: `pro`/`api` read `published_at` at zero lag where `public` reads `public_at`; `restricted`/`unknown` sources stay invisible on every tier, unchanged. `services/api/ratelimit.py`: a real in-memory per-tier token-bucket limiter (interface-compatible with a future Redis one) wired into both the existing public-tier middleware (per-IP, anonymous traffic only) and every new Pro/API route (per key/session); known gap recorded — a Pro/API credential calling the original Sprint 2 public routes directly isn't yet metered. `services/alerts/`: `matching.py` (saved-search/webhook query grammar against one row), `evaluate.py` (`run_alert_cycle` — matches new events since each saved search's `watermark_seq`, groups every match in one pass into a single digest `Alert` per channel, sends via `EmailPort`, attribution and a source link in every body), `feed.py` (unguessable `rt_` tokens, the private live `/feeds/saved/{token}` feed reusing `services/api/feeds.py`), `webhooks.py` (HMAC `X-Platform-Signature`, matching/enqueue/replay, `attempt_delivery`/`deliver_pending` against an injectable `Transport` with exponential backoff and pause-after-24-consecutive-failures). `services/api/pro.py`: `/v1/me`, `/v1/account`, saved-search CRUD + preview, alert history, key list/create/revoke (rotation is create-then-revoke — no separate endpoint exists in the spec), webhook CRUD/test/replay/deliveries, and an interim admin-only `PUT /admin/v1/accounts/{id}/entitlement` (billing/CRM is Sprint 3). `api/openapi.yaml`: 21 operations flipped `x-status: planned` → `live`, the new admin operation added, `UserRole`/`AuthProvider` enums corrected; `api/check_story_coverage.py` still reports 44/44 stories and all `$ref`s resolve. 97 new tests (`tests/test_api_pro_*.py`, `tests/test_alerts_*.py`) plus 6 new Pro-tier contract-schema tests in `tests/test_api_contract.py`; 588 total across `tests`, `services/db`, `services/ingest`, `services/api`, `pipeline`, all passing; `ruff`/`ruff format`/`mypy --strict` clean on every touched file. Full decisions list, endpoint table and verbatim command output in `services/README.md` "Pro tier and alerts". Remaining for billing (Sprint 3): the real Stripe/CRM adapter behind `account.entitlement_source = "sor"`, replacing today's `manual_grant` admin endpoint; magic-link/Google session issuance (the auth *mechanism* is built, the login endpoint is not, since none exists in the spec); metering Pro/API traffic that calls the original public routes; a scheduled worker actually calling `run_alert_cycle`/`deliver_pending` on an interval (Procrastinate integration, not built this sprint).

- 2026-09-13 product-designer: restyled the public site to the docs/30 reference study and docs/31 design system without changing what data it shows or how it's fetched (`web/templates/*.html`, `web/static/css/styles.css`, `web/static/js/map.js`). Document idiom per docs/30 §1: a running masthead (`INFRAQUEUE REGISTER · PUBLIC EDITION`, live delay figures) above the nav; decimal section numbers (`1. Sources`, `2. Lifecycle timeline`) on the about and detail pages; the lifecycle timeline rebuilt as a ruled ledger (mono date column + hairline rows) instead of a dotted activity feed; the delayed-tier notice restyled from a filled alert banner to a quiet left-rule document stamp, with the dev-preview notice kept visibly distinct (dashed copper rule) since it only renders in non-production builds. Map: cluster marks changed from filled navy discs to thin rings on the paper ground (family colour carried by the ring stroke and count text); the OSM raster and fallback basemap tokens desaturated and tinted toward the paper ground so land/water/borders sit beneath the data; the legend changed from colour swatches to icon+label pairs reusing the chip icon set (D-5); the detail drawer rebuilt as a document panel (ledger field rows, a provenance-bar source line, CSS-driven 200ms open / 150ms close per docs/31 §1.5) with every map animation (`flyTo`/`easeTo`/`zoomIn`/`zoomOut`) now honouring `prefers-reduced-motion`. Typography: Newsreader/Plex Sans/Plex Mono given explicit optical sizing and a global `tabular-nums` rule; the map filter bar rebuilt as an underline-only control strip (mono small-caps labels) instead of a bordered form. Fixed two dark-mode contrast bugs introduced mid-task (stamp label and delayed-notice rule were hardcoded to the light-mode `--color-ink`/`--color-copper-deep` tokens instead of the theme-aware `--text`/`--link` roles). Removed internal doc-path citations and one prototype aside from reader-facing copy (proposal detail, not-found, about, home map). `pytest web/test_e2e.py` passes unmodified (1 passed) and regenerated all six screenshots in `web/screenshots/`.
- 2026-09-12 backend-developer: added `api/openapi.yaml` (OpenAPI 3.1, 118 operations / 248 schemas / 117 parameters, every endpoint group in `docs/23` §3, §4, §9; attribution+licence envelope with `redactions[]` on every data response; `x-tier` + `public_at` semantics; API-key scopes and `RateLimit-*`/`Retry-After`/`Sunset` headers; cursor pagination and the §7 filter grammar as reusable parameters; one RFC 9457 `Problem` schema with the §8 `type` URIs; `/v1/proposals/geo` and `/v1/opportunities/geo` returning clustered GeoJSON with county-centroid precision for restricted sources; `x-prd-stories` on every operation), `api/check_story_coverage.py` (44/44 PRD stories covered, all `$ref`s resolve, every example validated; exits non-zero on failure) and `api/README.md` (viewing, validation with `openapi-spec-validator` 0.9.0 — `OK` — story-coverage table, and 19 open decisions for the solutions-architect, incl. one document vs three, the missing `legal` role in `docs/21` §3.12 user roles, branded webhook header names, and the class-split lag default).
- 2026-09-12 product-manager + solutions-architect + product-designer: added `docs/04-standards.md` (cross-discipline standard, 126 numbered rules P/D/E/DA/API/S/O/G/R with rationale; map-first design rules incl. reference study `docs/30-design-references.md` and banned patterns; DoD per artifact type; exceptions register; §10 resolves 11 conflicts between existing docs — lag defaults, restricted sources on Pro, `under_construction` state, event vocabulary, publisher location, SPP/NYISO/ISO-NE reuse class, `licence_id` naming, X cap, search latency, intake contacts, doc numbering; needs follow-up edits to `docs/21` §7.1, `data/sources.yaml`, `.claude/agents/content-social.md`). Owner directions applied: no default AI aesthetic; deal workflow is a later consideration, S0 = operations team; product name is `{{PRODUCT}}`/`{{DOMAIN}}`.
- 2026-09-12 data-scientist: added `docs/22-entity-resolution-and-change-detection.md`, `pipeline/{normalize,resolve,diff}.py`, `pipeline/status_map.yaml`, `pipeline/README.md`, `requirements.txt`, `tests/` (63 passing), `data/eval/{normalized,matches,clusters,events}.parquet` + `labels.csv` (88 hand-labelled pairs). Measured: 174/2,401 active ISO queue records link to EIA-860M (7.2%); P 0.927 / R 0.950 at threshold 75 (n=85, in-sample after rules; 0.78/0.90 before).
- 2026-09-12 legal-compliance: added `docs/13-legal-outreach-and-social.md` (CAN-SPAM, PECR/GDPR, TCPA, CASL, LinkedIn/X/Bluesky/Meta/Reddit rules, AI disclosure incl. AI Act Art. 50 and CA B.O.T. Act, human-sends L0–L4 rule set, suppression/record-keeping, channel gate, 10 counsel items).
- 2026-09-12 legal-compliance: added `docs/13-legal-data-rights.md` (verbatim ISO/licence/news terms with URLs and dates; PJM three-document conflict, SPP commercial-use bar, Google News robots Disallow, GEM CC BY-NC trap; scraping law, EU database right + DSM Art. 4 signals, personal-data rules, publication matrix, 12 counsel items) and updated `license`/`reuse`/`notes` in `data/sources.yaml` for 14 sources with verbatim clauses.
- 2026-09-12 solutions-architect: added `docs/21-data-model.md` (ERD, 22 entities field-level, event log with reversible merges, lifecycle state machines, reuse-class gating, corrections C-1..C-7 to docs/20), `docs/23-api-spec-outline.md` (endpoints by tier mapped to all 44 stories, auth/scopes, rate limits, errors, feeds/webhooks, attribution envelope; OpenAPI YAML due Sprint 1), `docs/adr/0001`–`0006` (0005 hosting and 0006 CRM/ERP proposed pending owner).
- 2026-09-12 data-engineer: added the Sprint 1 connector framework `pipeline/connectors/` (base contract fetch/parse/normalize with the provenance quartet + `raw`, registry over `data/sources.yaml` with code-enforced publication gating and a quarantine store, polite HTTP with per-host rate limits/backoff/robots, DQ gates per docs/04 DA-6, snapshot+diff runner writing `source_run` records, CLI `python -m pipeline.connectors`) and ten connectors: ERCOT GIS (1,778 rows), ERCOT large load (EMIL watch, 0 rows — the Protocol 3.2.7 report is not published; registry id NP3-990-CD does not exist), CAISO (2,278), NYISO (1,814), EIA-860M (2,341), NESO TEC (2,198), grants.gov (151), TED (698), Find a Tender (11), World Bank procurement (100, sector filter `sector.sector_code` confirmed). Recorded fixtures + 208 tests (parsers, gating, DQ, politeness, ERCOT end-to-end), ruff/mypy config and clean on `pipeline/`, `data/sources.yaml` verified fields refreshed, `pipeline/README.md` rewritten, `pipeline/pull.py` deleted (superseded).
- 2026-09-12 Added `docs/10-prd-mvp.md`: MVP PRD v0 (problem/thesis, six segments + routing team JTBD, scope with PJM/MISO gates, 44 user stories with acceptance criteria, 13 metrics, Sprint 1–3 plan, assumptions A-1..A-13, definitions of done for Sprint 0 deliverables) — product-manager.
- 2026-09-12 sales-bd: added `docs/33-gtm-and-sales-playbook.md` (ICPs, research/scoring, outreach sequences, discovery plan, partnerships, pricing, CRM, automation boundary).
- 2026-09-12 Added agent roster (`.claude/agents/`), `CLAUDE.md`, operating model doc.
- 2026-09-12 Phase 0: feasibility, source registry, probe script, master plan.
- 2026-09-12 solutions-architect: added `docs/20-architecture.md` (system spec v0). Companions 21/23/ADRs pending after agent interruption.
- 2026-09-12 content-social: added `docs/32-social-operating-playbook.md` (channels, account checklist, editorial, post pipeline, KPIs).
- 2026-09-12 market-researcher: added `docs/11-market-and-competition.md` (competitor teardown, bottom-up sizing, pricing ladder with delay schedule, demand-side counts, discovery guide).
- 2026-09-12 product-manager (coordinator): PRD US-104 elevated to map-first navigation per owner; US-901 and US-1001 amended for docs/21 corrections C-5 and C-7.
- 2026-09-12 coordinator: owner direction applied — existing Bankable workflow is a later consideration, not foundational (PLAN, CLAUDE.md, README, PRD v0.1, agent briefs, addenda on 01 and 11).
- 2026-09-12 coordinator: applied docs/04 §10 corrections — `under_construction` added to docs/21 proposal lifecycle; content-social brief points at docs/32; sources.yaml already carried legal's SPP/ISO-NE/NYISO classes.
- 2026-09-12 market-researcher + legal-compliance: added `docs/12-naming-shortlist.md` (brief and principles; 43-name longlist in three families with rejection reasons; five finalists Switchyard, Busbar, Isoline, Catchment, Platbook cleared on .com/.energy/.io via RDAP with calibration controls, Bluesky, LinkedIn slug, indexed USPTO records, collisions and ES/FR/DE/PT meaning; X and .co unverified (tooling blocked); USPTO/EUIPO/TMview are JS shells so all trademark findings are preliminary for counsel; ranked 1 Switchyard, 2 Platbook, 3 Busbar, 4 Isoline, 5 Catchment; owner chooses).
- 2026-09-12 coordinator: added the `legal` user role to docs/21 §3.12 — US-905 AC1 needs it to clear a source gate flag; unblocks the API gate endpoint (api/README.md open decision 2).
- 2026-09-12 coordinator (inline, no subagent): round two of naming appended to docs/12 — ten single-word candidates knocked out on live collisions; Gridslate recommended as placeholder, Siteplot as fallback.
- 2026-09-12 coordinator (inline): round three of naming — owner's `infra-` suggestion checked; infraqueue.com found unregistered with a free handle and no collisions; recommended as placeholder, infrafeed as fallback.
- 2026-09-12 product-designer: added `docs/30-design-ia.md` (three sitemaps as Mermaid — public/Pro/admin; nav
  model with map and search as co-equal entries per D-8..D-14 and US-104 AC1-5; page inventory tracing every
  page to PRD story ids, API reads and tier-gated elements; six core-job flows as Mermaid flowcharts; five
  wireframes — map-first home, proposal detail + drawer, opportunity list, Pro alerts, admin source health —
  with a state inventory per screen for empty/loading/error/delayed/restricted-precision/unplaced).
- 2026-09-12 product-designer: added `docs/31-design-system.md` (tokens with hex/role/contrast carried forward
  from `docs/30` §6, five-family status system marked adopted-pending-ratification; type/spacing/grid scales
  with 400px breakpoint; 13-component inventory with anatomy and states incl. status chip, provenance panel,
  map marker/cluster, detail drawer, filter bar, source health card, post review card; data-display, WCAG 2.2 AA
  and motion rules; a full `docs/04` §2 rule-to-implementation checklist).
- 2026-09-12 product-designer: added `docs/30-design-references.md` (D-6 study; andrewtgibson.com plus 15 products — the required Electricity Maps/Grid Status/Interconnection.fyi/Felt/Kepler.gl/Observable/FT/The Pudding/Linear/Stripe docs/Ember set (Ember and several live apps blocked, noted not invented) and four found independently — Flightradar24, OpenInfraMap, Global Energy Monitor, Vercel; synthesis table citing a docs/04 §2 rule per borrow/adapt/reject; typeface decision keep Newsreader+IBM Plex Sans/Mono with variable-font and OFL licence confirmed; type/spacing scale matching D-22; a proposed five-family status-colour system extending D-20/D-25 with WCAG contrast computed, not guessed, for every token incl. new success/danger/progress hues; map interaction model, motion rules and a banned-patterns checklist for D-7). Needs owner/PM ratification on the new status-colour families (REF-2) before docs/31 treats them as canon.
- 2026-09-13 frontend-developer: fixed a map defect flagged in review — every screenshot showed clusters floating on a blank white basemap. Diagnosis: `tile.openstreetmap.org` (and, tested as alternatives, `tiles.openfreemap.org` and `demotiles.maplibre.org`) all reset mid-TLS-handshake specifically for Chromium in this sandbox (plain `curl`/`urllib` through the same proxy succeed), and — the more significant finding — a raster tile source declared in MapLibre GL JS 5.24.0's *initial* style whose tiles all fail keeps the `load` event from ever firing at all, taking the whole map (data layers included) down with it; waiting for MapLibre's `idle` event does not help since `idle` has the same dependency. Fix in `web/static/js/map.js`: the OSM tile source is now added imperatively inside the `load` handler instead of the initial style, and a same-origin, always-available fallback outline layer (`web/static/data/basemap_fallback.geojson` — world countries + US states, Natural Earth and US Census Bureau TIGERweb, both public domain, generated by the new `web/data_ref/build_basemap_fallback.py`) renders underneath it, so the map reads as a map whether or not tiles load; OSM tiles remain the production default basemap. Re-ran the Playwright smoke test (now also asserting the fallback layer renders with tiles unreachable) and regenerated all six screenshots; `ruff`/`mypy`/`pytest web` still clean.
- 2026-09-12 frontend-developer: built the Sprint 2 public-site prototype under `web/` (public delayed tier only). `web/build_data.py` reads the latest normalised parquet per source (ERCOT, CAISO, NYISO, EIA-860M, NESO TEC; grants.gov, TED, Find a Tender, World Bank), applies the 14-/7-day delayed-tier rule (`--no-lag` flag for the prototype, delayed-tier notice always rendered), the D-9 restricted-precision rule (CAISO/NYISO derived-only per the legal register outcome — county centroid only, no raw), and D-8 placement precedence (exact point from EIA-860M only, else county centroid via a vendored Census Gazetteer 2024 lookup, else a state-aggregate marker, else Unplaced) into `web/static/data/{proposals.geojson,opportunities.json,stats.json}`; every feature carries the provenance quartet. `web/app.py` (FastAPI + Jinja2 + htmx) serves map-first home, proposals/opportunities lists with filters and pagination, detail pages with provenance panel/timeline/attribution, search and an About page reading `data/sources.yaml`; `web/static/js/map.js` runs MapLibre GL JS (CDN-pinned 5.24.0) with client-side clustering by lifecycle family, a synced accessible in-view list, and a focus-trapped drawer. Tokens, five-family chips and dark/light theming from `docs/31` implemented in `web/static/css/styles.css`; responsive to 400px. `web/test_build_data.py` (7 tests, built from `data/eval/normalized.parquet`) and `web/test_e2e.py` (Playwright smoke: map → list → detail, screenshots at desktop and 400px) pass; `ruff`/`mypy` clean on `web/`. Departures from `docs/31` (Google Fonts CDN instead of self-hosted; client-side MapLibre clustering instead of a server `/geo` endpoint; no map bottom-sheet or manual dark-mode toggle this sprint) recorded in `web/README.md`.
- 2026-09-12 data-engineer: added two stage-evidence connectors and a docket-linkage script for `docs/22` §10. `us.ferc.elibrary` (new `kind: "document"` on `base.Connector`, `base.DOCUMENT_COLUMNS`, and matching `dq.py` entries — docs/21 §3.8's first connector) polls the eLibrary `AdvancedSearch` JSON backend for ER/CP docket filings; two operational findings recorded in the connector docstring and `data/sources.yaml`: `filterDate` has no measured effect (filtered client-side instead) and `sortBy` values other than `""` return HTTP 200 `success:false` (a real captured null-reference error, fixture `ferc_elibrary_search_error.json`) — docket-number substring search (`ER26`/`CP26`) is what actually surfaces recent filings. Live run: 131 documents. `us.permits_dashboard` groups the FAST-41 Permitting Dashboard's milestone-level CSV into one proposal record per project (milestones nested in `raw`), filtered to six energy/transmission `Project Sector` values (104 of 458 projects); found and corrected a stale resource id in `data/sources.yaml` (`q7va-8c67` is retired — 24,008 bytes of newline padding, zero columns in its Socrata metadata — the live table is `mcm3-xbid`). Live run: 104 projects. `pipeline/link_dockets.py` proposes FERC-filing-to-ISO-queue links by explicit queue-id/name match and a sponsor-vs-filer rapidfuzz match; building it surfaced and fixed three classes of false positive from boilerplate word overlap (generic org words, "INTERCONNECTION", US state names all now stripped before scoring — see the script's docstring for the measured before/after). Measured, not estimated, against today's live pull: of 1,673 active ISO queue records, 1 (0.06%) gained a docket link; the low yield is itself a finding for the `docs/22` §10 recalibration (a 30-day window over ~131 documents rarely cites a specific queue id or project name). 30 new tests (10 FERC parser, 10 permits parser, 10 link_dockets units), 396 total in `tests/`+`pipeline/`; `ruff`/`mypy --strict` clean on `pipeline/`.
- 2026-09-12 content-social: added `services/social/` (Sprint 2 syndication pipeline, up to but not including live posting — no accounts exist yet, docs/32 §2). `models.py`: `PostDraft`/`Post` (docs/21 §3.18) with review metadata and JSON round-trip. `editorial.py`: the docs/32 §3.1 eligibility rules and §3.3 templates for all nine postable event types (`proposal.new/status_changed/withdrawn`, `opportunity.rfp_opened/rfp_closing/awarded`, `funding.cancelled/reinstated`, `digest.weekly`) rendered from structured fields only, the §4.3 hard validation gates, deterministic clause-dropping on overflow, and an adapter from `pipeline/diff.py`'s prototype event shape. `queue.py`: JSON-backed review queue with §4.8 duplicate suppression (idempotency key, cross-event window with priority order, 30-day content-hash guard), approve/reject/edit, §4.4 SLA expiry, §4.5 scheduling stubs, and a §4.6 graduation check that always refuses (LinkedIn unconditionally). `publishers/`: abstract `Publisher` plus dry-run-only `bluesky`/`linkedin`/`x` stubs; Bluesky's optional live path refuses without `BSKY_HANDLE`/`BSKY_APP_PASSWORD` and is otherwise unimplemented. CLI `python -m services.social {draft,review,dry-run,report}`. 138 tests (`tests/test_social_*.py`), `ruff`/`mypy --strict` clean; verbatim output and recorded editorial assumptions in `services/social/README.md`. Found and documented a source-id naming mismatch between `pipeline/diff.py`'s demo output and `data/sources.yaml`'s canonical ids (blocks `draft` against `data/eval/events.parquet` as-is; confirmed working via a manual alias table — 170 events → 176 drafts).
- 2026-09-12 backend-developer: Sprint 2 backend, public tier only. `services/db/`: SQLAlchemy 2.0 models for the 13 public-tier entities (`licence`, `source`, `source_run`, `snapshot`, `organization`, `organization_alias`, `location`, `proposal`, `proposal_source`, `opportunity`, `opportunity_source`, `event`, `match`) with the provenance quartet and licence columns `NOT NULL`, vocabularies as `text` + `CHECK` per docs/21 §1, the append-only `event` table (idempotency key, `seq` sequencing), and an Alembic migration (`services/db/migrations/versions/0001_initial_schema.py`) writing the canonical Postgres 16 + PostGIS DDL — not executed against a live database, since Postgres is not installed in this environment (`services/README.md` states the caveat and the SQLite test-target rationale in full). `services/ingest/loader.py`: an idempotent loader from a connector's normalised parquet + `pipeline/diff.py` events into the store, refusing any source whose registry `reuse` is `restricted`/`unknown` with an independent second check on the licence row it writes ("both must hold"), and computing `public_at` from `published_at` + a 14-day (supply) / 7-day (opportunities) lag per source class (`services/ingest/lag.py`). `services/api/`: a FastAPI app implementing 25 public-tier operations from `api/openapi.yaml` (proposals/opportunities list+geo+detail+events+sources, organizations, the global event feed, sources/licences registers, vocabularies, health, RSS/JSON Feed) with the attribution envelope, cursor pagination, an honest filter-parameter allowlist (`400 unknown_parameter` for anything not actually implemented, never silently dropped), RFC 9457 errors, static rate-limit headers, and GeoJSON clustering with the restricted-precision rule; matches/documents/intake/admin endpoints deferred (no upstream producer this sprint) and listed in `services/README.md`. 59 tests pass (`services/db`, `services/ingest`, `services/api`, plus `tests/test_api_contract.py` validating live responses against `api/openapi.yaml`'s own schemas with `jsonschema`); `ruff`/`ruff format`/`mypy --strict` clean on `services/` (added to `pyproject.toml`'s mypy `files` and pytest `testpaths`). Branch coverage 83% overall; the visibility predicate (`services/api/visibility.py`) and both licence-gate refusal paths are 100%. Eleven open decisions (no cross-source resolution yet, no geocoding, record `publish_state` defaults to `public` given the source-level gate already ran, the docs/20 vs docs/21 lag-default conflict resolved by following docs/04 §10, source-level public-tier gate requires `publish_state = 'public'` exactly, single-field sort, forward-only cursors, named filter subset, pure-Python geo clustering, ORM-side `event.seq` assignment, static rate-limit headers, generic licence-flag derivation) recorded in `services/README.md` rather than silently assumed.
- 2026-09-13 backend-developer: fixed the two `services/ingest/loader.py` limitations `services/resolve/README.md` documented (no public-interface change). Intra-run id reuse: a natural-key (`source_id`, `source_record_id`) match against a row from an *earlier* `load_dataframe` call is still an ordinary update; two distinct records (different connector `record_id`) sharing that key inside the *same* call are never merged — the second's `source_record_id` is suffixed deterministically (`#2`, `#3`, ...), mirroring `pipeline.connectors.base.Connector.finalize`'s own `record_id` suffixing, and a warning lands on both `LoadResult.warnings` and `source_run.dq`/`dq_status`; a dataframe repeating the *same* `record_id` (sequential snapshots bundled into one call, as `services/resolve/report.py` does) is correctly still an update, not reuse. Organisation slugs: `_get_or_create_organization` now falls back to a case/whitespace/punctuation-normalised match when the exact spelling misses, so two sponsor spellings differing only in punctuation resolve to one organisation with two `organization_alias` rows instead of raising `UNIQUE constraint failed: organization.slug`; letter-distinct names stay separate. Re-ran `services/resolve/report.py`: nyiso created rows 1,814 -> 3,164 (+1,350: the documented `0031`/`0127A` pair plus two large content-hash collisions among genuinely blank withdrawn rows, `he0f6cee65a13` x1,063 and `h07cc5855b76c` x287, previously silently collapsed), proposals before resolution 8,212 -> 9,563, after 7,770 -> 9,121; every downstream number (clusters merged 275/442, 3 gate-refused, >=2-source proposals 273, organizations 3,034 -> 2,784, precision/recall 0.946/0.946) unchanged, since the newly-preserved blank rows carry nothing the resolver can match. 6 new tests in `services/ingest/test_loader.py` (9 -> 15); full suite excluding `web/` and `tests/test_web_default_view.py` (another agent mid-edit on `web/`, pre-existing unrelated datetime bug) 464 passed; `ruff`/`ruff format`/`mypy --strict` clean on `services/db`, `services/ingest`, `services/ids.py`. Verbatim numbers and output in `services/README.md` "Loader fixes".
- 2026-09-13 data-scientist: added `services/resolve/` to make the store hold one `proposal`/`organization` row per real-world thing, reversibly (docs/21 §6.3/§6.4). `merge.py`: canonical-record choice (EIA id > most recent > lowest source id), a confidence gate (min pairwise score >= 75 among edges the store can actually see, independently re-checked rather than trusted from `pipeline/resolve.py`'s own `accepted` column, plus an id-reuse guard), and idempotent `merge`/`unmerge` for both proposals and organizations, each `merged` event carrying the absorbed row's full serialized state so `unmerge` restores it exactly without reading any other row (invariant M1) — proven by an exact field-by-field round trip in `tests/test_resolve_store_unmerge.py`. Departed, deliberately and measurably, from the task's literal id-reuse wording ("same source, different queue ids"): that reading refuses 73 of 281 loadable clusters, almost all legitimate multi-queue-position complexes (docs/22 §7.4); the reading actually implemented ("same source, same queue id, two distinct records" — the measured `isone:84`/`isone:84#5` bug, docs/22 §7.1) fires only on the 2 clusters that exhibit it (argued in full in `merge.py`'s docstring, `services/resolve/README.md`, and new docs/22 §13). `models.py` adds `resolution_decision` (docs/21 §6.4's named-but-unbuilt candidate-pair review queue) rather than misuse `match`, which is proposal↔opportunity only. `report.py` loads the real 2026-09-12 pull through the existing, unmodified `services.ingest.loader` (SPP/ISO-NE proven `GateRefused`, not skipped) and measures: proposals 8,212 → 7,770 (442 absorbed across 275 merged clusters; 3 clusters correctly refused for lacking direct evidence between loaded members); 273 proposals now carry >= 2 sources; organizations 3,034 → 2,784 (250 absorbed, deterministic normalised-name match only — no fuzzy pass without a labelled sample); store-path precision/recall on the 85 hand labels (77 usable) 0.946/0.946, not worse than `pipeline/resolve.py`'s own 0.927/0.950. Two pre-existing `services/ingest/loader.py` limitations found and documented (not fixed, out of scope): its per-source id uniqueness constraint silently collapses true id-reuse duplicates at ingestion before this gate ever runs; its organization slug has no collision suffix across punctuation-only spelling variants (worked around locally in `report.py`). 20 new tests (`tests/test_resolve_store.py`, `tests/test_resolve_store_unmerge.py`), 463 total; `ruff`/`ruff format`/`mypy --strict` clean.
- 2026-09-13 frontend-developer: converted the public site from reading `web/build_data.py`'s static JSON to reading `services/api` live, over HTTP or in-process (`web/api_client.py`; `web/dev_up.py` is the `make dev` equivalent — loads `data/normalized/*` through `services/ingest/loader.py` into SQLite and starts the API and site as two processes; pytest uses the in-process path). Map clustering moved server-side (`GET /v1/proposals/geo`, proxied at `/api/proposals/geo`); `web/static/js/map.js` renders what the API returns and no longer clusters itself. Fixed three product defects: (A) the default map/list now show active lifecycle states only (announced through under_construction) with withdrawn/cancelled behind an explicit "Include withdrawn & cancelled" toggle, and the notice above the map/list states the real active-vs-withdrawn counts computed from the live API (`web/viewmodels.py::lifecycle_breakdown`); (B) the placeholder-name note moved to `/about`, the footer is one line stating the real configured lag plus a sources link; (C) the detail-page provenance panel collapses each source's licence text behind `<details>`, with the source name, reuse-class badge and retrieval date visible outside it (`web/viewmodels.py::provenance_panel_rows`). Delayed tier is real: `public_at` from the API governs visibility everywhere, no lag bypass in the default path; a dev-only `--preview`/`WEB_DEV_PREVIEW=1` override (labelled in the UI when active) exists for previewing today's rows. Also applied, from `web/data_loading.py` only (no `services/` code touched): a US-county-centroid geocode backfill (the loader never geocodes, so nothing would plot without it) and a correction to CAISO/NYISO's `allows_raw_publication` flag and `location.precision_reason` (the loader hardcodes the former `True` for every source and never sets the latter for any source, so the restricted-precision note could never trigger otherwise) — both are documented workarounds for gaps in `services/ingest/loader.py`, not fixes to it. `tests/test_web_default_view.py` and `tests/test_web_provenance.py` (new) plus the updated `web/test_e2e.py` (now loads a real SQLite database instead of static JSON, samples the real per-source data to work around a measured 30-75s/page performance gap in `services/api/visibility.py`'s per-row query at full ~10,400-row volume) — 18 tests, all passing; `ruff`, `ruff format` and `mypy --strict` clean on `web/`. Full details, missing-API list and verbatim command output in `web/README.md`.
- 2026-09-13 devops-engineer: Sprint 2 DevOps. ADR 0005 marked Accepted (owner default D1, `docs/00-PLAN.md`); compute provider decided as Hetzner Cloud (`cx32`, `ash`), DNS/object storage kept on Cloudflare (R2 + DNS) per the ADR's own already-Accepted text, recorded in the ADR and `infra/terraform/versions.tf`. `infra/terraform/`: OpenTofu for 3 Hetzner VMs (app/worker/browser-worker) behind a private network and two firewalls, a Cloudflare DNS record set and an R2 bucket, no secrets, `tofu fmt`/`init`/`validate` clean and `tofu plan` reaches real provider auth (no live account exists to go further). `infra/compose/`: `docker-compose.yml` (postgres+postgis behind a `local` profile, api, web, scheduler, worker, browser-worker, social) + `compose.prod.yml` (Caddy TLS, no local Postgres, resource limits, healthchecks) — `docker compose config` merges cleanly for both `--profile local` and the prod overlay. `infra/docker/`: one multi-stage Dockerfile (`api`/`web`/`worker` targets) plus a Playwright/Chromium `Dockerfile.browser-worker`; both `hadolint`-clean. `infra/scheduler/`: a thin Procrastinate (ADR 0004) app — `cadence.py` buckets every `data/sources.yaml` cadence string (including the ambiguous/compound and event-driven ones) into a cron-expressible schedule, always rounding to the more frequent side; `app.py`'s periodic ticks enqueue `python -m pipeline.connectors run <source_id>` per due source, routed to the `fetch`/`fetch_browser` queue by `access: js_app` until DA-12's proposed `egress` field lands; 13 unit tests green. `infra/sops/`: SOPS + age, with a real, working example (a throwaway keypair round-trips `secrets.dev.example.enc.yaml` end to end, verified). `infra/scripts/`: `deploy.sh` (stop workers -> migrate -> restart api/web -> restart workers -> restart scheduler, per docs/04 O-4), `rollback.sh`, `backup.sh`/`restore_drill.sh` (R2-backed secondary copy of the managed Postgres provider's own PITR), `rotate_secret.sh`, `bootstrap_age_key.sh` — all pass `bash -n` and `shellcheck` (2 informational notes only). `docs/60-deployment.md`: the full design plus five docs/04 O-9-format runbooks (deploy, rollback, restore, source-blocked triage, rotate-secret) and a real, cited monthly cost table (Hetzner + Cloudflare R2 + Neon prices verified 2026-09-13; core total ≈ USD 80-170/mo today, ≈140-320 once `services/modelgw` exists — both inside the docs/04 O-8 ceiling). `.github/workflows/ci.yml`: the docs/04 O-3 gates (ruff, mypy --strict, import-linter via `infra/importlinter.ini`, pytest with an 80% coverage floor, OpenAPI validity + story coverage, an Alembic up/down/up job against a real Postgres service container — its first-ever live run, migrations job, gitleaks via `infra/gitleaks.toml`, pip-audit, image builds + trivy, axe/Lighthouse CI, docs link check), a stubbed preview-deploy job behind a GitHub environment secret; `.github/workflows/connectors-nightly.yml` runs the connector suite daily against fixtures only. `Makefile`: `dev` delegates to the already-built `web.dev_up --preview` (frontend-developer's own "make dev equivalent"); `test`/`lint`/`typecheck`/`test-web`/`test-core` all run today. Validated in this sandbox (after installing `tofu`/`sops`/`age`/`hadolint`/`gitleaks`/`actionlint` from their upstream release binaries, and Docker Compose's bundled CLI — the Docker *daemon* is absent, so no image was actually built): `infra/`'s own ruff/ruff-format/mypy --strict/pytest all clean; `test-core` 500 passed, `test-web` 8 passed at time of writing. `pipeline/`, `services/` and `web/` are being actively edited by other agents in this same shared session throughout this task (confirmed via concurrent, unprompted changes to `services/api/app.py`, `services/db/models.py`, migrations and new test files) — repo-wide `ruff`/`mypy` therefore show a moving, non-zero error count in those trees at any given snapshot; none are in this task's write scope (`infra/`, `.github/workflows/`, `docs/60-*.md`, `docs/adr/0005`, `Makefile`) and none are introduced by it. No secrets, no model identifiers, cost ceiling respected. Owner's first three manual steps are in `docs/60-deployment.md` §11.
- 2026-09-13 backend-developer: Sprint 2 fixes to `services/`. Fixed the reported 30-75s/page visibility-predicate performance gap: it was one missing index (`proposal_source.proposal_id`, `opportunity_source.opportunity_id`), not the predicate's shape — confirmed with `EXPLAIN QUERY PLAN` and measured 21.8s -> 0.13s for a list page over the real 10,409-proposal load; `GET /v1/proposals/geo` also needed narrower ORM loading (`load_only`, dropped eager `sponsor`) and SQL-side aggregates for its totals/licence-summary, landing at ~0.46-0.53s end-to-end (function-level profiling: ~0.35-0.46s) against the 500ms budget. `services/ingest/loader.py` now derives `Licence.allows_raw_publication` from the registry (CAISO/NYISO's own recorded terms, not a blanket `attribution` rule — GB NESO stays raw-ok), geocodes proposals via a new `services/ingest/geocode.py` (county/state Census centroids, ported off `web/build_data.py`'s table so `services/` no longer depends on `web/`), and defaults a gate-cleared source's `publish_state` straight to `public`. `services/api/geo.py`'s clustering grid is retuned (20-60 clusters at zoom 3-4 over the continental US, was 2-6) and `bbox` is now enforced, not just echoed. Added a `slug` list filter (proposals/opportunities), enforced the previously-ignored `technologies` filter on opportunities, and added `Licence.quote_text` (new column + migration `0002`) carrying `data/sources.yaml`'s free-text licence quote on the public `Source`/`Licence` resources. 487 tests pass (`tests pipeline services`, up from 464); `ruff`/`ruff format`/`mypy --strict` clean on the touched trees. Full writeup, before/after timings and cluster counts in `services/README.md` "Sprint 2 fixes".
- 2026-09-13 frontend-developer: switched the public site onto the full `data/normalized/*` load now that `services/README.md`'s "Sprint 2 fixes" made it fast enough to test against directly. Removed the three `web/data_loading.py` overrides the backend made redundant (`backfill_locations`, `apply_derived_only_licence_correction`, `_flip_publish_state_public` — the loader now geocodes and derives licence/publish-state itself); `backfill_eia_exact_points()` stays, documented as the one real gap left (EIA-860M exact-point promotion, still out of the loader's scope). `web/dev_up.py`'s sample flag renamed `--sample-per-state` -> `--sample` and reframed as a fast-iteration option, not a performance workaround; `web/test_e2e.py` and `tests/test_web_default_view.py` now load the full, unsampled ~11,400-row set (the latter switched off the separate `data/eval/normalized.parquet` fixture entirely). `web/app.py`'s slug-detail lookups now call the API's new `slug=` filter instead of scanning up to 200 search results; `web/viewmodels.py`'s provenance panel now renders `Licence.quote_text` verbatim instead of a paraphrase composed from permission flags. Regenerated all six screenshots on the full dataset at the default view: 5,837 active proposals shown, 3,215 withdrawn/cancelled hidden, 1,357 built/unknown-state outside the default, 1,825 unplaced; the map shows 22 clusters over the continental US at the default zoom (was 3). Page render times through the in-process API, full data, three runs each: home ~0.51s, list ~0.55s, detail ~0.02s. `ruff`/`ruff format`/`mypy` clean on `web/` (`mypy web`'s plain invocation surfaces 14 pre-existing errors in `services/alerts/feed.py`/`services/api/app.py` from another agent's concurrent, in-progress work there — confirmed unrelated via `mypy --follow-imports=silent web`, clean); `pytest web tests/test_web_default_view.py tests/test_web_provenance.py` 18 passed (451s, dominated by the loader's ~100-rows/second full-data loads). Full writeup and verbatim output in `web/README.md` "Full-data run".
- 2026-09-13 coordinator: Sprint 2 closed; Attio chosen over HubSpot as CRM (ADR 0006 updated); Sprint 3 kickoff hand-off written in docs/00-PLAN.md for a fresh session.
- 2026-09-13 coordinator: added `docs/34-crm-system-of-record.md`, the Attio object/attribute/relationship contract the owner builds and the adapter targets.
- 2026-09-13 backend-developer: Sprint 3 first wave. Built the Attio CRM adapter behind `services.sor.ports.CrmPort`: `services/crm/attio.py` (`AttioCrmAdapter`, retry/backoff on 429/5xx, `SorRejected`/`SorUnavailable` mapping, HMAC webhook verification), `services/crm/fake.py` (`InMemoryCrm` dry-run/test double with real read-back semantics), `services/crm/signals.py` (pure event-to-lead-signal mapping, docs/33 §2.3 timing component), and `services/crm/router.py` (`POST /webhooks/attio`, `POST /admin/v1/leads` for US-403). Added `api/fragments/crm.yaml` for the coordinator to merge (`/webhooks/attio` path + `AttioWebhookPayload`/`WebhookReceipt` schemas; also flags `/admin/v1/leads` to flip to `x-status: live`). 87 tests pass (`ruff`/`ruff format`/`mypy --strict` clean on `services/crm`; 96% coverage); `tests/test_api_contract.py services/api` (49 tests) confirm no regressions. Decisions, deviations and deferred work (the lead-signal-worker, `handled`-closes-task wiring, a filter-grammar assumption to verify live) are recorded in `services/crm/README.md`.
- 2026-09-13 backend-developer: Sprint 3 first wave. Built the Stripe billing adapter behind `services.sor.ports.BillingPort`: `services/billing/stripe.py` (`StripeBillingAdapter` over plain `httpx` form-encoded requests, no vendor SDK — customer/checkout/portal creation, subscription/invoice reads, webhook signature verification and event-to-`EntitlementChange` mapping, 429/5xx retry with `SorRejected`/`SorUnavailable` mapping), `services/billing/fake.py` (`InMemoryBilling` dry-run/test double sharing the real HMAC verification path), `services/billing/signing.py` (the shared Stripe webhook signature verifier), `services/billing/entitlement.py` (`apply_entitlement_change`: upserts the `subscription` mirror row, resolves `account.entitlement` via `entitlement_for`, guards `admin` accounts, idempotent on the Stripe event id, best-effort CRM mirror), and `services/billing/router.py` (`POST /webhooks/stripe`, `POST /v1/billing/checkout`, `POST /v1/billing/portal`, `GET /admin/v1/subscriptions`, plus an exported `subscriptions_for_account` helper for the coordinator's `GET /v1/account` wiring). Added the `Subscription` model (docs/21 §3.14) and migration `0004_subscription_mirror.py` to `services/db/`. Added `api/fragments/billing.yaml` for the coordinator to merge (`/webhooks/stripe`, `/v1/billing/checkout`, `/v1/billing/portal` paths and their schemas; flags `POST /admin/v1/subscriptions` to stay `planned` — the admin wave's write path). 82 tests pass in `services/billing` (96% branch coverage); `tests/test_api_contract.py services/api` (49 tests) confirm no regressions; `ruff`/`ruff format`/`mypy --strict` clean on `services/billing services/db`; `alembic history` resolves 0001->0004. Eight numbered decisions (notably: `Subscription.plan_tier` keeps docs/21's `free|pro|team|api` vocabulary, storing an `enterprise` purchase as `plan_tier="api"` with the vendor's exact `plan_code` preserved; `incomplete` Stripe subscriptions are "no change" at the webhook layer but fail-closed to `canceled` in `get_subscription`; the `subn_` public-id prefix was verified against `api/openapi.yaml` rather than assumed) and deferred items (the admin write path, mounting the router onto `services/api/app.py`, the `GET /v1/account` wiring point) recorded in `services/billing/README.md`.
- 2026-09-13 frontend-developer: added the login/registration surface on the existing session auth (Sprint 3, first wave). `services/api/auth_routes.py` (new router, `/v1/auth/register|login|logout|verify|resend-verification`) calls `services/api/auth.py`'s already-tested `register_user`/`authenticate_user`/`create_session`/`send_verification_email` (no public HTTP endpoint existed for them before this task, `services/README.md` "Pro tier and alerts" decision 2) and enforces the seat rule (US-602 AC2: login above `account.seats` non-revoked, unexpired sessions is `403 seat_limit`); appended `resolve_session_row`, `make_verification_token`, `read_verification_token` to `services/api/auth.py` (append-only, no existing function changed). `web/auth.py` (new router, `/login`, `/register`, `/logout`, `/verify`, `/account`, `/account/resend`) relays the session cookie between the web and API hosts (`web/api_client.py` gained `.post`/`.get_result`/`ApiResult` with per-request, non-persisted cookies) and enforces same-origin CSRF checks on every POST; dry-run mode (no `RESEND_API_KEY`) surfaces a `dev_verification_url` so the flow completes without a mail provider. 29 new tests (`tests/test_api_auth.py` 18, `web/test_auth.py` 11); `ruff`/`ruff format`/`mypy` clean on the touched files (`mypy web` shows only 2 pre-existing, unrelated errors in `web/build_data.py` and `services/ingest/loader.py`, confirmed via `git stash`). `api/fragments/auth.yaml` is a fragment for the coordinator to merge into `api/openapi.yaml`. Full writeup, decisions and verbatim output in `web/README.md` "Login and registration".
- 2026-09-13 backend-developer: follow-up per coordinator verification — `services/crm/attio.py`'s `_query_company` and the `lead_signals` lookup in `create_deal` now use Attio's verified verbose filter grammar (`{"$eq": value}`, confirmed against `docs.attio.com/rest-api/guides/filtering-and-sorting`) instead of the earlier unverified mixed shorthand/property shape; `services/crm/README.md` decision D-10 replaces the old A-34-2 assumption. `pytest services/crm` 87 passed, `ruff`/`ruff format`/`mypy --strict` clean, coverage unchanged at 96%.
- 2026-09-13 backend-developer: follow-up per coordinator verification — `services/crm/router.py`'s webhook and lead-hand-off handlers no longer put vendor exception text (`str(exc)`) into a response `detail`; they log at WARNING and return a fixed generic detail instead (`"The CRM could not be reached; try again shortly."` for `sor_unavailable`, no detail for the webhook's `401`), matching the pattern the lead route's `SorRejected` branch already used. Added `services/crm/README.md` decision D-11 and two router tests locking in that the vendor text never appears in a response body. `pytest services/crm` 88 passed, `ruff`/`ruff format`/`mypy --strict` clean.
- 2026-09-13 coordinator: Sprint 3 opened. Verified Attio API v2 and Stripe against their docs before coding; wrote `services/sor/ports.py` (CrmPort, BillingPort, `entitlement_for`) and `services/sor/wiring.py`; briefed and verified the three first-wave agents; merged their OpenAPI fragments into `api/openapi.yaml` (128 operations, `/admin/v1/leads` and `GET /admin/v1/subscriptions` flipped to live), mounted the auth, CRM and billing routers in `services/api/app.py`, wired the Stripe mirror into `GET /v1/account`, added the adapter import-boundary contract to `infra/importlinter.ini`; plan, ADR 0006 and services/README updated.
- 2026-09-13 backend-developer: implemented `BillingPort.create_subscription` (coordinator follow-up: `services/sor/ports.py` gained this member for US-902 AC2's operator-created subscription, no self-serve Checkout) in both `services/billing/stripe.py` (`POST /v1/subscriptions`, form-encoded, `collection_method=send_invoice`/`days_until_due=30`/`trial_period_days` when given, unknown plan → `SorRejected` before any call, mirror refreshed from the read-back via the existing `_subscription_state_from_object`) and `services/billing/fake.py` (`InMemoryBilling.create_subscription`, a per-plan fake MRR table, stored so a later `get_subscription` sees it). New tests in `services/billing/test_stripe.py` (request shape incl. `Idempotency-Key`, unknown plan, 4xx → `SorRejected`) and `services/billing/test_fake.py` (round-trip). `services/billing/README.md` gained the endpoint row and decision #10, flagging `days_until_due` and `trial_period_days` on the raw Subscriptions endpoint as unverified against the saved vendor pages (no dedicated "create a subscription" page was saved; `collection_method` is independently confirmed via a Stripe changelog reference in the pages that were saved). 79 tests pass in `services/billing`; `ruff`/`ruff format`/`mypy --cache-dir /tmp/mypy-billing` clean on `services/billing services/sor`.
- 2026-09-13 backend-developer: implemented Sprint 3 item 3, the admin panel's source-health, licence-gate, cost and audit surface. New `services/api/admin_sources.py` (router, mounted by the coordinator) covers all twelve `/admin/v1` operations from `api/openapi.yaml` (sources list/detail/create/update/run/publish-state, source-runs list/detail, snapshot detail, licence gate, costs, audit) exactly to their committed schemas. Notable decisions (full list in `services/api/admin_sources.md`): a `SourceRunner` protocol with a `QueuedSourceRunner` that enqueues `infra.scheduler.app.run_connector` under `queueing_lock_for` and wraps every queue failure into a fixed `503 unavailable` (never runs a connector inline); `Source`/`Licence` text ids get deterministic `uuid5` synthetic `event.subject_id`s for auditing (both PKs are text, `event.subject_id` is `uuid`); a licence reclassification creates a new `Licence` row per invariant L2 and repoints affected sources; `GET /admin/v1/costs` aggregates `model_call` + `source_run` in Python (SQLite-portable) with a documented rule that `CostRow.day` is always populated even when `group_by` omits it. Flagged two real `api/openapi.yaml` gaps for the architect: `SubjectType` has no `licence` value, and `AnyPublicIdValue`'s pattern excludes `source`/`licence`/`account`/`user`/`api_key` subject ids even though `SubjectType` lists them as valid. `tests/test_api_admin_sources.py` (63 tests, mounts the router onto the shared app itself pending coordinator wiring) covers every operation's happy path against the spec with `assert_valid`, 401/403/404/400/409/422 cases (each `gate_unmet` condition separately), audit before/after, cost aggregation and pagination. `pytest tests/test_api_admin_sources.py` 63 passed; `pytest tests/test_api_contract.py services/api` 49 passed (no regressions — `tests/test_api_admin_records.py` and `tests/test_web_default_view.py` fail/error independently of this change, confirmed in isolation); `ruff`/`ruff format`/`mypy --cache-dir /tmp/mypy-adm-src` clean; coverage 96% (branch) on `services/api/admin_sources.py`.
- 2026-09-13 backend-developer: implemented Sprint 3 item 3, the social review queue/channel switches, admin API-key issuance, and the public intake/report write endpoints. New `services/api/admin_posts.py` (router, mounted by the coordinator) covers `GET/PATCH /admin/v1/posts[/{post_id}]` and `.../approve|reject|schedule`, `PUT /admin/v1/channels/{channel}/auto-publish` (owner-only), `GET/POST /admin/v1/keys` + `DELETE /admin/v1/keys/{key_id}`, and the public `POST /v1/intake/proposals|opportunities` + `POST /v1/reports`, exactly to their committed (still `x-status: planned`) schemas. Notable decisions (full list in `services/api/admin_posts.md`): `POST_CHANNELS`/`POST_STATES` come from `services.db.models`, only the `REJECT_REASONS` constant is imported from `services.social.models` (no file-queue code dragged in); the reject endpoint's `ReasonRequest.reason` field doubles as the mandatory reject-reason code since the spec gives it no separate field; approve has no request body so its audit reason is a fixed system string; `channel_config` (no surrogate UUID key) gets a deterministic `uuid5(channel name)` as its audit `subject_id`; public intake/report endpoints create a `Task` with `pending_record` holding the validated submission verbatim and never touch `Proposal`/`Opportunity` directly (per `Task`'s own docstring — normalising into a real record is `adminApproveIntake`'s job, another agent's file); captcha is accepted but not verified this sprint (no provider wired), with a `website` honeypot and a shared `intake:<ip>` 5/hour bucket standing in, both flagged as follow-ups. `tests/test_api_admin_posts.py` (73 tests, mounts the router onto the shared app itself pending coordinator wiring) covers every operation's happy path with `assert_valid`, 401/403/404/400/409/422 cases (both `gate_unmet` conditions on approve, plus the channel-enable-without-disclosure one), audit before/after, idempotency-key replay and conflict for all three public endpoints, and the honeypot/rate-limit refusals. `pytest tests/test_api_admin_posts.py` 73 passed at 100% line coverage; `pytest tests/test_api_contract.py services/api tests/test_api_pro_keys.py` 58 passed (no regressions); `ruff`/`ruff format`/`mypy --cache-dir /tmp/mypy-adm-posts` clean on both files.
- 2026-09-13 backend-developer: implemented Sprint 3 item 3, the admin panel's users, task queue (reports, intake review, deletion requests), customers and subscriptions surface. New `services/api/admin_people.py` (router, mounted by the coordinator) covers all twelve `/admin/v1` operations from `api/openapi.yaml` exactly to their committed (still `x-status: planned`) schemas: users list/detail/update/delete, tasks list/detail/update/approve-intake, customers list/create/detail, and subscription creation (reusing `services/billing/router.py`'s `_serialize_subscription`/`subscriptions_for_account` and `services/billing/entitlement.py`'s `apply_entitlement_change`). Notable decisions (full list in `services/api/admin_people.md`): `Task.pending_record` is read as an already-AdminProposal/AdminOpportunity-shaped draft (per its own schema typing, not the raw intake-submission body, since that endpoint is unbuilt this sprint) with defensive field-mapping and a documented convention for the coordinator/future intake-endpoint author to confirm; the task brief's literal on-demand source id `platform.intake` was changed to `intake.platform` since the literal fails `SourceIdValue`'s pattern; `IntakeDecisionResponse.lead` is always `null` (the `Lead` schema requires a `match_id` this flow has none of) while the CRM writes still happen; only a user's own API keys (not the whole account's) are revoked on disable/delete/redaction; flagged for the coordinator/architect that `AnyPublicIdValue`'s pattern excludes `usr_`/`acc_`/`key_` even though `SubjectType` lists `user`/`account`/`api_key` as valid subjects, same gap already flagged by the `admin_sources` agent for `source`/`licence`. `tests/test_api_admin_people.py` (60 tests, standalone-app pattern per `services/crm/test_router.py`'s precedent so this suite never depends on the other three concurrent admin-panel files) covers every operation's happy path with `assert_valid`, 401/403/404/400/409 cases, audit before/after, the deletion-task redaction flow (CRM call first, then redact; `SorUnavailable` leaves both the user and the task untouched), intake approve/link/reject incl. `create_lead` landing a company+signal in the fake CRM, and customer/subscription creation through the fakes incl. `503` on adapter failure. `pytest tests/test_api_admin_people.py` 60 passed at 92% line coverage; `pytest tests/test_api_contract.py services/api services/billing/test_router.py services/crm/test_router.py` 84 passed (no regressions); `ruff`/`ruff format`/`mypy --cache-dir /tmp/mypy-adm-ppl` clean on both files.
- 2026-09-13 backend-developer: implemented Sprint 3 item 3, the admin panel's record editing, publish/unpublish, merge/unmerge, resolution review and extraction review surface. New `services/api/admin_records.py` (router, mounted by the coordinator) covers all thirteen `/admin/v1` operations from `api/openapi.yaml` exactly to their committed (still `x-status: planned`) schemas: proposal/opportunity GET+PATCH with `overrides`/`field_provenance`/ungated sources+events, organization PATCH, per-record publish-state (with takedown nulling event visibility), proposal merge (signed 15-minute preview token, then apply via `services.resolve.merge.merge_proposal`) and unmerge (`unmerge_proposal`), resolution-candidate list/decide (`same`/`different`/`defer` — the real enum; `ResolutionDecision`, `services/resolve/models.py`, has no counterpart in `docs/21` for a fourth outcome, so `defer` is a genuine no-op), and extraction list/accept/reject with the `docs/21` §8 licence gate on `allows_derived_publication`. Notable decisions (full list in `services/api/admin_records.md`): `services.ids.public_id` imported as `make_public_id` to avoid shadowing route path parameters also literally named `public_id`; `rc_`/`evt_` ids decoded via the same Crockford walk as `services/api/app.py::_find_event_by_public_id`/`services/crm/router.py::_find_match_by_public_id`; `merge_proposal`/`unmerge_proposal`'s own event is the audit record (this module sets `actor_user_id` on it directly rather than writing a second, duplicate `merged`/`unmerged` event via `record_audit_event`); `organization` has no `publish_state` column in `services/db/models.py`, so `PUT .../publish-state` on `record_type=organizations` is a documented `400`, not a fabricated no-op. `tests/test_api_admin_records.py` (82 tests, standalone-app pattern per `services/crm/test_router.py`'s precedent so this suite never depends on the other three concurrent admin-panel files) covers every operation's happy path with `assert_valid`, 401/403/404/400/409/422 cases, audit before/after, merge preview-then-apply-then-unmerge round trip (survivor keeps sources, absorbed row restored exactly), resolution confirm-merges/reject-does-not/defer-is-a-no-op, and extraction accept-changes-the-field/reject-leaves-it-unchanged. `pytest tests/test_api_admin_records.py` 82 passed at 96% line coverage; `pytest tests/test_api_contract.py tests/test_resolve_store.py tests/test_resolve_store_unmerge.py services/api tests/test_api_admin_records.py` 151 passed (no regressions); `ruff`/`ruff format`/`mypy --cache-dir /tmp/mypy-adm-rec` clean on both files.
- 2026-09-13 coordinator: admin backend mounted (`services/api/admin_sources.py`, `admin_posts.py`, `admin_people.py`, `admin_records.py`), 48 `/admin/v1` operations and the three public intake/report writes flipped to live in `api/openapi.yaml`, spec subject vocabulary and id pattern widened for admin audit events, task `pending_record` admits the submission shape, cross-module intake flow test added; admin tables and migration 0005 written as the shared contract; plan updated.
- 2026-09-13 backend-developer: coordinator follow-up on Sprint 3 item 3 — added `GET /admin/v1/channels` (`adminListChannels`) so the admin UI can read auto-publish state before an owner toggles it with the existing `PUT`. Returns one row per `POST_CHANNELS` entry (bluesky, linkedin, x): the stored `channel_config` row, or the all-defaults state (`auto_publish: false`, `disclosure_label`/`daily_cap`/`changed_by_user_id`/`event_id`/`updated_at` all null, `review_required: true`) for a channel never toggled. `services/api/admin_posts.py`'s `serialize_channel_config` is now shared by `GET` and `PUT` (same per-channel shape, `event_id` resolved from the latest `admin_edit` audit event on the channel's deterministic `uuid5` subject id) so the two cannot drift apart. `api/openapi.yaml` is untouched per this follow-up's rules; the new operation and its `ChannelConfig`/`ChannelConfigListResponse` schemas live in new `api/fragments/channels.yaml` for the coordinator to merge, with a note asking them to also refactor `ChannelConfigResponse.data` (currently inline) to `$ref` the new `ChannelConfig` schema. Four new tests in `tests/test_api_admin_posts.py` (401, operator-may-read, defaults for an unconfigured channel validated against the fragment's schema via a merged-spec-copy helper, and the stored row after a `PUT` matching that `PUT`'s own response field-for-field) bring the file to 75 tests, still 100% line coverage on `services/api/admin_posts.py` (476 statements). `pytest tests/test_api_contract.py services/api tests/test_api_pro_keys.py tests/test_api_admin_posts.py` 133 passed; `ruff`/`ruff format`/`mypy --cache-dir /tmp/mypy-adm-posts` clean.
- 2026-09-13 frontend-developer: built Sprint 3 item 3's Sources, Records, Resolution and Extractions admin screens, server-rendered over the live `/admin/v1` API. New `web/admin/sources.py` (router, mounted by the coordinator) covers `GET /admin/sources` (health table with status glyph/GATED/paused chips, filters `health`/`publish_state`/`implemented`, "add issuer"), `GET+POST /admin/sources/new`, `GET /admin/sources/{id}` plus action POSTs for run-now/pause/resume/edit-cadence/publish-state/licence-gate, `GET /admin/source-runs[/{run_id}]` and `GET /admin/snapshots/{id}`. New `web/admin/records.py` covers `GET /admin/records` (public-id lookup), `GET+POST /admin/records/proposals/{id}` (edit, publish-state, two-step merge preview/apply, unmerge), the same for `opportunities`, a minimal `organizations` edit, `GET /admin/resolution` with decide forms (`same`/`different`/`defer`) and `GET /admin/extractions` with accept/reject forms. No JavaScript; every filter lives in the query string; every write carries a mandatory `reason` and follows the shell's `require_same_origin` → API call → 303-with-flash-or-re-render-with-`problem_notice` contract. Notable decisions (full list in `web/admin/README.md`): filters are single-valued selects, not multi-select, since a JS-free multi-select does not render at 400px; "add issuer"'s `licence_id` is a plain text field because `GET /v1/licences` is still `x-status: planned`; the gate-clearance form renders disabled (not hidden) for non-`legal` roles, with the API's own role check as the real enforcement; the "GATED" chip is derived client-side from the embedded licence's `reuse_class`/`gate_flag`, mirroring `services/api/admin_sources.py::_licence_gate_missing`; merge is one route with a `step=preview|apply` field since there is no session-storage table to hold a preview between two page loads, so the preview response itself carries the conflicts and a signed `preview_token` forward as hidden fields; the organisation detail screen reads the public `GET /v1/organizations/{public_id}` endpoint since no admin-only GET exists for organisations. `web/test_admin_sources.py` (16 tests) and `web/test_admin_records.py` (17 tests) copy `web/test_admin_shell.py`'s fixtures verbatim and mount both this task's web routers and the matching `services/api/admin_sources.py`/`admin_records.py` API routers onto the shared apps, guarded against double-registration; cover list/empty-state rendering, the GATED chip, licence evidence on source detail, pause/resume, `422 gate_unmet` on a restricted source's publish attempt, the gate form's role gate (403 operator / 303 legal), run-now's real sandbox `503` (plus a fake-runner override proving the `202` success path), records edit, full merge-preview-apply-unmerge round trip, resolution `same` merging, extraction accept/reject, same-origin refusal (403) and anonymous redirects. `pytest web/test_admin_sources.py web/test_admin_records.py` 33 passed; `pytest web --ignore=web/test_e2e.py` 80 passed (no regressions against the other three concurrent frontend agents' files); `ruff`/`ruff format`/`mypy --cache-dir /tmp/mypy-fe-a web/admin` clean.
- 2026-09-13 frontend-developer: built Sprint 3 item 3's Tasks and Posts admin screens, server-rendered over the live `/admin/v1` API. New `web/admin/tasks.py` (router, mounted by the coordinator) covers `GET /admin/tasks` (queue table, filters `type`/`status`/`assignee_user_id`, cursor pager), `GET /admin/tasks/{id}` (pending record and contact as definition lists, audit event ids, a deletion-request redaction warning), `POST .../update` (`PATCH /admin/v1/tasks/{id}`) and `POST .../approve-intake` (carries the created/linked record's public id and URL through the success redirect's query string so the record link renders after the required 303). New `web/admin/posts.py` covers `GET /admin/posts` (review queue, filters `channel`/`state`, cursor pager, gate-checked-at age), `GET /admin/posts/{id}` (body in a monospace block with the live channel character count/limit, edit/approve/reject/schedule forms), the matching write POSTs, and `GET/POST /admin/posts/channels[/{channel}/auto-publish]` — read-only for non-owners, `PUT`-backed for owners, and now reading real state from `GET /admin/v1/channels` (a coordinator follow-up that landed after this task's brief, which had assumed no such read existed). No JavaScript; every write carries a mandatory `reason` and follows the shell's `require_same_origin` → API call → 303-with-flash-or-re-render-with-`problem_notice` contract; status/state chips use the five docs/31 families with the API's own vocabulary as the label, never colour alone. Notable decisions (full list in `web/admin/README.md`): vocabulary tuples are hardcoded locally rather than imported from the server, mirroring `services/api/admin_posts.py`'s own "web layer only talks to the API" call; the task update form only sends `assignee_user_id` when non-empty or an explicit "Unassign" checkbox is ticked, so leaving it blank never clobbers an existing assignment; `datetime-local` schedule input is treated as UTC (no per-operator timezone preference exists); channel body-length counts mirror the API's own interim Python-string-length measure so the two never disagree. `web/test_admin_tasks.py` (12 tests) and `web/test_admin_posts.py` (14 tests) copy `web/test_admin_shell.py`'s fixtures, create intake tasks through the real public `POST /v1/intake/proposals` endpoint, and seed posts by inserting `Post` rows against a `make_event`-built `Event`; cover both queues' list/empty-state rendering, task detail's pending record/contact, the update and approve-intake flows (incl. the deletion-request redaction path and its CRM-`503` notice), post detail's character count, approve/reject/schedule incl. `422 gate_unmet` and the past-schedule `400`, the channels page's owner/operator split and its `422` on missing disclosure, same-origin refusal (403) and anonymous redirects. `pytest web/test_admin_tasks.py web/test_admin_posts.py` 26 passed; `ruff`/`ruff format`/`mypy --cache-dir /tmp/mypy-fe-b web/admin/tasks.py web/admin/posts.py` clean; `pytest web --ignore=web/test_e2e.py` 92 passed, 3 failed — all three failures are in `web/test_admin_people.py` against a different concurrent agent's `web/admin/people.py`, reproduced in isolation with none of this task's files involved.
- 2026-09-13 frontend-developer: built Sprint 3 item 3's Users, Customers, Keys, Costs and Audit admin screens, server-rendered over the live `/admin/v1` API. New `web/admin/people.py` (router: `GET/PATCH /admin/users`, `GET/POST /admin/users/{id}/delete` redirecting to the API's returned task URL; `GET/POST /admin/customers`, `GET/POST /admin/customers/new`, `POST /admin/customers/{id}/subscriptions`) and `web/admin/ops.py` (router: `GET/POST /admin/keys` with a one-time secret page on issue, `POST /admin/keys/{id}/revoke`, `GET /admin/costs` with a page-module-computed totals row, `GET /admin/audit` and `GET /admin/audit/{event_id}` — the latter paging the list since no single-item audit operation exists in `api/openapi.yaml`); templates under `web/templates/admin/{users,customers,keys,costs,audit}/`; `web/static/css/admin-people.css`/`admin-ops.css` (small layout-only additions). 26 new tests (`web/test_admin_people.py` 15, `web/test_admin_ops.py` 11), all against fresh in-memory CRM/billing fakes; found and fixed a real template whitespace bug (the US-902 AC3 stale-banner sentence line-wrapped across two source lines, so it never rendered as one contiguous string) that a concurrent agent's own verbatim tail had already flagged as three failing tests in `web/test_admin_people.py`. Flagged two API-side gaps for the coordinator: `admin_list_audit`'s `_resolve_any_subject_uuid` does not decode the `usr_`/`acc_`/`key_` prefixes `AnyPublicIdValue` was widened to cover for responses, so a `subject_id` filter on those never narrows correctly; and no `adminGetAuditEvent` operation exists, so the audit detail screen scans bounded pages of the list instead. `.venv/bin/python -m pytest web --ignore=web/test_e2e.py`: 106 passed. `ruff check`/`ruff format --check` clean; `mypy --strict` on both new modules: no issues.
- 2026-09-13 coordinator: admin UI shell written (`web/admin/shell.py`, chrome, `admin.css`, generic request on the API client); six admin page modules mounted in `web/app.py`; `GET /admin/v1/channels` merged into the spec (129 operations); Sprint 3 item 3 closed in the plan with the follow-ups it surfaced.
- 2026-09-13 backend-developer: added `services/alerts/worker.py` (Sprint 3 item 4, the scheduled alert cycle and delivery worker): `run_alert_tick(session_factory, *, email_port=None, transport=None, now=None) -> AlertTickReport` runs `evaluate.run_alert_cycle` (transaction 1) and `webhooks.deliver_pending` (transaction 2) each in its own session, committing independently so a failure in one never blocks the other; `HttpxTransport` (real `httpx.Client`, 10s timeout) and `DryRunTransport` (`--dry-run`) satisfy `webhooks.Transport`. Found that `evaluate.py`'s `run_alert_cycle` already sends every alert synchronously (channel `email`, status flips `queued` -> `sent`/`suppressed` before the row is ever visible) — no separate "send queued alerts" step exists or was added. Found that `webhooks.attempt_delivery` absorbs every `Transport.post` exception internally by design, so a raising transport surfaces as a counted delivery failure, not a tick-level error — documented in `services/alerts/README.md` "Found in webhooks.py" with no patch proposed (working as intended). New `services/alerts/__main__.py` (`python -m services.alerts` delegates to `worker.main()`); CLI `python -m services.alerts.worker [--dry-run]` prints one `key=value` summary line via `sys.stdout.write` (T20-clean, no `print`) and exits 1 on any recorded error. `tests/test_alerts_worker.py`: 16 new tests (happy path, two-tick idempotency proved via an empty fake-transport response queue, both halves' failure containment, real `HttpxTransport` against `httpx.MockTransport` for a 2xx and a connection error, CLI `--dry-run`/exit-code behaviour); 99% line coverage of `worker.py` (only its own `if __name__` guard uncovered). `.venv/bin/python -m pytest tests/test_alerts_worker.py tests/test_alerts_evaluate.py tests/test_alerts_webhooks.py`: 41 passed. `ruff check`/`ruff format --check` clean on `services/alerts` and the new test file; `mypy --strict services/alerts`: no issues. No other files touched.
- 2026-09-13 devops-engineer: wired the alert cycle and social draft generation into the Procrastinate scheduler (Sprint 3 item 4). New `infra/scheduler/jobs.py`: `build_session_factory()` (cached, via `services.db.session`, falls back to in-memory SQLite absent `DATABASE_URL`) and `alert_tick_job()`/`post_draft_tick_job()`, each dynamically importing (`importlib`, not a literal `from X import Y`, so mypy strict does not depend on the module existing yet) `services.alerts.worker.run_alert_tick`/`services.social.worker.draft_posts_tick`, logging one structured line of counts plus `error_count` (never the `errors` tuple's text — CLAUDE.md minimum-personal-data), and returning the report as a JSON-safe dict. `infra/scheduler/app.py`: two new periodic tasks following the existing bucket-tick split — `tick_alert`/`tick_post_draft` on `SCHEDULER_ONLY_QUEUE` (cron `*/15 * * * *` / `7 * * * *`, US-502 AC1 and off-the-hour respectively) each defer exactly one `alert_tick`/`post_draft_tick` job onto the `alert`/`post_draft` queues the `worker` service already consumes, `queueing_lock` per job name, `retry=0` (the next tick is the retry), a 10-minute timeout via a new `_run_with_timeout` (thread + bounded `Future.result()`, the in-process equivalent of `run_connector`'s `subprocess.run(timeout=...)`, with the same "abandons rather than kills" limitation noted in both the code and `docs/60`). **Decision: folded the placeholder `social` compose service into `worker` rather than pointing it at `post_draft,publish_post`** — `docs/20` §4.1's process table has no separate social process (`worker-plain` already reads "alerts, publishers"), and `worker`'s queues line already included `alert`, `post_draft` and `publish_post`, so a standalone `social` service would just double-consume the same queues; removed from both `infra/compose/docker-compose.yml` and `compose.prod.yml`, and fixed the now-stale `social` mention in `docs/60-deployment.md` §10.1's deploy runbook. `docs/60-deployment.md` §6.1 documents both jobs (cron/queue/lock/timeout/what they call/how to run one by hand). Tests (`infra/scheduler/test_jobs.py`, 21 total incl. the pre-existing `test_cadence.py` 13): fake `_run` proves one log line + dict return and that a raising fake propagates for both jobs; `build_session_factory` caching and its no-real-database fallback proven against a live in-memory SQLite `SELECT 1`; periodic registrations checked by importing `infra.scheduler.app` with a dummy `DATABASE_URL` (confirmed by reading `PsycopgConnector.__init__` that constructing it opens no connection, so no App-instance helper was needed) and inspecting `app.periodic_registry`/`app.tasks` directly; both compose files parse (a small local YAML loader tolerates Compose's `!reset`/`!override` merge tags, which plain `yaml.safe_load` cannot). Written with explicit `if ...: raise AssertionError(...)` throughout instead of bare `assert`, since `infra/scheduler/test_jobs.py` has no ruff per-file-ignore for S101 and extending `pyproject.toml`/the Makefile was out of this task's write scope. `.venv/bin/python -m pytest infra/scheduler`: 21 passed. `ruff check infra` / `ruff format --check infra`: clean except the pre-existing `test_cadence.py` S101 findings (unchanged by this task; the Makefile's own `--extend-per-file-ignores` already covers that file). `mypy --strict infra/`: clean. Docker is not available in this sandbox; `docker compose config` was not run — both compose files were instead validated by parsing (see tests above) and by a full visual diff confirming every other service kept every key it had. Not done: `infra/scripts/deploy.sh` (outside this task's write scope) still names a `social` service in its stop/start commands (lines with `stop worker browser-worker social scheduler` and `up -d --pull always worker social`) and needs the same fold-in; `infra/docker/Dockerfile` and `infra/terraform/main.tf` carry now-slightly-stale comments describing a "social dry-run" role that no longer has its own compose service (cosmetic, not functional). `services.social.worker.draft_posts_tick` does not exist yet (a concurrent agent's task); `services.alerts.worker.run_alert_tick` landed during this task and matches the contract exactly, verified against its real signature and `AlertTickReport` dataclass.
- 2026-09-13 content-social: added `services/social/worker.py` (`draft_posts_tick(session_factory, *, limit=500, now=None) -> DraftTickReport`, the exact contract `infra/scheduler/jobs.py` already dynamically imports) and `services/social/db_events.py` (`social_event_from_db(db, event) -> SocialEvent | None`, the `event` table -> `editorial.SocialEvent` bridge), turning the real `event` log into rows in the real `post` table for the admin review queue — nothing here reimplements a template, threshold or validation gate from `editorial.py`, and nothing ever calls a network adapter (every row lands `draft`, or `approved`/`auto_published=True` when `ChannelConfig.auto_publish` is on). Watermark is `MAX(event.seq)` joined through existing `post` rows (no new table, as instructed); hard gates (`services/api/visibility.py` at `entitlement="api"`, `event.published_at` not null, `licence.allows_derived_publication`, not `unpublished`) and docs/32 §4.8 duplicate suppression (exact `(event_id, channel)`, and a content-hash guard scoped to `(subject, channel)` over `queue._content_hash`) are counted separately per `DraftTickReport`; one event's exception is caught per a `Session.begin_nested()` savepoint and recorded in `errors` without blocking the rest of the tick. `queue.py`'s file-backed `ReviewQueue` is untouched and stays for its own scope (the CLI's offline `pipeline/diff.py`-parquet dry run), a parallel path to a different store, not superseded by this one. Two known gaps recorded (services/social/README.md "Draft generation from the event log"): `opportunity.awarded`/`funding.cancelled`/`funding.reinstated` never draft yet (no `awardee_org`/`award_usd`/`funding_program` field exists anywhere in `services/db/models.py` or any connector's `event.after`); and the watermark does not advance past an event that earns zero posts (ineligible, gated, or fully duplicate), so such an event is rescanned every tick until a later event succeeds past it — cheap and idempotent, tested and documented rather than fixed with a new table. `tests/test_social_worker.py`: 23 new tests, 100% line coverage of both new files (`coverage run --source=services.social.worker,services.social.db_events`). `.venv/bin/python -m pytest tests/test_social_worker.py tests/test_social_editorial.py tests/test_social_queue.py`: 132 passed; `tests/test_social_cli.py tests/test_social_models.py tests/test_social_publishers.py`: 29 passed (nothing else broke). `ruff check`/`ruff format --check` clean on `services/social` and the new test file; `mypy --cache-dir /tmp/mypy-social services/social`: no issues. Two pre-existing, unrelated failures confirmed on the full repo suite (`web/test_e2e.py`'s Playwright smoke test, `tests/test_web_default_view.py`'s dev-database fixture) — reproduced in isolation with none of this task's files involved.
- 2026-09-13 content-social: coordinator-directed fix to the watermark in `services/social/worker.py` — the post-derived `MAX(event.seq)` watermark from the entry above never advanced past an event that earned zero posts (gated, ineligible, or fully duplicate), so a backlog of more than `limit` such events would be rescanned in full on every tick and could starve a real, later, eligible event behind the page limit forever: a livelock, per the coordinator's review, not merely the "not retried forever" limitation the previous entry described. Fixed against the coordinator-added `services.db.models.WorkerWatermark` (migration `0006`, one row per scheduled worker): `_watermark_row` reads/creates the `"social.draft_posts"` row, bootstrapping it from the old post-derived `MAX(event.seq)` join the first time it is missing so an upgrading deployment keeps its position; at the end of each tick, in the same transaction as that tick's own `post` inserts, its `seq` is set to the highest `event.seq` *examined* this tick (not just posted), so every event is passed exactly once regardless of outcome. The exact `(event_id, channel)` duplicate check stays as the defensive guard the coordinator asked to keep. Three new tests per the coordinator's spec: 600 gated events followed by one eligible event with `limit=500` draft on the second tick, not never; the watermark row advances even when a tick creates zero posts; a pre-existing deployment with `post` rows but no `worker_watermark` row bootstraps from the post-derived seq. `tests/test_social_worker.py`: 26 tests (up from 23), 100% line coverage of both `worker.py` and `db_events.py` unchanged. `.venv/bin/python -m pytest tests/test_social_worker.py tests/test_social_editorial.py tests/test_social_queue.py`: 135 passed. `ruff check`/`ruff format --check` clean on `services/social` and the test file; `mypy --cache-dir /tmp/mypy-social services/social`: no issues. `services/social/README.md`'s decision 6 rewritten to describe the table and the "examined once" rule.
- 2026-09-13 data-engineer: Sprint 3 item 4, the loader bulk-insert pass. Measured baseline (unmodified `services/ingest/loader.py`, `data/eval/normalized.parquet`'s four open/attribution sources, 9,563 rows, fresh SQLite file db): 93-97 rows/s, matching `docs/00-PLAN.md`'s ≈100 rows/s. `cProfile` found the top cost was `_get_or_create_organization`'s per-miss O(n) re-scan of the whole `organization` table (81% of wall time on a 1,778-row slice), plus per-row `flush()`es and autoflush. Fixed in `services/ingest/loader.py` without changing any gate, dedupe, event-emission or `SourceRun` behaviour: one `_LoadCache` preloading links/entities/organisations(exact+punctuation)/aliases/slugs once per `load_dataframe` call instead of per row; every id generated client-side (`new_uuid()`) so `public_id`/`slug` need no flush-then-overwrite; `records_df.to_dict("records")` replacing `.iterrows()`; `session.no_autoflush` plus an explicit two-phase flush (entities/orgs/locations, then the `ProposalSource`/`OpportunitySource` links created alongside them) every `batch_size` rows (new keyword, default 500) — the two-phase split is required because those link tables carry no ORM `relationship()` back to `Proposal`/`Opportunity` for flush-order inference. `Event` inserts stay one-at-a-time per `services/db/models.py`'s `seq` `MAX()+1` listener (documented, not an oversight). Re-measured: 2356-2406 rows/s, a ≈25x speedup, past the 5x bar. Added `services/ingest/bench_loader.py` (the reproducible measurement script) and three new pinning tests in `services/ingest/test_loader.py` (`batch_size=1` vs `500` write byte-identical content via a deterministic digest; default-batch-size re-run is idempotent; gate refusal writes nothing to the store) — 60 tests in `services/ingest`, 109 across the finish checks (`tests/test_api_contract.py services/api/test_routes.py`), all green; `ruff`/`ruff format`/`mypy --strict` clean. Full write-up, exact numbers and decisions in `services/README.md` "Bulk-insert pass (Sprint 3)". Deferred: re-measuring the real ~11,400-row site load (`data/normalized/*` is absent in this environment) and a real Postgres sequence for `Event.seq` so event inserts can batch too (`services/db/*`, out of this task's write scope).
- 2026-09-13 coordinator: Sprint 3 item 4 closed — alert worker, social draft worker (watermark table `worker_watermark`/migration 0006 added after verification found a livelock), scheduler wiring, loader bulk-insert pass verified and committed; deploy script aligned with the folded social service; plan updated.
- 2026-09-13 product-manager: launch runbook and pricing copy (Sprint 3 item 6). `docs/40-launch-runbook.md`: coverage statement (S3-4) with the exact About-page sentence from `docs/13` §6, an owner account checklist consolidated from `docs/32` §2 / `docs/34` §6 / `docs/60` §11 / the kickoff's open owner actions (secret name, who creates it, done-check per row), first-deploy steps in order (accounts/secrets before infrastructure before services, per `docs/60` §11 and §10.1), a go/no-go checklist against `docs/04` §9 R-4 and US-908 AC1, day-one operations (scheduled jobs, admin screens, the two manual overrides — `PUT /admin/v1/accounts/{id}/entitlement` and `POST /admin/v1/sources/{id}/run`), known gaps stated plainly, and rollback per `docs/60` §10.2. `docs/41-pricing-page-copy.md`: tier copy from `docs/11` §3 only, every number cited by row, anchor figures (Team's $9,000 Cleanview match, the $25,000 enterprise API tier) marked "verify before publishing," the coverage line from `docs/13` §6, a legal footer noting the licence-pass-through claim is blocked on undrafted customer terms (`docs/13` §7 item 8), and an "enforced by" table citing `services/sor/ports.py`, `services/ingest/lag.py`, `services/api/ratelimit.py` by path only. Two go/no-go items found failing on inspection, not softened: no `/privacy` route exists in `web/app.py`, and the `unsubscribe_token` column (`services/db/models.py`, populated by `services/alerts/evaluate.py`) has no endpoint anywhere that reads it — both are US-908 AC1 requirements. `tests/test_docs_links.py` does not exist; ran the CI's `docs-link-check` shell command from `.github/workflows/ci.yml` directly (45 markdown files, no broken relative links). No code, `docs/00-PLAN.md`, or other files touched.
- 2026-09-13 data-engineer: Sprint 3 item 5, EIA-860M exact-point promotion moved into the loader. `services/ingest/loader.py`: new `_extract_exact_point(raw_payload)` (validated `Latitude`/`Longitude` — numeric, world bounds, not `(0, 0)` — a plain key lookup, not EIA-specific, so any other source's raw payload carrying the same keys promotes identically); `_get_or_create_location` tries it before the county/state geocoder, gated off for `derived_only` sources per docs/04 D-9 (an exact coordinate is a raw field like any other) so those still fall back to a centroid with `precision_reason = "licence"` even when their raw payload has a valid pair; a validated pair stores `precision = "exact"`, `kind = "point"`, `geocoder = "source_provided"` (docs/04 D-8). New `LoadResult.locations_exact_promoted` field. Batching and the licence gate unchanged. Removed `web/data_loading.py::backfill_eia_exact_points` (the frontend-side correction this superseded), its call in `load_dev_database`, and the `eia_exact_points` report key — no test asserts that key (checked `tests/test_web_default_view.py`, `web/test_e2e.py`, every `load_dev_database` call site), so nothing had to keep a placeholder. Nine new tests in `services/ingest/test_loader.py`: valid pair promotes; five invalid-pair shapes fall back and are not counted; no-raw-coordinates row is byte-for-byte unaffected; a `derived_only` source with a *valid* pair still falls back with `precision_reason = "licence"`; re-running the same frame is idempotent; `batch_size` 1 vs 500 write identical stores; one true end-to-end test through the real connector over `tests/fixtures/eia860m_planned.xlsx` (25/25 fixture rows promote). `services/ingest/bench_loader.py` (untouched, out of scope) before/after on `data/eval/normalized.parquet`: ~2,270–2,420 rows/s both times (no regression), 0/9,563 rows promoted — that fixture predates the `raw` payload column entirely, for every source, not just EIA-860M, so it has nothing to promote; the real connector-fixture run above (25/25) is the closest available measurement of production behaviour, since `data/normalized/*` is absent in this environment (same gap the bulk-insert pass entry recorded). `.venv/bin/python -m pytest services/ingest tests/test_connector_gating.py tests/test_web_provenance.py`: 59 passed. `.venv/bin/python -m pytest tests/test_api_contract.py services/api/test_routes.py`: 49 passed. `ruff`/`ruff format`/`mypy --cache-dir /tmp/mypy-eia` clean on `services/ingest web/data_loading.py`. Decision: promotion applies only at record creation, not on re-ingest of an already-existing entity (matches the pre-existing location-assignment scope; a store loaded under the previous loader is not retroactively upgraded — flagged, not silently done, as a possible follow-up backfill). Full write-up in `services/README.md` "EIA exact-point promotion (Sprint 3)"; `web/README.md`'s "Data-layer corrections" section updated to record the fourth and last correction's removal.
- 2026-09-13 data-engineer: Sprint 3 item 5, GB TEC-register substation geocoding. Checked the candidates named in the task in order (NESO GIS boundary/ETYS datasets — Thiessen polygons or study-boundary paths, not substation points; National Grid Electricity Transmission's own open data — geocoordinates explicitly not included per its own documentation, and its licence bars commercial use in any case) before finding NESO's own **FES 2024 Grid Supply Point Info** dataset (part of "Regional breakdown of FES data (Electricity)" on the NESO Data Portal): 373 named GSPs with lat/lon, under the identical NESO Open Data Licence already quoted for the TEC register itself (`package_show` confirms the same license_title/license_id/license_url) — no OSM/OS evaluation needed. Vendored 371 rows (2 byte-identical dropped) to new `services/ingest/data/gb_substations.tsv`; `services/ingest/data/README.md` (new) records this and the pre-existing `us_county_centroids.tsv`'s provenance, the latter previously undocumented — spot-verified today against a fresh download of the live 2024 Census Gazetteer county file (all 3,222 rows byte-identical). `services/ingest/geocode.py`: added `SubstationGazetteer`/`normalize_substation_name` (tolerant to case, "GSP"/"Substation"/"S/S", single- and dual-voltage `kV` suffixes, and the one-word/two-word spelling variants observed in the fixture — "Upperboat"/"Upper Boat", "Clydesmill"/"Clyde's Mill" — verified to introduce no cross-site collisions across the 371-row gazetteer) and a `country` keyword on `geocode()` (default `None`, existing behaviour and call sites unchanged): `country` starting "GB" resolves `county` (the connector already carries the raw "Connection Site" string there) against the gazetteer at `county_centroid` precision, never `exact` — a substation is not the project's location (docs/21-data-model.md §3.7) — else `unknown`, never guessed. `geocode()` still returns its original 2-tuple, unchanged, because `loader.py` unpacks it positionally and this task did not touch `loader.py`; there is nowhere in that tuple to carry which tier resolved a point, so `services/ingest/geocode.py`'s module docstring and `services/ingest/data/README.md` both leave an explicit note for whoever wires the `country` argument through at the loader's call site: also set `Location.geocoder = "gb_substation"` there, and extend `docs/21` §3.7's `geocoder` vocabulary to name it. No changes needed to `pipeline/connectors/gb_neso_tec_register/connector.py` — `county` already carries the raw Connection Site string, exactly what the gazetteer lookup expects. Measured coverage: fixture (`tests/fixtures/neso_tec_register.csv`) 6/30 distinct Connection Site strings (20.0%), 9/38 rows (23.7%); the live TEC register (fetched through the connector's own CKAN `package_show` -> CSV path, 2,198 rows, 1,237 distinct sites) 346/1,237 distinct sites (28.0%), 739/2,198 rows (33.6%) — the shortfall is structural (most unmatched sites are a substation built new for that specific generator/BESS project, which no general gazetteer covers), not a normalisation gap. New `services/ingest/test_geocode.py` (27 tests): gazetteer loads; 12 real fixture strings' normalisation asserted; the fixture's exact 6-site coverage list asserted with the measurement date in a comment; `country=None` behaviour (including a GB-looking county string on the US path) unchanged; unmatched GB strings stay `unknown`. `data/sources.yaml`: one new entry `gb.neso.fes_gsp_gazetteer` (category `registry`, reference data, not a proposal/opportunity feed) in the file's existing style. `docs/02-data-sources.md` §8 (new) summarises source, licence and coverage. `.venv/bin/python -m pytest services/ingest/test_geocode.py pipeline/connectors/gb_neso_tec_register`: 33 passed. `.venv/bin/python -m pytest services/ingest tests/test_connector_gating.py`: 83 passed (loader unaffected by the sibling EIA-exact-point and FERC/link_dockets work landing in parallel). `ruff check`/`ruff format --check` clean on `services/ingest` and `pipeline/connectors/gb_neso_tec_register`; `mypy --cache-dir /tmp/mypy-gb services/ingest/geocode.py`: no issues (strict). Not done: `Location.geocoder` wiring and the `docs/21` §3.7 vocabulary extension (left to the coordinator, above); OS Open Names/OSM were not evaluated since an on-portal NESO/OGL source already answered the task.
- 2026-09-13 backend-developer: US-908's two hard blockers closed (docs/40-launch-runbook.md §4 rows 5 and 9). `services/api/unsubscribe_routes.py` (new): `POST /v1/alerts/unsubscribe` and the `GET ?token=…` one-click twin — public, no session, `Alert.unsubscribe_token` is the credential; removes `email` from that saved search's `channels` (pausing it if `channels` becomes empty), writes an `Event` (`subject_type="saved_search"`, `actor_type="system"`, `reason="unsubscribe via token"`, mirroring `services/billing/entitlement.py`'s non-pipeline-event convention), idempotent on a second use, generic `404` on an unrecognized token, rate-limited 20/window per IP via the shared `default_limiter`. `services/alerts/evaluate.py`: `render_digest_body` now takes `unsubscribe_token` and appends the sender identity (`alerts@{{DOMAIN}}`, the same address `ResendEmailAdapter` sends from) and the `https://{{DOMAIN}}/unsubscribe?token=…` link (docs/13 CAN-SPAM/CASL §1/§8); `run_alert_cycle` reordered minimally so each digest `Alert` row (and its token) is created before its own body is rendered — no existing evaluate/worker test needed its assertions changed, all 26 stayed green. `web/legal.py` (new): `GET /privacy` (data inventory, lawful bases, retention/deletion route citing US-910, `{{POSTAL_ADDRESS}}` placeholder, last-updated date) and `GET`/`POST /unsubscribe` (token-entry form, confirmation/not-found rendering, same-origin CSRF check on the POST) with `web/templates/legal/privacy.html` and `unsubscribe.html`; added the footer "Privacy" link in `web/templates/base.html`. `api/fragments/unsubscribe.yaml` (new) for the coordinator to merge: `unsubscribeAlert`/`unsubscribeAlertOneClick` operations and `UnsubscribeRequest`/`UnsubscribeResponse` schemas — flags an important mount-order hazard: `services/api/pro.py`'s existing `GET /v1/alerts/{alert_id}` will swallow `/v1/alerts/unsubscribe` as a literal `alert_id` unless `unsubscribe_routes.router` is mounted on `services.api.app.app` *before* `pro.router` (both new test files reproduce and route around this for their own in-process apps; documented in the fragment too). 42 new tests (`tests/test_api_unsubscribe.py` 10, `web/test_legal.py` 7, plus the two files above already counted) all green alongside the existing 26 `test_alerts_evaluate.py`/`test_alerts_worker.py` tests; `tests/test_api_contract.py web/test_auth.py` (35) unaffected. `ruff check`/`ruff format --check` and `mypy --cache-dir /tmp/mypy-legal` (strict) clean on every touched file. Not done: mounting the two new routers and merging the OpenAPI fragment (coordinator's task per the hard rules); a real postal address (deliberate `{{POSTAL_ADDRESS}}` placeholder, same convention as `{{DOMAIN}}`).
- 2026-09-13 data-engineer: Sprint 3 item 5, FERC multi-year filer-name backfill to retest docket linkage (docs/00-PLAN.md's "Docket linkage measured" decision row). `pipeline/connectors/us_ferc_elibrary/connector.py`: instance attribute `.window: tuple[date, date] | None` (default `None`, unchanged 30-day cadence) replaces the "last WINDOW_DAYS" window in both `fetch()` (`_effective_window`/`_build_queries`, now spanning every year `[start.year, end.year]` touches, not just the two the old two-year-only code assumed) and `parse()`; four new tests, default behaviour and its existing tests untouched. `pipeline/link_dockets.py`: `docket_year`/`filter_er_docket_years` (ER-class, docket-embedded year), `dedupe_filings_per_docket` + `_filer_index` (collapse repeated filings from the same filer under the same docket before `_sponsor_matches`'s `cdist` call, expanded back to one link row per underlying filing) — no-ops on the single-filing-per-docket fixtures the existing tests pin, 8 new tests including a multi-year end-to-end fixture. `pipeline/backfill_ferc.py` (new): runs ERCOT/CAISO/NYISO live plus `us.ferc.elibrary` over 3 trailing one-year windows via `.window`, time-boxed ~20 minutes, then `link_dockets.run`; 6 new fixture-only tests for its measurement functions. Live run 2026-09-13: 5,870 ISO records + 1,290 FERC documents (3 years, 60 requests, 301.6s, no window skipped) -> 81 links (20 explicit, 61 filer-name) -> **active link rate 21/1,673 = 1.26%** (vs 1/1,673 = 0.06% on the 30-day test, ~21x, same denominator) and 3/216 = 1.39% among `contracted`/`under_construction`. Hand-reviewed a 50-link random sample (verdicts in `data/probes/ferc-backfill-2026-09-13.json`): **precision 48.0%** (76.9% explicit, 37.8% filer-name), 81% of errors traced to three narrow, named bugs (a degenerate 2-letter sponsor-residual gate, no numeral-awareness against shell-company siblings, no ER-only restriction on the explicit method) rather than the method being unsound. **Honest read, recorded in `docs/22` §14: recall confirmed the "over years" hypothesis (~21x) but precision (48.0%) is far below the ≥0.95 bar `docs/22` §10 sets, so docket linkage does not clear a bar worth counting in M-1 today** — next step (not done here, out of this task's scope) is the three targeted gate fixes named in §14.5 and a re-run, not a full 600-label programme yet. `.venv/bin/python -m pytest pipeline/connectors/us_ferc_elibrary tests/test_link_dockets.py tests/test_connector_framework.py pipeline/connectors -q`: all passed; `ruff check`/`ruff format --check` and `mypy --cache-dir /tmp/mypy-ferc` (strict on the connector) clean on every touched file. Nothing written under `data/normalized/` (all live fetches kept in memory; scratch only, git-ignored). Not done: the three precision fixes themselves (§14.5's next step) and the certified held-out labelling `docs/22` §10 calls for.
- 2026-09-13 coordinator: Sprint 3 items 5 and 6 closed (EIA exact points, GB substation gazetteer wired into the loader with `gb_substation` recorded in docs/21 §3.7, FERC backfill measured, launch runbook and pricing copy, privacy notice and unsubscribe built and mounted, spec at 131 operations); Sprint 3 closed in the plan with the owner actions and follow-ups carried forward.
- 2026-09-14 coordinator: docs/00-PLAN.md decision row and open question 7 for the post-launch built-infrastructure context layer (EIA, GEM, OSM pending counsel).
- 2026-09-15 coordinator: shared contract for the context layer — `built_plant` and `ui_event` models, migrations 0007/0008, docs/21 §3.20–3.21, model tests.
- 2026-09-15 data-engineer: EIA-860M "Operating" sheet -> `built_plant` context layer (docs/21 §3.20). `pipeline/context/eia_plants.py` (new): `parse_operating_sheet` (sheet "Operating", `header=2`, drops the workbook's trailing note rows, same convention `pipeline/connectors/us_eia_860m/connector.py` uses for "Planned"); `aggregate_plants` collapses one row per generator to one row per Plant ID — dominant `technology`/`technology_raw` by largest summed nameplate share (`pipeline.normalize.classify_tech`), a full `technologies` raw-label split, `capacity_mw` via `pipeline.normalize.capacity`'s existing "0/blank is missing" rule, `generator_count`, `earliest_operating_year`, and `lon`/`lat` validated with the same rule `services/ingest/loader.py::_extract_exact_point` already applies to this workbook's Latitude/Longitude on the Planned sheet (finite, in world bounds, not `(0, 0)`). CLI `python -m pipeline.context.eia_plants [--workbook PATH | --latest-snapshot] [--out PATH]`, default input the newest `data/snapshots/us.eia.860m/*.xlsx`, default output `data/normalized/context/us.eia.860m.plants.parquet` (already `.gitignore`d under the existing `data/normalized/` rule). `services/ingest/plants.py` (new): `PlantsLoadResult`, `load_plants`/`load_plants_parquet` upsert `built_plant` by `(source_id, source_plant_id)` reusing `upsert_licence_and_source` unchanged, one bulk `SELECT` of existing keys (no per-row query), commits once; a re-run always overwrites every mapped field and counts it as `updated`, never `skipped` (idempotency comes from the unique constraint, not a diff — documented in the module). CLI `python -m services.ingest.plants --parquet PATH [--db PATH]` (default `web/.data/dev.db`, or `DATABASE_URL` if set). 17 new tests (`pipeline/context/test_eia_plants.py`, `services/ingest/test_plants.py`: aggregation, dominant-technology tie rule, technologies split, the zero-nameplate/capacity rule, `(0,0)` coordinate rejection, insert/idempotent-re-run/geom-round-trip/unique-constraint). Measured against the real snapshot already on disk (`data/snapshots/us.eia.860m/20260913T202539Z.xlsx`, July 2026 workbook): `{"plants": 14659, "with_coordinates": 14659, "without_coordinates": 0, ..., "elapsed_s": 13.58}` then `{"plants_seen": 14659, "inserted": 14659, "updated": 0, "placed": 14659, "unplaced": 0, "elapsed_s": 1.83}` against a scratch copy of `web/.data/dev.db`; a second loader run against the same parquet reported `inserted: 0, updated: 14659`, no duplicate rows. Docs: `docs/02-data-sources.md` §9 added. Config: `pyproject.toml` per-file-ignores/mypy-exclude extended for `pipeline/context/test_*.py` (S101/no-untyped-def), matching the existing `services/**/test_*.py` convention — no other project outside this task's file list touched. Gates clean: `ruff check`/`ruff format --check pipeline services/ingest`, `mypy pipeline/context services/ingest/plants.py` (strict), `pytest pipeline/context services/ingest/test_plants.py` (17 passed). Not done / left for the coordinator: a proposed `data/sources.yaml` note for `us.eia.860m` (below, this wave's `sources.yaml` is another lane's); the model itself is unchanged (no concerns found with `BuiltPlant`'s columns against what the Operating sheet actually gives).
- 2026-09-15 devops-engineer: Basemap tiles (docs/40 §2.7 gap; owner decision docs/00-PLAN.md 2026-09-14/15; docs/adr/0007-basemap-protomaps-on-r2.md, new). `infra/terraform/storage.tf`: second R2 bucket `${var.r2_bucket_name}-tiles-${var.environment}` (public tiles, kept separate from the private raw-snapshot bucket), same `count` guard as the existing bucket; the custom-domain (`tiles.{{DOMAIN}}`) and CORS (`GET,HEAD`+`Range` from the site origin) steps are documented as manual dashboard/API actions with a commented-out future resource each, because the pinned Cloudflare provider (`~> 4.36`) does not expose `cloudflare_r2_custom_domain` or R2 bucket CORS as resources — confirmed by diffing the v4.52.0 (latest v4) and v5.0.0 schema docs, not assumed. `infra/terraform/outputs.tf`: `tiles_bucket_name`, `tile_url`. `infra/scripts/build_basemap.sh` (new): resolves the latest Protomaps weekly planet build from `https://build-metadata.protomaps.dev/builds.json` (the `build.protomaps.com/` root itself 404s; there is no plain index page, only this JSON the protomaps.com build browser's own JS fetches) unless `BUILD_DATE` is pinned; installs `go-pmtiles` via `go install` if absent; extracts ONE archive covering the contiguous US + Great Britain (`--bbox=-125,24,1.85,60.9 --maxzoom=12`) via HTTP range requests against the remote ~138 GB planet file (never downloaded whole); `pmtiles verify`s it; uploads to a dated R2 key then promotes (server-side copy) to the canonical `basemap.pmtiles`, matching the owner's decided single `MAP_TILE_URL`. Chose one file over the two-file (`basemap-us.pmtiles`/`basemap-gb.pmtiles`) fallback the brief allowed: measured (real extracts, not estimates, against the 2026-09-14 build) CONUS alone = 1,906,871,982 bytes/27.6s, GB alone = 346,738,596 bytes/16.1s, the combined single bbox = 3,892,675,048 bytes/28.1s (8 threads) — ~$0.06/month at R2's per-GB rate either way, so the single file's simplicity (one URL, no web/ multi-source style) wins; a sanity-scale extract (Texas, `--maxzoom=10`) was 19,897,851 bytes/4.1s. `.github/workflows/basemap.yml` (new): `workflow_dispatch` + monthly cron (`23 5 1 * *`), runs against the `production` GitHub Environment's R2 secrets plus a new `R2_TILES_BUCKET` secret, verifies the uploaded object is range-request-capable. `infra/compose/.env.example`: `MAP_TILE_URL` documented (three modes: self-hosted PMTiles URL / hosted raster template / unset-dev-only). `infra/compose/docker-compose.yml`: `MAP_TILE_URL` passed through to the `web` service only (web/ itself untouched, out of this task's scope — another lane wires it into `web/static/js/map.js`). `docs/40-launch-runbook.md` §2.7 rewritten as decided with the concrete owner steps and done-checks (including the `map.basemap_failed` counter, flagged as web/'s to build, not assumed done). `docs/60-deployment.md` new §9.1 pointing at the script/workflow/ADR and this task's measured numbers. Validated in-sandbox (network access to github.com/protomaps.com and GitHub releases was available): real, non-dry-run `pmtiles extract`/`verify` runs against the live Protomaps build (numbers above); `bash -n` and `shellcheck v0.10.0` clean on `build_basemap.sh` (zero findings, cleaner than the two pre-existing informational notes in `deploy.sh`); `actionlint v1.7.7` clean on `basemap.yml` (and, as a baseline check, on `ci.yml`/`connectors-nightly.yml`); installed real OpenTofu v1.8.5 and ran `tofu fmt -check` (clean), `tofu init` (real `hcloud`/`cloudflare` provider plugins downloaded), and `tofu validate` (success) against the whole `infra/terraform/` config, plus a full `tofu plan` with a dummy-but-64-char `HCLOUD_TOKEN` reaching a real 12-resource plan (the new tiles bucket, its outputs, and the count-guard-off case with `cloudflare_account_id=""` both checked); `docker compose config` merges `docker-compose.yml`+`compose.prod.yml` cleanly with `MAP_TILE_URL` present in the `web` service. Not validated (no real R2/Cloudflare/GitHub-Actions-environment credentials in this sandbox): an actual upload to a real R2 bucket (the script's upload step was exercised against a faked `aws` shim standing in for the real CLI, proving the script's own logic and argument-building, not R2 connectivity itself); the manual custom-domain/CORS dashboard steps (nothing to click against without a real zone/bucket). `mypy --strict infra/` and `.venv/bin/ruff` were not run against this task's changes specifically since none of them are Python (Terraform, one shell script, one YAML workflow, three doc/markdown edits) — the gate commands still ran clean (no infra/ Python files were touched, so nothing new to lint/type-check).

- 2026-09-15 backend-developer: built-infrastructure context layer API (docs/00-PLAN.md 2026-09-14) — `services/api/context_geo.py`/`context_routes.py` add `GET /v1/context/plants/geo` (clustered `built_plant` GeoJSON, public on every tier, no lag, same grid clustering as `services/api/geo.py` via direct import of `_grid_cell`/`SPLIT_THRESHOLD`/`_in_bbox`; `technology`/`country` filters, technology validated against `pipeline.normalize.classify_tech`); `services/api/ui_events.py` adds `POST /v1/ui-events` (no auth, 202/empty body, allowlisted `name`/`props` per `UI_EVENT_NAMES`, identifier-shaped values rejected, 60/min per-IP limit with the IP used only for that limit and never stored) and `GET /admin/v1/ui-events/summary` (operator-only, weekly counts per event name with `map.layer_toggled` on/off split); one-line hook in `services/api/pro.py` writes `UiEvent(name="alert.created")` when a saved search is created; both routers mounted in `services/api/app.py`. Spec: 3 new operations in `api/openapi.yaml` (131 -> 134), valid, 44/44 stories. Decision: the technology-vocabulary and `ui-events` `props` rejections are `400 validation_error`, not the brief's literal "422" — `services/api/errors.py::ERROR_CODES` fixes `validation_error` at 400 for every caller in this codebase, and that file was read-only for this task; flagged for the architect. 27 new tests (`services/api/test_context_routes.py`, `services/api/test_ui_events.py`), full `services/api` suite and the whole repo suite green except one pre-existing, unrelated Playwright/chromium failure already on record as unverified. Details in `services/README.md` "Context layer endpoints".
- 2026-09-15 frontend-developer: map page basemap, existing-plants context layer, regional quick views, attribution page and measurement (docs/40 §2.7; docs/00-PLAN.md 2026-09-14/15 owner decisions). `web/app.py`: `MAP_TILE_URL`-keyed `_tile_mode`/`_basemap_attribution` (dev/raster/pmtiles, computed once server-side, reaches the page as `data-tile-url`/`data-tile-mode` on `#map`, never inline JS); `GET /api/context/plants/geo` and `POST /api/ui-events` same-origin proxies (forward only `bbox`/`zoom`/`technology` and `name`/`props` respectively, never cookies, ui-events always answers 202); `GET /attribution` (every source plus Basemap/Context-layers sections); regions computed server-side from `GET /v1/sources?publish_state=public` via new `web/regions.py` (`us`/`gb`/`eu`/`ca`/`au`, case-insensitive jurisdiction-prefix match). `web/static/js/map.js`: three-mode basemap construction (raster template swap, or `pmtiles.Protocol`/`@protomaps/basemaps` `layers()`/`namedFlavor("light")` with land/water/roads tinted to the paper-ground tokens), `map.basemap_failed` beaconed once on either CDN-script failure or a runtime tile/source error, always behind the pre-existing same-origin fallback outline layer; existing-plants layer (small SDF-square markers at 0.6 opacity, faint-ring clusters, inserted beneath every proposals layer, technology filter shared, plant drawer variant, second aria-live sentence); regional quick-view buttons (`fitBounds`, reduced-motion respected, `region=` in the URL); `navigator.sendBeacon`/`fetch(keepalive)` event emission for `map.layer_toggled`/`map.region_jumped`/`map.basemap_failed`; header "Sign in" link kept in sync with `?next=<path+query>` so `layers=` survives the login/register round trip. `web/auth.py::register_submit` posts `auth.registered {layers: "<comma-joined>"}` (a string, matching `services/api/ui_events.py`'s `_ALLOWED_PROPS` typing — not a list) after the redirect's cookies are set, swallowing any failure. `web/templates/home_map.html`, new `web/templates/attribution.html`, `web/templates/base.html` (footer "Attribution" link), `web/templates/legal/privacy.html` (one sentence: map interaction counts carry no cookie/IP/UA/referrer). `web/static/css/styles.css`: seven `--plant-*` technology tokens, light and dark, each recomputed and raised until ≥4.5:1 against `--bg` (D-20) since the legend renders them as text, not only as marker fills. `web/dev_up.py`: loads `data/normalized/context/us.eia.860m.plants.parquet` via `services.ingest.plants.load_plants_parquet` if present, one log line either way, never fails the rest of the load. New `web/test_map_layers.py` (proxy forwarding/cookie-stripping/defaults, ui-events 202-always, `/attribution`, region gating, tile-mode template wiring — against a hand-written fake `Transport`, since `GET /v1/context/plants/geo`/`POST /v1/ui-events` had not landed when this was started); extended `web/test_auth.py` (`auth.registered` against the real, now-landed `POST /v1/ui-events`, asserted via the `UiEvent` row) and `web/test_e2e.py` (whole suite now runs in pmtiles mode with both CDN scripts aborted, asserting the fallback renders and exactly one `map.basemap_failed` beacon, plus a plants-layer-toggle check). Measured (D-13): `map.js` 9.4 KB gzipped; the two pmtiles-only scripts add 14.6 KB gzipped when that mode is selected — but `maplibre-gl.js` 5.24.0 alone is already 276 KB gzipped, over the 250 KB budget by itself in every mode including the pre-existing dev one; flagged as pre-existing and outside this task's read-list (a version pin, not this task's files), not fixed unilaterally. Not done: basemap glyph/sprite wiring for pmtiles-mode text labels (flagged in `web/README.md` as a follow-up — no glyph host was named in the contract and the choice is better made against real PMTiles data). `.venv/bin/ruff check web`/`ruff format --check web`/`mypy web` clean; `pytest web --ignore=web/test_e2e.py` 136 passed; `pytest web/test_e2e.py` 1 passed. Details in `web/README.md` "Map basemap", "Existing-plants context layer", "Regional quick views", "Attribution page", "Measurement", "Dev loop: existing plants".
- 2026-09-15 coordinator: context-layer wave verified and committed lane by lane (plants loader, API + index cache, web, basemap infra, legal register), admin Engagement page mounted, EIA source display name no longer says Planned sheet (entry feeds both sheets), decision row in docs/00-PLAN.md with the measured numbers.
- 2026-09-15 coordinator: pipelines added to the plan — existing (EIA Energy Atlas, registered as us.eia.atlas.gas_pipelines, second context layer gated on the plants measurement) and proposed (FERC pending pipelines and DOE LNG connectors in the supply backlog).
- 2026-09-15 coordinator: plant palette widened to eleven families (biomass/waste, oil, geothermal, other get their own tokens), a Plant type filter (`plant_technology`) decoupled from the proposals technology filter, three-letter plant labels from zoom 9; real-archive e2e polls for map load.
- 2026-09-15 coordinator: permits decision recorded in docs/00-PLAN.md (federal permit events first post-launch sprint; state permits per customer state; local land-use permits declined) and EPA RBLC registered as us.epa.rblc for new-source air permits.
- 2026-09-15 coordinator: county land-use permits reversed from no to a bounded 25-county pilot with kill criteria (docs/00-PLAN.md); county_fips gap recorded as a prerequisite defect.
- 2026-09-15 coordinator: ruff/mypy pinned to the verified versions; the misplaced Cloudflare worker moved out of .github/workflows (it failed as a workflow on every push; real CI only runs on main pushes and PRs).
- 2026-09-15 coordinator: docs/40 §0 owner's morning checklist (decisions, local test, account order, browser-only terms tasks, counsel items, what waits on what).
- 2026-09-15 data-engineer: fixed the `location.county_fips` defect (NULL on every row). The vendored `services/ingest/data/us_county_centroids.tsv` had dropped the Census Gazetteer's `GEOID`; re-derived it from the same 2024 Gazetteer source file and appended it as a fifth `county_fips` column (`services/ingest/data/README.md` has the join method and the 6 state/independent-city FIPS collisions it disambiguates correctly). `services/ingest/geocode.py`'s `CountyGazetteer` now loads and serves it (`county_fips()`), and `loader.py::_get_or_create_location` sets `Location.county_fips` whenever the row's own state/county name resolves — for a `county_centroid` location and, separately, for an `exact` one whose source row also names a resolvable county — never from `geom` (no point-in-polygon geocoder is vendored). Added the idempotent `python -m services.ingest.geocode backfill-fips --db PATH` CLI; measured against a copy of `web/.data/dev.db`: `{"rows_seen": 10405, "filled": 7787, "unresolved": 2618, "by_precision": {"county_centroid": {"seen": 6203, "filled": 5464}, "state_centroid": {"seen": 378, "filled": 0}, "unknown": {"seen": 1483, "filled": 0}, "exact": {"seen": 2341, "filled": 2323}}}` — 100% of US `county_centroid` rows filled (the unfilled 739 are `gb_substation` rows correctly excluded, FIPS is US-only); a second run fills 0 (verified by test, not re-run against the copy since it was deleted after use per instructions). `docs/21-data-model.md` §3.7 updated with when `county_fips` is set. `web/data_ref/us_county_centroids.tsv` (out of file area) is untouched and now has 4 columns where the ingest copy has 5 — flagged, not fixed. New tests in `services/ingest/test_geocode.py` (fips lookup, state-scoped disambiguation, unresolved stays NULL) and `services/ingest/test_loader.py` (county-centroid row gets FIPS, unresolvable county stays NULL, exact-point row with a county name gets FIPS, backfill fills-and-is-idempotent). `ruff`/`mypy`/`pytest services/ingest` (82 passed) all green.
- 2026-09-15 coordinator: first CI run on PR #1 — six environment fixes (trivy action pinned by commit, gitleaks false positive removed, axe uses the runner's chromedriver, coverage and procrastinate in requirements, browser test falls back to Playwright's Chromium); D-13 map budget re-based to 350 KB by owner decision.
- 2026-09-15 coordinator: CI runs 2–3 on PR #1 — FastAPI 0.141 / starlette ≥1.3.1 (seven advisories), pip-audit ignores for the three packages gridstatus pins (recorded as an exception with a follow-up), browser test falls back to the committed eval fixture when data/normalized is absent and can opt into Playwright's Chromium (E2E_USE_PLAYWRIGHT_CHROMIUM=1).
- 2026-09-15 coordinator: CI run 4 on PR #1 — twelve of thirteen jobs green; image scan: Debian packages upgraded in the Dockerfile (libpcre2), scan fails only on fixable findings, .trivyignore records the cryptography finding blocked by the gridstatus pin.
- 2026-09-15 coordinator: CI run 5 — image scan: wheel and jaraco.context upgraded in the image; cryptography and lxml findings blocked by the gridstatus pin added to .trivyignore with reasons.
- 2026-09-15 coordinator: CI run 6 — the two remaining image-scan findings are setuptools' vendored wheel and jaraco.context (pinned via gridstatus); recorded in .trivyignore, ineffective upgrade line removed.
- 2026-09-15 coordinator: name adopted — Infraque at infraque.com (owner); Infraqueue and the {{DOMAIN}}/{{PRODUCT}} placeholders replaced across 84 files; docs/12 §11 records round four; counsel clearance, registrations and the wordmark brief remain.
- 2026-09-15 coordinator: us.eia.860m connector — owner's Python 3.14 run was robots-blocked on an /archive/ placeholder link (eia.gov disallows /*archive/; 3.11 parsers ignore the wildcard). Archive links dropped before any request, blocked candidates skipped and recorded in meta; two tests added; register note updated.
- 2026-09-15 coordinator: search — `q` on /v1/proposals and /v1/opportunities now matches the sponsor/issuer organisation name and any active source record id as well as the name/title (base.html had promised "name, sponsor, queue ID"; only the name matched). Tests in services/api/test_routes.py.
- 2026-09-15 coordinator: static assets — styles.css and map.js are linked with a content-hash `?v=` (web/assets.py) after a stale cached map.js on the owner's machine ran against the new template; registered in all four Jinja environments; web/test_assets.py.
- 2026-09-15 coordinator: map — `[hidden] { display: none !important; }` added to styles.css; `.filter-field`/`.legend` set `display`, which outranked the UA hidden rule, so the plant-type select and plants legend showed with the layer off (owner's screenshot). Guarded in web/test_map_layers.py; verified in Chromium off/on/off.
- 2026-09-18 coordinator: midstream and fuels wave recorded (owner's three answers) — docs/00-PLAN.md decision row (context layers + ownership graph, objective features only, valuation excluded, gate unchanged); twelve sources registered in data/sources.yaml section K with 2026-09-18 probe results (EIA-860 owner schedule confirmed in the 2024 archive; LMOP, AgSTAR, ethanol capacity, GLEIF reachable; Argonne and PHMSA behind Cloudflare challenges); docs/13 §2.12–2.14, matrix rows and counsel item 13 (EPA copyright hedge, GLEIF CC0, not-retrieved pages); docs/33 §1.8 candidate segment (midstream, RNG, ethanol operators) and priority list item 5; docs/40 §0 four browser terms tasks. No code.
- 2026-09-18 coordinator: ADR 0008 (assets first-class, placement grades, no rewrite) with the owner's decisions recorded in docs/00-PLAN.md; docs/21 §3.7 precision gains `country_centroid` and the grade mapping, §3.22 `asset` and §3.23 `asset_owner` specified; docs/23 §3.1 contract for /v1/assets, /v1/assets/geo, nearby-proposals, organisation assets, /v1/geo/regions and the region features and `placement`/`county_fips` filters on /v1/proposals/geo; data/sources.yaml `us.census.cartographic_boundaries` registered (both 20m zips probed 200). Build lanes dispatched against this contract.
- 2026-09-18 data-engineer: ADR 0008 data lane — `pipeline/connectors/us_eia_860/` (kind `document`, no `context` kind exists yet; comment explains the choice) crawls the EIA-860 index for non-archive `eia860<year>.zip` links (eia.gov disallows `/*archive/`, same rule `us_eia_860m` already honours) and skips a robots-blocked or placeholder candidate instead of aborting; today the only non-archive candidate, `xls/eia8602025ER.zip`, is itself a 55 KB HTML placeholder. The real archive sits under `archive/xls/`, which the connector must never request, so `pipeline/context/eia_owners.py --archive PATH` (shares the connector's `find_owner_member`/`parse_owner_sheet`) is the actual producer of `data/normalized/context/us.eia.860.owners.parquet` (`source_plant_id`, `generator_id`, `owner_name`, `ownership_pct` float 0–100, `as_of` date, provenance quartet) — same fetch/CLI split as `us_eia_860m`/`eia_plants.py`. Downloaded the real `archive/xls/eia8602024.zip` once by hand (curl, one-off human-equivalent fixture download, not the connector) to measure and to build a 60-row test fixture (`tests/fixtures/eia860_owner_2024_sample.zip`): the "Ownership" sheet header is on the second row (one row earlier than Planned/Operating); 5,496 raw rows, 1 with no Generator ID (dropped), 2 with a blank Percent Owned (kept, `ownership_pct` null) -> 5,495 owner rows; parse 0.76s. `Percent Owned` is a 0–1 fraction in the source, multiplied by 100. `pipeline/context/regions.py` vendors `data/vendored/regions/{us_counties,us_states,countries}.geojson` for the region-grade proposal polygons: Census cartographic boundaries (`pyshp`, no geopandas) for counties (3,222, GEOID `region_id`, state_code joined from the state file's own STATEFP→STUSPS records, none unmapped) and states (52), Natural Earth 1:110m Admin 0 Countries (public domain, via its GitHub mirror) for countries (177; two, N. Cyprus and Somaliland, have no ISO 3166-1 alpha-2 code in `ISO_A2` or its `ISO_A2_EH` fallback and share `region_id` "-99", a source data gap, noted, not fixed); both `www2.census.gov/robots.txt` and `raw.githubusercontent.com/robots.txt` (404, permissive) checked live. All geometry normalised to MultiPolygon, coordinates rounded to 4 decimals; `us_counties.geojson` measured 1,604,893 bytes, under the 4 MB budget, so the 3-decimal fallback was not needed; `us_states.geojson` 270,052 bytes, `countries.geojson` 219,447 bytes. `requirements.txt` gains `pyshp`. `data/sources.yaml` `us.eia.860` and `us.census.cartographic_boundaries` `verified.note` updated with these measurements; the census entry's `notes` corrected — countries.geojson is freshly vendored from Natural Earth, not reused from `web/static/data/basemap_fallback.geojson` as an earlier note assumed. 31 new tests, all offline (recorded fixture or in-test synthetic shapefiles/GeoJSON, no live fetch in CI): `ruff`/`ruff format`/`mypy` clean on this lane's files; `pytest pipeline` (141 passed). `pytest pipeline tests/test_connector_framework.py` could not be run to completion: `tests/conftest.py` imports `services.api.app`, which fails to import (`services/api/context_routes.py` references `Asset.generator_count`/`earliest_operating_year`, columns the in-flight `asset` migration dropped) — pre-existing breakage in another lane's in-progress files (`services/`, out of this lane's file area), confirmed via `git status` showing those files already modified before this session started.
- 2026-09-18 frontend-developer: ADR 0008 web lane — map: a "Placement" fieldset (exact/region/none, default exact+region) writes `placement=` to the URL and `/api/proposals/geo`; region features render as fill+outline+count-label layers (new `GET /api/geo/regions` proxy, batched per level, cached client-side for the session) beneath the assets layer and beneath proposal points/clusters (stacking falls out of insertion order alone, see `addRegionLayers()`'s comment); hover shows name/count, click navigates by level (county → `/proposals?county_fips=`, state → `?jurisdiction=`, country → the matching quick-view button or `?jurisdiction=`); the "Unplaced" note now reads `totals.unplaced` (was `meta.unplaced_count`); one `map.layer_toggled {layer:"placement"}` beacon per checkbox, no new event name. Assets: existing-plants fetch repointed at new `GET /api/assets/geo?asset_type=` proxy (`/api/context/plants/geo` untouched, still works), checkbox relabelled "Existing assets (EIA)", new asset-type select (`power_plant` enabled, eleven ADR 0008 types listed `disabled title="coming"`), asset drawer gains an "Open asset page →" link when the feature carries a slug. Pages: `/assets/{slug}` and `/organizations/{ident}` (identity, attributes table, owners/assets table linking by `public_id`, nearby exact-grade proposals, provenance) — organisation resolves by slug then falls back to a direct `public_id` lookup; `/search` gained an Organisations section (`GET /v1/organizations?q=`); new `/sitemap.xml` (none existed) covering proposals/opportunities/assets/organisations, cursor-paginated capped at 25×200 rows per resource, a failing resource skipped rather than failing the file. `web/dev_up.py` loads `us.eia.860.owners.parquet` via `services.ingest.ownership.load_owner_shares_parquet` when present, same optional-both-ways guard as the plants loader. New tests in `web/test_asset_pages.py` (23, all pass) drive a fake `Transport`, coded against the docs/23 contract; could not verify against a live API or the browser e2e path — `services/api/context_routes.py`'s in-flight `Asset` rename (same bug the data-engineer lane's changelog entry above records) makes `services.api.app` fail to import, so `web/test_e2e.py` times out waiting for `/health` with or without this task's changes (confirmed against the pre-task tree). `ruff`/`ruff format --check`/`mypy web` clean on every file in this lane's area (the six remaining `mypy web` findings and the ten `pytest web` collection errors are in `services/api/*`/`services/ingest/plants.py`, outside `web/**`, pre-existing). `web/README.md` "ADR 0008 — placement grades and the region layer" section added.
- 2026-09-18 backend-developer: ADR 0008 backend lane. Migrations `0009` (`built_plant`→`asset`: renames and widens per docs/21 §3.22 — `public_id`/`slug` backfilled with `services.ids`, slugs de-duplicated; `asset_type` check constraint over the twelve types, existing rows become `power_plant`; `source_asset_id`/`commissioned_year`/`unit_count` renamed from `source_plant_id`/`earliest_operating_year`/`generator_count`; `capacity_value`/`capacity_unit`/`geom_line`/`attributes`/`county_fips`/`first_seen`/`last_changed` added; `location.precision` check constraint gains `country_centroid`), `0010` (`asset_owner`, docs/21 §3.23), `0011` (`organization.parent_org_id`/`parent_source_id`) — all written with `op.batch_alter_table(..., recreate="always")` so the same code runs on SQLite and Postgres; two Alembic bugs found and worked around, both recorded in the migration/env.py comments: `context.configure(target_metadata=...)` outside autogenerate makes `drop_constraint`/`create_check_constraint` double-apply the ambient naming convention (`ck_location_precision_vocab` → `ck_location_ck_location_precision_vocab`) — fixed by passing `target_metadata` only when `config.cmd_opts.autogenerate` is set (`services/db/migrations/env.py`); a batch that renames a column and adds a constraint referencing the new name in the *same* `batch_alter_table` block silently drops the constraint on SQLite — fixed by splitting into two passes (already the shape `upgrade()` used; `downgrade()` needed the same split). Round-tripped (`alembic upgrade head`, `downgrade 0008`, `upgrade head` again) against a SQLite database stamped at a hand-built 0008-equivalent schema (`services/db/test_migrations.py`) — the full `0001`-`0008` chain itself still cannot run on SQLite at all (Postgres-only `CREATE EXTENSION`/`JSONB`/`UUID`/`geoalchemy2.Geography` with no fallback, predating this task; `services/db/migrations/env.py`'s own docstring already said so), which the stamp works around rather than fixes — out of this task's scope. Models: `Asset` (replaces `BuiltPlant`, kept as a plain alias — no code outside `services/db`/`services/api`/`services/ingest` was found importing it), `AssetOwner`, `Organization.parent_org_id`/`parent_source_id`, `LOCATION_PRECISIONS` gains `country_centroid`. `services/ingest/assets.py`: `load_assets_parquet(session, path, asset_type)` generalises the plants loader (idempotent upsert on `source_id`+`source_asset_id`, `public_id`/`slug` assigned once on insert, never recomputed); `ASSET_TYPE_SOURCE_IDS` wires only `power_plant`→`us.eia.860m` this sprint, one source per type, matching ADR 0008's "one parser each". `services/ingest/plants.py` is now a thin wrapper (renames an incoming `source_plant_id` column, delegates, translates the result back to `PlantsLoadResult`'s pre-ADR field names) — `web/dev_up.py`'s `load_plants_parquet(session, path)` import is unaffected. `services/ingest/ownership.py`: `load_owner_shares_parquet` reads Schedule 4 generator-level shares, aggregates to one `asset_owner` row per `(plant, owner)` (nameplate-weighted mean when a `generator_capacity_mw` column is present, unweighted mean otherwise — this sprint's parquet schema carries no per-generator weight, so it is always the unweighted mean in practice; stated and reasoned about in the module docstring, including a recorded limitation: two spellings of the same owner *for the same plant* aggregate as separate groups rather than one, since the group key is not yet `norm_org`-normalised). Coordinator correction applied: organisation resolution goes through `pipeline.normalize.norm_org` (corp-suffix/punctuation-stripping), not a raw case-fold — a case-fold key was measured to over-split organisations 8.2% on the eval corpus; alias confidence is `0.9`, not the loader's exact/punctuation-match `1.0`, since this is a looser match criterion; new test proves "NextEra Energy Resources, LLC" and "NEXTERA ENERGY RESOURCES LLC" resolve to one organisation. API (all new endpoints public, no lag, no tier gate, matching the pre-ADR context layer): `services/api/assets.py` — `GET /v1/assets` (filters `asset_type`/`technology`/`state`/`organization`/`q` over name, operator name and owner names), `GET /v1/assets/{public_id}` (`owners[]`, `attributes`), `GET /v1/assets/geo` (same process-local index-cache/cluster design as the old context-layer endpoint, generalised: keyed on `(count, max(last_changed))`, `asset`/`asset_cluster` feature kinds, `dominant_asset_type`), `GET /v1/context/plants/geo` kept as a same-router alias forcing `asset_type=power_plant` (rejects an explicit `asset_type` param — it is what the path means, not a choice), `GET /v1/assets/{public_id}/nearby-proposals` (haversine within `radius_km`, default 25 max 100, exact-precision proposals only, public visibility rules), `GET /v1/organizations/{public_id}/assets`. `services/api/regions.py` — `GET /v1/geo/regions` (`level` required, `ids` optional csv capped at 500, `REGIONS_DIR` env var so tests use a tiny fixture directory instead of the ~1.6 MB vendored county file; 404 with a clear detail when the level's file is absent). `services/api/geo.py` — `GET /v1/proposals/geo` grows `region` features: `county_centroid`/`state_centroid`/`country_centroid` proposals grouped by `(precision, region_id)`, geometry the group's own representative point (identical for every member, so no averaging is needed); `exact` keeps its existing point/cluster path unchanged, both kinds can appear together. `services/api/app.py` — new `county_fips` (real narrowing filter, affects totals) and `placement` filters (`exact,region,none`; drawing-only, never changes `meta.unplaced_count`/`totals.records`/the lifecycle and technology counts) on both `GET /v1/proposals` (no default — a `none`-grade record belongs in list/search per docs/21 §3.7) and `GET /v1/proposals/geo` (default `exact,region`, matching docs/23 §3.1 exactly). `services/api/conftest.py` gains `make_asset`/`make_asset_owner` and a `county_fips` default (Travis County's real FIPS, `48453`) on `make_location`, since a `county_centroid` location with no `county_fips` cannot be grouped into a region feature — an existing-test compatibility fix, not new behaviour. `services/api/context_geo.py`/`context_routes.py`'s old plant-specific implementation is retired (`BuiltPlant`'s renamed columns broke it outright); `context_routes.py` is kept as a one-line `TECHNOLOGY_VOCAB` re-export only because `web/test_map_layers.py` still imports it from there. `api/openapi.yaml`: new `AssetType`/`AssetStatus`/`AssetOwnerRole`/`PlacementGrade` enums, `Asset`/`AssetSummary`/`AssetOwnerRow`/`AssetGeoProperties`/`AssetClusterProperties`/`AssetGeoFeature(Collection)`/`AssetListResponse`/`AssetDetailResponse`/`NearbyProposalsResponse`/`RegionFeature(Collection)`/`RegionsResponse` schemas, `GeoRegionProperties` added to the existing `GeoFeature` discriminator (`state_aggregate`/`GeoAggregateProperties` kept, unreferenced by any route, not removed), `Placement`/`AssetType`/`AssetOrganizationFilter`/`RadiusKm`/`RegionLevel`/`RegionIds`/`CountyFips`(existing) parameters, six new paths, `/v1/context/plants/geo` repointed at the asset schemas with an updated description; `LocationPrecision` gains `country_centroid`. `openapi-spec-validator` and `python api/check_story_coverage.py` (44/44 stories, 140 operations, all `$ref`s resolve) both clean. Tests: `services/db/test_migrations.py` (new, 1 test, the round trip above), `services/db/test_models.py` (+4), `services/ingest/test_plants.py` (rewritten for the new field names, 7), `services/ingest/test_assets.py` (new, 5), `services/ingest/test_ownership.py` (new, 6, including the coordinator's norm_org test), `services/api/test_assets.py` (new, 18), `services/api/test_regions.py` (new, 9), `services/api/test_placement.py` (new, 11), plus two pre-existing `services/api/test_routes.py` tests updated for the new region-feature behaviour (a `county_centroid` proposal now renders as a `region` feature, not a `proposal` point — ADR 0008's explicit intent). Measured: `pytest tests services` (whole shared suite, not just this lane's files) passes in full, 1,172 tests by dot count, exit code 0; `ruff check .`, `ruff format --check .`, `mypy` (plain config) and `lint-imports` all clean repo-wide. `mypy --strict services` has 5 remaining findings, all pre-existing and outside this task's changes (`services/sor/wiring.py:16,23`, `services/api/admin_records.py:1383,1389`, `services/api/app.py:541` inside the pre-existing `_source_licence_aggregate` — the same "unused ignore" pattern I fixed in my own new code in `services/ingest/assets.py`, `services/ingest/ownership.py` and `services/api/assets.py`); left untouched as out of scope rather than fixed silently. Could not do: wire any `asset_type` beyond `power_plant` to a real source (no other ADR 0008 connector existed to load from yet, per the "one parser each" phasing); exercise the Postgres branches of the new dialect-aware code (no Postgres in this environment, same standing caveat as every other module here).
- 2026-09-18 coordinator: verified and landed the three ADR 0008 lanes together (core 1,332 passed, web 166 passed incl. both browser tests, ruff/format/mypy/strict-infra/import-linter/spec 44/44 over 140 operations). Audit fixes applied in the same commit: drawer and tooltip markup now escapes register text and validates hrefs (web audit, DOM injection); `/v1` GETs carrying any credential are `private, no-store` with `Vary` (API audit S1); a credential that resolves to nothing is metered on the public IP bucket (S3); login `next` rejects backslash and host-bearing paths; alembic env keeps existing loggers (order-dependent billing test); sitemap cached one hour and tolerant of transport errors; 21 MB of unreferenced static data dumps removed from `web/static/data/`. Sixteen review reports received (five code audits, eleven specialists); synthesis in docs/50-audit-2026-09-18.md. Live-probe fixes in the same commit: `API_INTERNAL_TOKEN` service identity so the site's server-side calls skip the anonymous per-IP bucket (a sitemap render plus a few map pans had exhausted 60/h and 429ed the map); `slug` filter on `/v1/organizations` so company pages resolve; the map reads `meta.unplaced_count` (docs/23 aligned). Measured on the ADR 0008 dev load: active proposals by precision exact 2,341 / county 2,235 / state 15 / unknown 1,246 (5,837 active; 40% exact, 39% region, 21% none); 14,659 power_plant assets; five state polygons 14 KB gzipped; sitemap 9,339 URLs in 6.4 s cold, cached for an hour. Also fixed from the same probe: cursor pagination on a NULL sort value (API audit S5; `NULLS LAST` on both dialects, cursor condition handles a null value; test walks the opportunities list both directions across null `due_at`) and the dev stack now sets the service token before the API process starts. `slug` filter also added to `/v1/assets` (asset pages resolve by slug like proposal pages); spec parameter added.
- 2026-09-18 coordinator: CI migration job red on 01378f6 — `op.batch_alter_table(recreate="always")` rebuilds the table on Postgres too, and geoalchemy2's after_create hook fails on the TypeDecorator-wrapped geometry column (`'Text' object has no attribute 'spatial_index'`). Migrations 0009 and 0011 now recreate only on SQLite (`_recreate()`), Postgres gets plain ALTERs; SQLite round-trip test unchanged and green.
- 2026-09-18 coordinator: CI web job red on 01378f6 — the browser smoke test loads the 2026-09-12 eval parquet (no county FIPS, no exact coordinates), so after ADR 0008 nothing rendered: county-grade rows without a FIPS were dropped from region features and no point features existed. `services/api/geo.py` now groups a county-precision location without a FIPS at its state (then country) region instead of dropping it; the smoke test accepts region rendering as the honest map when a fixture has no exact rows. Reproduced locally with `E2E_DATA_ROOT` pointing at an empty tree, green after the fix; placement test added.
- 2026-09-19 devops-engineer (blockers sprint, infra lane): env file reaches every container (SOPS→dotenv), `SESSION_SECRET` fails fast outside dev, GHCR release workflow + `IMAGE_TAG`, deploy.sh reordered (sync→pull→stop→migrate→up→health→rollback), base Compose valid alone, nightly backup timer + tools + R2 retention 14d/8w, `SENTRY_DSN`-gated error tracking and JSON logging via `infra/entrypoint.py`, CI gates measured (88% floor on; visibility 96% and spec 120/135 report-only), preview stub cannot fail; tests `infra/test_*.py`, `tests/test_session_secret.py`.
- 2026-09-19 data-engineer (blockers sprint, pipeline lane): content-keyed duplicate ids (no positional `#2`; legacy keys accepted on read), event idempotency key carries before-state and observation time and removed events now load, provenance re-stamped on change, runner fails closed on any parse error and the CLI batch continues, 403/503 challenges reported blocked and robots evaluated for the Bankable token first; scheduler: exponential fetch backoff (25/125/625/3,125 s measured, transient only), `source_run` + `source.health` written on the scheduled path, and the loop closed with `load_source` → `resolve_tick` → `enrich_tick` under per-source locks.
- 2026-09-19 backend-developer (blockers sprint, licence-gate lane): register/manifest reconciliation check (`scripts/check_manifest_licences.py`; 14 sources gated to `unknown`; explicit `publication` field on all 75 sources, read by the loader with the notes regex as a warned fallback); asset licence gate on every read path; exact coordinates under raw-forbidding licences served at region grade with `precision_reason: licence`; events visible only when their subject is; spec updated; 34 tests.
- 2026-09-19 backend-developer (blockers sprint, privacy/publishing lane): digest, feed and webhook paths gated on event AND subject-record visibility; erasure hashes identifiers in the audit log, tombstones email, clears task contact, pauses searches; `suppression` + `privacy_request` tables (migration 0012), `POST /v1/privacy/requests` + admin reads + `/privacy/request` form; `List-Unsubscribe`/One-Click headers, legal postal line and tier notice from env with production refusal; "None" template gate; disclosure names Infraque via `PRODUCT_*` env (docs/13 §6.5, docs/32 §2.2).
- 2026-09-19 coordinator: integrated the four lanes (privacy router mounted, four spec operations added, 144 total, 44/44 stories); third `SESSION_SECRET` fallback in admin_records routed through `auth.session_secret`; production switch unified on `ENVIRONMENT` with `APP_ENV` as alias; new env vars documented in `infra/compose/.env.example`; commit-msg guard (`scripts/check_commit_messages.py`, `make hooks`) ahead of the one-time history rewrite (owner decision 2026-09-18).
- 2026-09-19 coordinator: CI web job red on 8399630 — the browser smoke test loads the 2026-09-12 eval parquet on the runner, whose repeated byte-identical "Untitled" NYISO rows share one content key under the new dedupe and the second violated `proposal_source`'s unique key. The loader now falls back the way `suffix_duplicates` does (raw-row hash, then an order suffix); regression test in `services/ingest/test_loader_events.py`; reproduced with `E2E_DATA_ROOT` on an empty tree, green after the fix.
- 2026-09-19 frontend-developer (midstream slice, map and pages lane): map "Existing assets" control lists asset types (five live, ethanol/RNG coming), gas pipelines as a line layer (solid interstate / dashed intrastate, width by zoom, hover name+operator, click opens the shared drawer), diamond/ring/triangle icons for gas processing/storage/LNG, per-type legend, region and asset rows in the in-view list (keyboard-reachable); shared basemap.js; asset page with static SVG + MapLibre map, pipeline fields, operator linked to the company page, nearby proposals with distance; company page with role×type summary and groups, asset map, parent/subsidiaries, proposals near its assets, sources derived from its records (no false "withheld under licence"); /search Assets section; /assets/by-id/{public_id}; docs/31 §1.6, docs/30 §3.1; 22 web tests added (184 pass).
- 2026-09-19 data-engineer (midstream lane): EIA Atlas gas layers -> asset (pipeline/context/eia_atlas.py + shapefile.py/geo.py, no new deps): ArcGIS services token-gated on 2026-09-19, fell back to EIA's own map_data zips (pipelines 32,961 segments -> 259 (operator,type) rows / 230,252 geodesic mi / 2.3 MB parquet; processing 478; storage 412; LNG 8); geom_line widened to MultiLineString (migration 0013, GeographyLine now WKT on both dialects, PostGIS round-trip verified); services/ingest/midstream.py writes 1,126 operator + 457 owner edges and curated parent links (data/vendored/organizations/parents.yaml: Tallgrass Energy over 9 Atlas operator strings, source id curated.organization_parents); docs/21 §3.22-3.23, docs/22 §15; 47 tests.
- 2026-09-19 data-engineer (fuels lane): ethanol and RNG point layers on the asset model — pipeline/context/{fuels,ethanol_capacity,lmop,agstar,ethanol_plants}.py with fixture tests (113 in pipeline/context); live runs: EIA capacity table 191 plants, LMOP 1,353 assets + 129 proposals, AgSTAR 498 + 73, EIA Atlas ethanol 197 (ArcGIS service token-gated → EIA map_data shapefile, terms quoted); EPA cover-sheet checks recorded (docs/02 §10); parquets load through the existing asset loader unchanged.
- 2026-09-19 backend-developer (midstream slice, API lane): line assets on `GET /v1/assets/geo` (`asset_line` features, bbox intersection, zoom simplification, 1,500 cap, `slug`/`url`, `organization` filter), line-aware asset detail (`geometry`, `length_miles`) and nearby-proposals (distance to the line, SQL bbox cut: 2,470 → 194 ms measured), organisation asset totals, roles and hierarchy, `GET /v1/organizations/{id}/nearby-proposals`, subsidiary roll-ups, generic asset geometry licence gate, gzip responses (national view 930 KB raw / 200 KB wire, 224 ms warm); dev_up loads the eight midstream and fuels parquet files with operator edges and curated parents; spec 145 operations; 25 tests.
- 2026-09-19 coordinator (midstream slice integration): asset pages fetch the detail envelope after resolving the slug (owners and geometry were missing on the first Tallgrass screenshots); company pages request the organisation group (`include_subsidiaries=true`) for assets and nearby proposals and show which subsidiary holds each row; docs/13 §6 row for `curated.organization_parents`; three web tests (187 pass). Verified on the integrated tree: 1,655 core tests, 187 web tests, migration 0013 up/down/up on PostGIS, static gates, spec 145 operations, manifest 76 sources.
- 2026-09-19 data-engineer (ownership lane): EIA-860 Schedule 4 fetched live (`xls/eia8602025.zip`, 23,622,347 B, 5,680 rows); owner parquet gains generator status and Schedule 3 nameplate; the ownership loader computes plant-nameplate shares, flags over-100% generators rather than normalising them, skips the `Other` placeholder, and fixes an organisation-key idempotence bug that re-created one organisation and 20 edges every run; 3,118 edges over 2,315 plants (1,735 organisations created, 132 matched, re-run clean); `web/dev_up.py` loads owner shares then calls `enrich.apply_context_features` behind a guard; `data/vendored/organizations/aliases.yaml` seeded with four SEC-sourced rules; docs/22 §15.3–15.4. Recorded for the entity-key slice: `norm_org` strips industry words, so 7 of 2,037 owner strings over-merge distinct companies.
- 2026-09-19 frontend-developer (fuels map lane): ethanol and RNG on the map (toggles live, hexagon and pentagon icons, legend groups with unplaced-row notes, tooltips, in-view rows) and their asset pages (nameplate, feedstock, PADD, project type, rated MW, LFG flow, end use, host landfill or digester type, years); company-page descriptor for `other`-typed asset holders with the raw type kept in the fields; EIA-860M generator units grouped as "× n units" with summed capacity on nearby and in-view lists (shared helper); search labels the asset type; `/api/assets/{public_id}` relay so the drawer can show attributes the geo feature omits. Two defects fixed: `fmtNumber(null)` rendered a real-looking "0 MMgal/yr", and the in-view count used the API's dataset-wide `totals.records` instead of the viewport. 17 tests added (210 web tests pass).
- 2026-09-19 coordinator: corrected a reported API bug. `/v1/assets/geo`'s `totals.records` was called wrong for ignoring the bounding box; it is deliberate and matches `/v1/proposals/geo` (a record the viewport excludes, or one with no geometry, stays counted). The spec left `records` undescribed, which is how it was misread: described now in `AssetGeoTotals`, in the builder, and pinned by `tests/test_api_assets_lines.py::test_assets_geo_records_total_covers_the_whole_filter_match_not_the_viewport`. The map's client-side in-view count stands as the right fix for the label.
- 2026-09-19 data-scientist (entity-key lane): organisation key narrowed to legal forms only (`pipeline/normalize.py::norm_org`, new `org_key` used by the resolver, the ownership index and the midstream parent lookup). Measured census of all 557 pairs the old key merged: 46.3% were not one company (121 different companies, 137 parent-or-affiliate); over-split rate 1.5% (3 of 200, 95% CI 0.51–4.32%). Merge precision 0.537 → 0.957; resolver precision/recall 0.927/0.950 → 0.975/0.975; store path 0.946/0.946 → 1.000/0.973; +137 organisations and 122 of 3,217 `asset_owner` edges re-attributed on rebuild. Labelled set `data/eval/organization_pairs.csv`, guard tests `tests/test_org_key.py`, docs/22 §16.
- 2026-09-20 data-engineer (features lane): PHMSA per-operator mileage, vintage, diameter mix and incident counts (origin 403s at its Akamai edge, so the connector records that and reads the Internet Archive copy; 1,376 operators, 297,165 onshore transmission miles; three source workbooks damaged at source, the newest intact year is used and named); EIA-923 2025 final plant features (14,045 plants; capacity factor and heat rate derived at enrichment, nothing clamped, a heat rate written only where a combustible fuel is reported); EPA Part 80 RFS D-code registrations (1,138 facilities; the manifest's RFS page carries no facility-level file, which settles that browser task). New source `us.eia.form923` with docs/13 §2.15 terms and a §6 matrix row; §2.16 records the PHMSA access route and that the Archive's own terms are unread. `services/ingest/enrich.py::apply_context_features` merges all three onto assets, idempotent and creating nothing: PHMSA 92/259 pipelines, EIA-923 13,910/14,659 plants, RFS 271/388 ethanol and 48/1,851 RNG. Cross-dataset operator aliases live in `external_operator_aliases.yaml`, kept apart from the ownership lane's `aliases.yaml`. 70 tests on committed fixtures, no live network.
- 2026-09-20 frontend-developer (navigation lane): `/assets` and `/organizations` index pages (were 404), with by-type counts, filters, sort and cursor pagination mirroring the proposals list; Open Graph, Twitter card, canonical and robots meta on every public page, each with one source and `noindex` on auth, form and search pages; JSON-LD `BreadcrumbList`, `Organization` and `ItemList` with absent fields pruned and markup characters escaped to `\uXXXX`; `robots.txt`; the sitemap raised past a silent 5,000-URL truncation to 34,692 measured URLs, split into an index plus chunks above 25,000, with the whole build cached per base URL (20 s cold, 8 ms warm); docs/30 §3.2, docs/31 §5.14; 33 cases in `web/test_seo.py` (243 web tests pass). Asked of the API: `asset_counts` on organisation list rows, which would remove one call per row from the company index.
- 2026-09-20 backend-developer (leftovers lane): migration 0014 drops the stale `idx_built_plant_geom` GiST index the 0009 rename left on `asset` (confirmed on PostGIS, round-tripped; the same implicit-index pair survives on `location` and `event`, left for a bounded follow-up); event visibility gains the missing opportunity-subject tests, mutation-checked, and its CI coverage gate is enforced at 100% with nothing excluded; the spec-versus-app step enumerates the app from `app.openapi()` and the spec from `paths` plus `webhooks`, reconciles with `check_story_coverage` (145 = 136 + 9), and lists the 15 unimplemented operation ids by area, still report-only, pinned by `tests/test_spec_operation_inventory.py`.
- 2026-09-20 coordinator: moved `POST /v1/privacy/requests` and the three `/admin/v1/privacy-requests` operations from `webhooks:` into `paths:` in `api/openapi.yaml`. They were misfiled when the privacy lane's block was merged on 2026-09-18 (the insertion targeted the line above `components:`, which is the end of `webhooks:`, not of `paths:`), so four served endpoints were documented where an endpoint cannot be validated as one. Found by the leftovers lane's new inventory step, which first reported it as spec drift; the operation count is unchanged at 145.
- 2026-09-20 data-scientist (ownership-graph lane): GLEIF Level 2 parent links behind a 272-pair hand-labelled census (`data/eval/gleif_matches.csv`). `pipeline/context/gleif.py` reads the 24,368,364 B relationship golden copy plus one streaming pass over the 504,405,368 B entity file into 259,784 direct and ultimate parent rows carrying GLEIF's own as-of dates. `services/ingest/organizations.py` matches on `pipeline.normalize.org_key` plus a country-evidence gate: precision 0.985 (198/201, 95% Wilson CI 0.957–0.995) against 0.949 without the gate, and zero wrong-company matches. 199 organisations linked to 105 parents on a copy of the dev store (9 → 208 with a parent, 304 carrying an LEI, `asset_owner` untouched); migration 0015 adds `organization.parent_as_of`. GLEIF publishes no Level 2 record for any of the nine Tallgrass children, so every curated rule stands and the curated file is a permanent mechanism rather than a placeholder. `aliases.yaml` gains a loader and the three SEC-sourced rules docs/22 §16.5 left for it, under the new source `curated.organization_aliases`. Corrections route specified, not built (docs/22 §17.6). docs/22 §17, docs/21 §3.5 and §3.23.
- 2026-09-20 coordinator: updated the dev-runner ordering pin for the organisation-graph step the GLEIF lane added (the test lived outside that lane's file area and had stubbed only the first two steps, so the real third one ran against a dummy session), and replaced a test that asserted the wrong contract for it: unlike the owner-share and feature steps, it always has work to do, because the curated alias file ships in the repository and applying it registers its source.
- 2026-09-20 coordinator: the site and the API now say which build served the page. `services/api/build_info.py` resolves the commit from a build-time stamp (`GIT_SHA` and three common conventions) or, failing that, from the working tree, flagging uncommitted edits; `data_as_of` reports the newest `retrieved_at` across loaded sources, which moves independently of the publish lag. `/v1/health` carries both under `build` and `source_data_as_of` (documented in the spec), and the footer prints the short commit and the fetch date on every page, in all three template environments. The helper sits in `web/viewmodels.py` because `web/auth.py` and `web/legal.py` keep their own globals to avoid a circular import. Answers "how do I know I am seeing the latest version?", which is two questions: current code and current data. 6 tests.
- 2026-09-20 coordinator: `docs/61-run-the-latest-locally.md` — step-by-step instructions for running the current code and current data on the owner's machine, written after "how do I know I am seeing the latest version?". Covers the 2026-09-19 history rewrite (reset, do not pull), the new dependencies, every context-layer connector with its download size, and the footer check. README quickstart updated: it predated every context layer and named only the proposal and opportunity connectors.
- 2026-09-20 data-engineer (pipeline-merge lane): EIA Atlas pipeline parts are chained at ingest. `pipeline/context/geo.py::merge_touching_lines` joins parts whose endpoints coincide at the stored 6-decimal precision into maximal runs, reversing a part where that is what makes it join, and `normalise_pipelines` calls it before writing WKT (run summary now reports `parts_before_merge` / `parts_after_merge`). Measured on the recorded 2026-09-19 snapshot: 33,184 parts → 17,997, stored vertices 194,887 → 179,700, zoom-4 parts 17,613 → 10,511 and vertices 36,571 → 24,309 (66.5%), national zoom-4 geometry (empty feature properties, so the difference is geometry alone) 662.5 → 445.5 KB JSON and 168.8 → 114.9 KB gzipped. The cause was part count, not detail: Douglas-Peucker keeps both endpoints of every part, so 96% of the zoom-4 vertices were the two-per-part floor and no tolerance could move them. Chains stop at any node where three or more parts meet — chaining greedily through junctions would reach 11,945 parts and 19,955 zoom-4 vertices, measured and not taken because it asserts continuous runs the source does not describe. `miles`, `states_crossed` and every row's distinct vertex set are identical before and after; the representative point moved on 131 of 259 rows (median 92 km) and stays a vertex of its own line, and `state_code` changed on 30 multi-state rows (25 to another state the line crosses, 5 from null), reported in docs/21 §3.22 rather than shipped quietly. The `simplify_parts` docstring in `services/api/lines.py` carried two wrong figures (67,710 stored, "a few thousand" at zoom 4); corrected to the measured 194,887 and 36,571, with the two-vertex floor named. Behaviour of `simplify_parts` unchanged — the fix is at ingest. docs/21 §3.22, ADR 0008.
- 2026-09-20 backend-developer (paywall-by-shape lane): the blanket public lag is gone. `services/ingest/lag.py` now computes `public_at = published_at` for every `proposal` and `opportunity` (no per-source knob: there is no record delay to configure), and `published_at + lag` for a change event only where its source declares one. The predicate is data, not code: `data/sources.yaml` gains `change_event_lag_days`, carried today by exactly the eight `us.iso.*` interconnection-queue registers at 14 days, mirrored into `source.lag_days` by the loader and overridable per event type by an audited admin PATCH; keyed off the source row rather than `source.category`, whose `generation_queue` vocabulary also covers six non-ISO connection registers (NESO, AEMO, EirGrid, IESO, AESO, the OASIS non-ISO queues). Migration 0016 recomputes the stored column on existing rows — records to `published_at`, ISO events to +14d, every other event live, `source.lag_days` seeded — round-tripped upgrade/downgrade/upgrade on PostGIS with seeded rows (14d/7d → 0/0/14d → 14d/7d → 0/0/14d, idempotent) and on SQLite in `tests/test_migration_0016.py`. **`services/api/visibility.py` was not edited at all**, which is the licence-gate proof: `tests/test_licence_gate_survives_lag_removal.py` pins that the licence clause is byte-identical across `public`/`pro`/`api` when compiled with literal binds, and that restricted and unknown sources — PJM named specifically — stay invisible on records, events and all three feeds; mutation-checked (widening `PUBLISHABLE_REUSE_CLASSES` fails all 10 of its cases, a pro/api-only widening fails 3; baseline 10 passed). Paid shapes pinned in `tests/test_paid_shapes_are_gated.py`: alerts and saved searches need `pro`, keys and webhooks need `api`, and **export and watchlists have no routes at all** — `exports_per_day`/`export_rows_max` are quota fields on `GET /v1/me` with nothing behind them, which is why `docs/41` now says Team is seats-plus-support until they exist. **Finding, measured, reported not acted on:** the surviving ISO delay is defeatable by anyone who looks twice. `pipeline/diff.py` fires events on exactly `lifecycle_state`, `capacity_mw` and `proposed_cod`; all three are public record fields, the record updates the instant the event is withheld, `last_changed` publishes the date, and `-last_changed` is the public list's default sort. `tests/test_free_tier_change_reconstruction.py` reconstructs the withheld `(field, before, after)` from two public reads for all three fields while `GET /v1/events` returns nothing, and prices a full daily sweep at 53 requests (10,409 proposals ÷ the 200-row page cap) against the public tier's own 1,440/day. The two event types that name no canonical field are legible too: `first_seen` and `gone_at` are in the public provenance block, so arrival and disappearance from a register are dated on the day they happen. Docs corrected to the shape model with the superseded text struck rather than deleted: `docs/21` §5.4 (+ D-1, C-4, §3.10, §4.1, §8 table), `docs/04` D-3/D-26/D-28 and §10 pick 1, `docs/10` (tier table, Lag definition, US-101 AC3, US-201 AC4, US-503 AC1, US-601, US-604, A-7), `docs/41`, `docs/20` §5/A-8, `services/README.md` decision 4; `api/openapi.yaml` tier tables, `public_at_semantics`, `Meta.lag_days`, `AdminSource.lag_days`, `lag_days_default` (new `iso_change_events`), feed titles and the record examples. Flagged, not rewritten: `docs/11` §3's delay schedule and its 1–2% free-to-Pro conversion estimate were both argued from a free tier that shows stale data, so the estimate now rests on a mechanism the product no longer sells and has not been re-derived; `docs/11` §5 item 2's "freshness premium" sentence likewise. Site copy follows (masthead, footer, banner, about) and the admin source form relabels its lag field to "change-event lag" with a hint.
- 2026-09-20 data-engineer (gb-settlement-geocode lane): NESO TEC register rows whose "Connection Site" names a town rather than a Grid Supply Point now place. The licence question came first and both candidates cleared it: the ONS *Index of Place Names in Great Britain (July 2024)* and OS Open Names are each Open Government Licence v3 — the ONS portal item's own `licenseInfo` resolves to a page stating ONS material may be re-used "whether commercially or privately … freely without a specific application for a licence", and OS Open Names' terms were read from the `Doc/licence.txt` inside its own package (ranged request, not a 103 MB download), which points at a URL redirecting to the same OGL v3 text. ONS was chosen on fit, not rights: WGS84 coordinates against OS's British National Grid eastings/northings, and one 47 MB CSV against 820 per-grid-square files covering roads and postcodes too. OpenStreetMap was excluded and never fetched (ODbL share-alike, open question 7(b), commercial product). New source `gb.ons.ipn_gazetteer`, docs/13 §2.17 and the §6 matrix; 52,110 rows vendored to `services/ingest/data/gb_settlements.tsv` from the 89,689 settlement-type records (locality/built-up area/parish/community), one row per normalised name. `services/ingest/geocode.py` gains `SettlementGazetteer` as a fallback *after* the GSP lookup, which always wins — a substation's own coordinates beat a settlement centroid. Measured on the stored snapshot: 739 → 1,068 of 2,198 rows placed (33.6% → 48.6%) and 346 → 512 of 1,237 distinct sites (28.0% → 41.4%), every gain inside the "named after a settlement" class and none in the connection-node or offshore classes. Ambiguity is refused, not tie-broken: 56 sites (105 rows) name more than one place ("Thornton" is six, "Overton" sixteen) and stay `unknown`; the count is carried per name in the vendored table, computed across all four settlement record types after the cross-type check caught "Coddington", which has one locality record in Derbyshire and built-up-area/parish records for the Newark one the register means. Word boundaries are kept here, unlike the 371-name GSP key, because folding them across 52,110 names put "Whitelee" (the wind farm south of Glasgow) on "White Lee" in Kirklees. Precision stays `county_centroid` and argued as such in the code: the vocabulary has no bucket between it and `exact`, and what the grade must describe — how far the project may be from the point — is county-scale for both GB tiers. **Finding, measured and not acted on:** cross-checking all 166 new placements against the transmission owner the register names in `HOST TO` puts 11 outside that owner's licence area (1 of 346 for the GSP tier); about 8 are genuinely wrong, all Scottish sites colliding with a small English locality — "Torness" onto a Torness in Highland, "Griffin" onto a Griffin in Lancashire, "Greens" onto a Greens in Rossendale. Nothing in the matched record distinguishes them; the constraint that would is the `HOST TO` region, which `geocode()` never sees, so wiring it belongs with wiring `country` through at the loader's call site. Until then the whole GB tier stays dormant. 24 tests; `docs/21` §3.7 `geocoder` gains `gb_settlement`.
- 2026-09-20 frontend-developer (pricing lane): `/pricing` exists (it did not), rendering the four tiers from `docs/41` — every price, period and inclusion taken from that document, nothing invented — linked from the primary nav and added to the sitemap. `docs/41`'s do-not-publish rules are enforced by a test, not by care: `web/test_pricing.py::test_unbuilt_shapes_are_not_advertised` fails if the words export or watchlist reach the rendered page in any signed-in or signed-out state, so the copy cannot ship before the routes; the same reasoning keeps the calendar, proposal-to-opportunity matching, licence pass-through and bulk pulls off the page (bulk is `x-status: planned` with no implementation, which `docs/41` does not flag — reported to the coordinator). The two anchor figures `docs/41` marks "verify before publishing" (Team $9,000, API $25,000) render as "Indicative price", carried as a `price_is_provisional` field rather than styling. The one surviving delay is read from `GET /v1/health`'s `lag_days_default.iso_change_events` (never hard-coded) and rendered through one variable in two adjacent blocks, so the owner's open question about dropping it is a one-field change; a test asserts both sentences disappear together when the API reports zero. Upgrade path: signed-in POST → `POST /v1/billing/checkout` → follow the hosted session URL (https-only redirect; no card form anywhere in this repo); signed out → `/register?next=/pricing?plan=…` and back to the tier picked; **a user with no email on file is shown what to do before pressing anything** and again if the route's `conflict` arrives, since no route in this codebase can set an email after the fact; a paid entitlement shows the billing-portal form instead of buy buttons. Because no processor account exists, the dry-run billing port answers with a `.invalid` URL (RFC 2606, guaranteed not to resolve); following it would put a dead browser page where the payment form should be, so the page detects the reserved TLD and answers "Payments are not switched on yet" with a contact address instead of redirecting. 21 tests in `web/test_pricing.py` against the real API and `services/billing/fake.py` (no network), plus a Playwright smoke that reaches the page from the nav at 400px and measures zero horizontal overflow. **Open for the owner: no Stripe account exists, so the buy buttons cannot complete a purchase today** — built linked as instructed; the coordinator's report argues for unlinking until the processor is live.
- 2026-09-20 data-engineer (gb-settlement-geocode lane, second pass): the `HOST TO` region constraint is built, not recorded as a follow-up. `geocode()` takes an explicit keyword-only `region` — **not** the unused `state` argument, because `loader.py` already passes `state` positionally from `Location.state_code`, so reusing it would have meant the loader silently supplying a region the moment `country` was wired, which is exactly the switch-on this is meant to prevent; a distinct name also keeps one parameter from carrying two incompatible vocabularies. It is a precondition, not a filter: without a region the settlement tier places nothing at all, so wiring `country` alone cannot turn on unconstrained placement. The GSP tier deliberately takes no region and is unchanged, asserted rather than assumed in a test — those 371 names are NESO's own list of the substations the register connects into, and the cross-check finds 1 disagreement in 346 there against 11 in 166 for the settlement tier. The area test is not latitude bands: `_P` and `_N` overlap by three quarters of a degree, which is why the old bands called Carradale in Kintyre and Cousland in Midlothian impossible. `services/ingest/data/gb_substations.tsv` gains a `region` column re-derived from the source CSV's own `GSP Group` — re-downloaded 2026-09-20, all 371 rows joining back on an exact `(Name, Latitude, Longitude)` triple, `_P` → north Scotland (72), `_N` → south Scotland (94), the other twelve groups → England and Wales (205), the two group-ambiguous rows (East Claydon `_B`/`_H`, Ferrybridge B `_M`/`_F`) resolving because both members are England and Wales. No new source and no new licence: same NESO dataset, same `gb.neso.fes_gsp_gazetteer` row, column-addition precedent of the 2026-09-15 `county_fips` work. `SubstationGazetteer.region_of_point()` classifies a point by nearest GSP — no tuned radius, and "whose network is this in the catchment of" is the question `HOST TO` answers too (Branxton in Northumberland classifies as south Scotland because Eccles and Berwick are its nearest GSPs, which is the intended semantics). Validated at 345 of 347 GSP-placed site/owner pairs; the 2 exceptions are register-side, both `SHET` rows standing in south Scotland. **Measured cost of the constraint: 19 rows / 13 sites.** Placement goes from 1,068/2,198 rows and 512/1,237 sites unconstrained to **1,049/2,198 (47.7%) and 499/1,237 (40.3%)** constrained, against 739/346 before the tier existed. Per-site inspection of all 13: 8 sites (13 rows) correctly refused — every wrong placement previously reported, Greens, Torness, Blacklaw ×2, Griffin, Nant, Fairburn, Blackcraig; 3 sites (3 rows) lost that were right — Cousland and Dunlop, recorded `NGET` while standing in Scotland (same register-side inconsistency as the validation exceptions), and Gretna, the one true classifier artefact, refused on a 1 km margin because Harker near Carlisle is 9 km away and Chapelcross 10 km; 2 sites (3 rows) uncertain. The independent latitude-band cross-check that found 11 impossible placements in 166 finds **0 in 153**. Outcomes are reported rather than collapsed: `gb_settlement_match()` returns one of `placed | no_region | unknown_region | not_a_settlement | ambiguous | outside_region`, while `geocode()` still returns `unknown` for all five refusals, because docs/21 §3.7's precision vocabulary has no value for "matched and refused" and inventing one is a CHECK-constraint schema change. **Still wrong and stated as such:** `Norton East 400kV Substation`, an `NGET` site whose projects are in County Durham and Teesside, still places on Norton East in Cannock Chase — an England-to-England collision no region key in the register can separate. 20 further tests (60 → 80 in the module), including one asserting Greens resolves by name and is refused only by the region, so deleting the check fails it.
- 2026-09-20 coordinator: `services/billing/README.md`'s "Deferred" list had three entries that were done — `POST /admin/v1/subscriptions` (implemented, `x-status: live`), mounting the billing router (`services/api/app.py:128`) and wiring `subscriptions_for_account` into `GET /v1/account` (`services/api/pro.py:172`, since the Sprint 3 first wave). The drift was not free: the pricing lane read the file, reported the account endpoint as still returning an empty `subscriptions` list, and the claim reached a pull request body before anyone checked the code. Corrected, with the three moved to a "Done" heading and a note that a handed-off item stops being deferred once it lands. Naming the plan on the pricing page's manage-billing block is left undone deliberately: `/v1/me` carries the tier but not the plan, so it would cost an extra call per render for "You are on Pro" over "You are on a paid plan".
- 2026-09-20 coordinator: `web/dev_up.py` rebuilds the local database instead of reusing a stale one. `init_db` is `metadata.create_all`, which creates missing tables and never adds a column to a table that already exists, so a `web/.data/dev.db` written before a migration kept its old schema for ever. Found by running the owner's own `docs/61` instructions: the site started and then died on the proposals query with `no such column: organization_1.parent_org_id`, because that database predated migration 0015. A run without `--skip-load` now deletes the file first (it reloads every row anyway, so the old file bought nothing but the failure), and a `--skip-load` run compares the ORM columns against the database and exits with the missing names rather than letting a SQL error surface from whichever page reads the column first.
- 2026-09-20 backend-developer (company-page relevance lane): the company page's "Proposals near these assets" list is filtered by default to the technologies the company's own asset types make relevant, and says so in words (owner, 2026-09-20: "given tallgrass doesnt do solar, I dont want them seeing solar or at least have the ability to just see whats relevant to them such as natural gas"). Three pieces. (1) `GET /v1/organizations/{public_id}/nearby-proposals` takes `technology` (CSV, the `csv_param` convention and `_technology_filter_values`, so an unknown token is a 400 rather than a silent pass) and `totals` now carries **both** counts: `proposals_within_radius` is what the filter left, `proposals_within_radius_unfiltered` what it was taken from. Two counts, not one, because one count cannot say "N of M" and a caller that shows a shortened list without a denominator is lying by omission; the filter is applied after the distance pass rather than in the candidate SQL for exactly that reason, at no measurable cost since the candidate load is bounded by the bbox either way. (2) `data/vendored/relevance/asset_technology_relevance.yaml`, an owner-editable mapping from asset type to relevant technologies, headed by a statement that it is a commercial judgement and not a derived fact, with the reasoning next to each list. Gas midstream (`gas_pipeline`, `gas_processing_plant`, `gas_storage`, `lng_terminal`, `compressor_station`) → `gas_cc, gas_ct, gas_steam, gas_ice, gas_other, hydrogen, fuel_cell`; fuels (`ethanol_plant`, `biodiesel_plant`, `rng_project`) → `biomass, waste, gas_other, hydrogen, fuel_cell`; `refinery` → the gas family plus `oil`. **Standalone `storage` is deliberately excluded and commented as the line most likely to be wrong** — 3,218 of 10,409 proposals on the current load, the biggest bucket, and standalone storage is usually not a midstream operator's business although co-located storage is, a distinction the `storage` token does not carry. `power_plant`, `transmission_line` and `substation` are listed as deliberately unfiltered with a reason each (a transmission owner's business is every generator that wants to interconnect), and holding any unmapped type switches the whole company back to the full list, so the failure mode is always "too much shown". A test fails if an ADR 0008 asset type appears in neither list, and another pins every technology token against the API's own vocabulary. (3) The page applies it by default and states it at body size above the list — "Showing 18 of 67 nearby proposals, narrowed to gas-related technologies because this company operates gas infrastructure" — with a "Show all 67" link beside it and a `<select>` for picking technologies by hand, all through a plain GET form on `?nearby_technology=` with no JavaScript, the same idiom `/proposals` uses. An unrecognised token in the URL degrades to the unfiltered list, never to a 400 or an empty page. **Measured on the 2026-09-20 load for `tallgrass-energy` (group, 10 assets considered): 67 nearby proposals unfiltered — 28 solar, 12 gas_ct, 9 storage, 6 wind, 3 gas_cc, 3 gas_ice, 3 oil, 2 hydro, 1 nuclear — and 18 after the default filter.** Also fixed on the way: `GET /v1/organizations/{public_id}/proposals` accepted `technology` and `jurisdiction` in `check_allowed` but applied neither, so `?technology=solar` returned the unfiltered list and no error; both now filter, with a regression test. **Reported, not acted on:** `nearest_asset.asset_type` is not a usable relevance signal — 3,028 of the 3,072 organisations with assets hold exactly one asset type, so it is constant across a company's whole nearby list (all 67 of Tallgrass's nearest assets are `gas_pipeline`), and the 44 that hold more are the mixed holders the mapping leaves unfiltered anyway. Separately, the register carries **no `load` proposals at all** (kinds are generation 6,692, storage 3,237, other 277, transmission 203), so the data-centre and large-load rows that would be the strongest gas-demand signal for a midstream reader are a coverage gap, not a filter-design question.
- 2026-09-21 legal-compliance (GEM-ownership-terms lane): terms review of GEM's **Global Energy Ownership Tracker** — the private-equity/fund ownership edge GLEIF structurally misses (GEM's own page: only 2,133 of 27,980 entities, 7.6%, are publicly listed). **Nothing was fetched, downloaded or vendored, and no email address was submitted anywhere.** Finding: **the dataset's page carries no licence statement at all.** `https://globalenergymonitor.org/projects/global-energy-ownership-tracker` (retrieved 2026-09-20, HTTP 200) gives only a recommended citation and a site footer "© 2026 Global Energy Monitor All Rights Reserved"; a case-insensitive search of its HTML for `creative commons` / `cc by` / `licen[cs]e` / `non-commercial` / `share-alike` returns nothing. CC BY 4.0 is named for this dataset in exactly one place — the download gate. GEM's download component (`https://api.globalenergymonitor.org/static/gem-download-form.bundle.js`, retrieved 2026-09-20) renders, for any element with a download surface and so for `slugs="energy-ownership-tracker"`, a required checkbox `I have reviewed and understand Global Energy Monitor's Creative Commons Public License`, defaults `_licenseUrl` to `/creative-commons-license`, records `license_text` as `"Creative Commons Attribution 4.0 International (CC BY 4.0)"`, and refuses submission if the box is unchecked. That linked page is the verbatim CC BY 4.0 text and scopes itself to no dataset ("Licensed Material means … material to which the Licensor applied this Public License"). Verdict: **commercial use is permitted and there is no share-alike** — CC BY §3(1)(b) only requires an Adapter's Licence not to prevent compliance, and §4(a) expressly grants extraction and re-use of a substantial portion of the database contents, so this is the opposite of the ODbL problem recorded at `13` §2.10 — but the grant is evidenced by a component default rather than by a sentence GEM wrote about this dataset, so it is classified **`attribution-restricted` / `derived_only`**, not `open-attribution` / `raw_ok`. **No TransitionZero-style row carve-out found**: the Solar Power Tracker, which carries the TZ notice, is not one of the nine trackers this dataset covers. **One separate-terms problem found, at column level not row level:** the entity identifier set includes **S&P Capital IQ (SPCIQ)** ids that GEM describes as found in "your S&P IQ account"; CC BY licenses only rights the Licensor "has authority to license", so the SPCIQ column must be dropped at ingest by the same mechanism as the TZ rows — as must every `natural person` row (`13` §5.4). PermID's own terms are unread and left open. **What I could not read, stated as such:** the in-file attribution notice sits behind a registration wall (the component POSTs name/email/organisation/sector/country to a Supabase `mint_submission` RPC, then presigns the file); passing it needs an email address and a licence acknowledgement on the company's behalf, which is an owner action — `13` §7 item 14. `web.archive.org` is unreachable from this container, so no earlier capture was consulted. **Collateral finding, flagged not fixed:** `13` §2.2's "the same Download Data boilerplate appears on each tracker page" no longer holds — every `/download-data/` URL now 301s to its project page, and of thirteen tracker pages retrieved 2026-09-20 **only the solar page still carries the CC BY sentence**, including the Integrated Power Tracker page that `global.gem.trackers` points at. Not reclassified (the 2026-09-12 quote was genuinely retrieved and a redesign does not withdraw a grant), but its `raw-ok` evidence is weaker than the §6 row implies and the same in-file check settles both. Fit, assessed separately from terms: scope is fossil and heavy industry only — coal plants, oil & gas plants, coal mines, iron & steel, bioenergy, iron ore mines, gas transmission pipelines, oil/NGL transmission pipelines, cement & concrete — so **no solar, wind, hydro, geothermal, nuclear or LNG terminals and no queue-stage proposals**, but Tallgrass-class midstream gas pipelines are in scope; the data is BODS-structured with LEI, EIA-860 owner ids and national registry numbers on part of the entity set (identifier joins to `global.gleif.lei` and `us.eia.860m`, not name-only), while the private and state entities we actually want are the ones least likely to carry an id, so `org_key` would be doing the work exactly where it is weakest; and GEM's "parent" is an opinionated rollup (lowest-level listed company, index funds demoted, SASAC exception) that must not be used as our parent edge — use the subject/interested-party relationships. New source `global.gem.ownership_tracker` in `data/sources.yaml` (tier 3, `reuse: attribution`, `publication: derived_only`, not ingested), `docs/13-legal-data-rights.md` §2.18 with a §6 matrix row and §7 item 14, `docs/02-data-sources.md` §4. `scripts/check_manifest_licences.py`: PASS, 80 sources; `tests/test_manifest_licences.py`: 17 passed.
- 2026-09-21 backend-developer (ownership-tree lane): the ownership walk is recursive in both directions and every ownership claim is dated at the point it is rendered (owner, 2026-09-20: "Seeing everything -> all blackrock -> Tallgrass -> trailblazer as well as proposals from them and those they may be interested in" — Blackstone, not BlackRock; Blackstone Infrastructure Partners took control of Tallgrass in 2019 and bought a further ~30% from Enagás in 2024, and the correct name is used in code, data and copy). **(1) `services/api/orgtree.py`** replaces the one-level `_subsidiary_ids` / `_org_scope_ids` / `_include_subsidiaries_param` block in `services/api/assets.py`: `org_scope()` descends the whole tree, `org_ancestors()` climbs to the root, both with a visited set and both capped at `MAX_DEPTH = 10` levels and `MAX_SCOPE_ORGS = 500` organisations. **Every cap is visible, never silent** — `scope.depth_capped`, `.truncated` and `.cycle_detected` ride on every scoped response and the page prints them in words. The cycle guard is not hypothetical: two loaders write `parent_org_id` and neither sees the other's rows; tested against a two-node cycle, a self-parent row and a rejoining diamond, each of which hangs the request without it. **Cap justified on the data**: the deepest real chain is 3 levels below its root, so 10 is three times the observed depth and still bounds a request at ten queries. **(2) `scope=self|children|all` is the new parameter and `include_subsidiaries` keeps the meaning it was published with** (`true` = one level = `scope=children`, `false` = `self`), deprecated in `api/openapi.yaml`; sending both is a 400. Widening a live parameter's meaning would have changed what an existing caller's numbers mean with nothing in the response saying so — a test pins that the legacy spelling still stops at one level and does **not** reach a grandchild. `GET /v1/organizations/{id}/proposals` gained the same `scope` (it filtered `sponsor_org_id` single-org, so a holding company's proposals page was an empty page that was technically correct), as did `GET /v1/assets/geo?organization=`. **(3) Cost measured, not assumed.** The descent is breadth-first *by level*: one `WHERE parent_org_id IN (level ids)` per level, so **query count scales with depth, not node count** — counted with a `before_cursor_execute` hook on the 2026-09-20 load (7,412 organisations, 298 parent links): `scope=self` 0 queries, `children` 1, `all` on `tallgrass-energy` 2, a synthetic 500-wide fan-out 1, a synthetic 12-deep chain 11. The consuming query is unchanged, still one `IN` over the id list, which is why `MAX_SCOPE_ORGS` exists. A **recursive CTE was benchmarked and rejected**: median of 25 runs on SQLite, `WITH RECURSIVE … UNION`, BFS vs CTE — Tallgrass 2.4 vs 6.4 ms, THE SOUTHERN COMPANY (15 orgs, 3 levels) 3.6 vs 6.0, RWE (19 orgs) 2.4 vs 6.3, synthetic 500-wide + 12-deep 2.4 vs 10.3 — slower at every size in the data, and it can express neither the depth cap nor which bound stopped it. Re-measure on Postgres at the first deploy, where round trips cost more than SQLite's in-process call. **(4) Dated provenance on every edge.** `parent_edge` (the direct link with its `source_id`, `as_of`, `share_pct`) and `ancestors` (root-first, for breadcrumbs) are new on `GET /v1/organizations/{id}`; the NULLs are **kept, not omitted**, and the page prints "Ownership link recorded from curated.organization_parents; no date stated by the source" rather than letting an undated claim read as current — the Lincoln Land Energy Center failure. Measured: 289 of 298 loaded links are dated (GLEIF Level 2, from the relationship period start), 9 are not (the curated file cites when a page was read). `web/ownership.py::ancestor_claims` shifts each date one crumb down on purpose, because an `ancestors[i]` entry names a *parent* and carries the date of the link reaching it from below, so rendering it where it arrives labels the wrong company. **(5) Migration 0017 adds `organization.parent_share_pct`** (`Numeric(6,3)`, nullable, mirroring `asset_owner.share_pct`): **NULL on every row and nothing infers a value** — neither loaded source states a percentage — so the percentage-stake chains GEM's ownership dataset models do not require the edge to be rebuilt if its licence clears. The expensive half (a stake belonging to a *set* of parents) is deliberately not taken: no loaded source has produced a second parent for any organisation. **(6) A fund-level page is not a company page.** `totals.by_organization` (the portfolio: one row per organisation in scope holding assets, largest first, cap 100) rides on the existing totals query at no extra cost, and `web/ownership.py::group_view` decides the layout from the API's own counts — a group drops the map above **40** assets (`ORG_MAP_DETAIL_CAP`, the number of geometries the page can fetch: above it the map was already drawing whichever 40 came first in `asset_owner.id` order) and drops the flat asset table above **100** (the page's single `limit=100` call), printing the reason both times. Both are limits the page already had, now stated instead of silently hit; **single-company pages are untouched**, because the eight scopes in the current data above 40 assets are all single companies with no subsidiaries (largest: WM Renewable Energy, 102 landfill-gas assets) and the dot scatter the owner described is a fund problem. **(7) Drill-down is URLs.** `?scope=` on the company page (page default `all`, API default `self` — a page choosing a wider view for readers is not the same as changing an unparameterised API call), three plain scope links, ancestor breadcrumbs, a hidden `scope` field on the technology GET form because a browser replaces the whole query string on submit, and `nearby_notice` gained a `keep=` argument so the "Show all N" link stays at the reader's level of the tree. No JavaScript anywhere in it. **Measured for `tallgrass-energy` at each level** (2026-09-20 load): `scope=self` — 0 assets, 0 nearby, 1 organisation (the holding company holds no `asset_owner` edge of its own, which is the whole reason the scoped read exists); `scope=children` and `scope=all` — 10 assets across 9 subsidiaries, 10 organisations, depth 1, **67 nearby proposals unfiltered and 18 after the PR #8 relevance default**, identical to the figure that lane measured, so the filter is intact at group scope. Down a level: `trailblazer-pipeline-co` 1 asset, 12 nearby → 8 filtered, ancestor `Tallgrass Energy` (curated, **no date**); `rockies-express-pipeline` 1 asset, 33 nearby → 11 filtered, same ancestor. `children` and `all` are identical for Tallgrass because its tree is one level deep — 130 of the 137 rooted trees in the data are (5 are two levels, 2 are three; 9 organisations have a grandchild, and for those the old `group_asset_counts` was simply wrong — THE SOUTHERN COMPANY reported its 2 direct subsidiaries' assets, not the 18 its group holds). **Honestly: the multi-level behaviour is proven on fixtures, not on the register.** Every test of depth beyond two levels, of either cap, of a cycle, of a large portfolio and of an undated-vs-dated pair builds its own organisations; the deepest thing the loaded data can exercise is 3 levels (THE SOUTHERN COMPANY) and nothing in it reaches a rendering threshold. Both new test modules say so in their docstrings. **Not done and deliberate:** no ownership edges were invented — Blackstone→Tallgrass is not in the data, so Tallgrass renders as the root it is and the fund layer stays empty until a source lands. 30 new API tests (`services/api/test_orgtree.py`), 24 new web tests (`web/test_ownership.py`); `services/api/visibility.py` has zero changes.
- 2026-09-21 data-scientist (schedule-slippage lane): a **slippage signal** — a proposal whose own stated `proposed_online_date` has passed while it is still nominally active — derived at read time, not stored, not a lifecycle state, no migration. **The premise was checked before anything was built and half of it was wrong in a useful way.** Splitting `withdrawn` into announced vs. inferred, GEM-style, has nothing to split: all **3,215** withdrawn rows carry a literal `WITHDRAWN` (1,762) or `Withdrawn` (1,453) in `status_raw` from the ISO queue, so every one is announced. The other direction reproduces exactly: **469** active proposals are past their stated date (`under_construction` 316, `permitted` 47, `studied` 36, `filed` 36, `contracted` 34), **8,180** carry a date at all, and `last_changed` is useless for staleness — every one of the 10,409 rows has `first_seen` and `last_changed` inside a seven-second window on load day, and the `event` table holds **0** rows, so there is no observed-progress history to infer from. Only the ">3 years" figure differs from the brief: **14**, not 12, on a strict 2023-09-21 cut. **The measurement that set the design: a bare "date has passed" test is about two-thirds artefact.** All 2,341 `us.eia.860m` dates and all 201 NYISO dates are first-of-month, because the source field is a (`Planned Operation Month`, `Planned Operation Year`) pair — **182** of the 469 carry `2026-09-01`, the current month, which cannot be late — and a further **125** carry `2026-08-01`, a month the evidence itself predates, since the loaded file is `july_generator2026.xlsx` retrieved 2026-09-13. **111** of the 316 flagged `under_construction` rows are EIA status `(TS) Construction complete, but not yet in commercial operation`: reporting lag, not slippage. Flagged count by grace period, with the EIA-860M share of each — 0 d 469 (308 EIA), 31 d 283 (129), 60 d 146 (5), **90 d 129 (0)**, 120 d 121 (0), 365 d 62 (0) — so **90 days** is where the curve flattens, it is longer than any calendar month and longer than EIA's publication lag, and at 90 days **none of the 129 survivors come from EIA-860M at all** (79 NESO, 33 CAISO, 16 ERCOT, 1 NYISO) and `under_construction` falls from 316 flagged to **9 of 1,038**. Honest split: ~65 % of the naive 469 is source-date artefact, the 129 are defensible, and the **62 beyond a year / 14 beyond three years** (dates in 2012, 2016, 2020, 2021) are as close to certain as one snapshot allows. **Computed at read time** because a stored `is_overdue` is wrong the day after it is written — the record does not change, the calendar does — and there is no daily recompute job to keep one true; the predicate is a sargable range comparison over 129 of 10,409 rows, so a column buys nothing. Two twins, `slip_days()` in Python and `slip_filter()` in SQL, pinned equal by a test, the same idiom `visibility.location_exact_permitted()` already uses. One trap recorded: `NOT (NULL < date)` is NULL, so a naive negation for `slipped=false` would silently drop all **2,229** undated proposals out of the complement; the clause leads with `is_not(None)` and a test holds it. **API: both a boolean and buckets, from the distribution not from taste** — 129 of 5,826 dated active proposals (2.2 %) makes `?slipped=true` worth having, but the 129 split **67 / 48 / 14** and a NESO "Consents Approved" eight months late is not the same prospect as one three years late, so `?slip_bucket=under_1y,1_to_3y,over_3y` (91–365 / 366–1,095 / 1,096+ days) exists too; unknown tokens are a 400 naming the token, `slipped=false` with a bucket list is a 400 naming the conflict rather than a 200 with an empty page, and `GET /v1/meta/vocabularies` publishes the day boundaries and `grace_days` so a caller never guesses where our line is. `Proposal.schedule_slip` is `{target_date, days_late, bucket, grace_days}` or **`null` — never a zeroed object**, so "nothing promised" cannot read as "on schedule". **Rendering is graded so it does not cry wolf**: the grace already keeps the badge off the ordinary EIA construction population, and beyond that `under_1y` renders as plain outlined text with no colour while `1_to_3y`/`over_3y` take the warning colour; deliberately not a status chip, because the chip states what the source reports and this states an inference of ours. It is on the list and the detail page and deliberately **not** on the map: the map's feature payload is column-narrowed on purpose (`_GEO_PROPOSAL_COLUMNS`), `proposed_online_date` is not in it, and a per-row marker is unreadable at cluster scale — though the filter does work on `/v1/proposals/geo`, so a caller can still draw a map of only the slipped ones. The target date prints beside the marker every time and the detail page says the verdict is derived at read time and that a register which stops maintaining the date would look exactly like this. **Answer to the framing question, recorded in docs/22 §18.8 as A-22-14:** the target date is *better-grounded* evidence than GEM's absence-of-news (a dated claim by the party who would know, not our failure to find something) and *narrower in reach* (it can only speak about the 8,180 records carrying a date, and it inherits each register's date-maintenance discipline). Also measured and recorded rather than guessed at: the *structural* analogue of GEM's inference is already in the schema and empty only for want of a second pull — 10,409 `proposal_source` rows, **0** with `gone_at` set, 0 inactive, 0 snapshots, 0 events, all `first_seen` on one day — and a disappearance is evidence about the register where a missed date is evidence about the project, so the two are complements, not alternatives. The obvious second signal is **not** better and is not built: `Queue Date` is present on 1,673 of 5,837 active proposals (28.7 %) and only from the three US ISO queues, against 99.8 % coverage for `proposed_online_date`, though its distribution (283 queue positions over six years old) is worth returning to. `scripts/measure_slippage.py` reproduces every number above from a loaded store and writes the grace sweep to `data/eval/slippage_distribution.csv`, because 90 days is a judgement backed by one curve and that curve has to be re-runnable. `services/api/slippage.py` new; `api/openapi.yaml` documents `slipped`, `slip_bucket` and the `ScheduleSlip` schema on `/v1/proposals`, `/v1/proposals/geo` and `/feeds/proposals.{format}`; 42 new API tests (`services/api/test_slippage.py`) and 13 web tests (`web/test_slippage_view.py`), covering the computation, both twins, the unknown-value 400, a date exactly today, and a proposal with no date being excluded rather than treated as overdue. `services/api/visibility.py` has zero changes.
- 2026-09-21 data-engineer (ownership-edge lane): the edge above Tallgrass, curated, cited and **dated**. `data/vendored/organizations/parents.yaml` gains one rule — `Blackstone Infrastructure Partners` over `^tallgrass energy(,? ?l\.?p\.?)?$`, `as_of: 2019-03-11` — read live from SEC EDGAR: Tallgrass Energy, LP (CIK 1633651) 8-K filed 2019-03-11 (Explanatory Note: "acquisition vehicles controlled by affiliates of Blackstone Infrastructure Partners … acquired … 100% of the membership interests in Tallgrass Energy GP, LLC … for aggregate consideration of $3,213,161,149 in cash"; Item 5.01: "Prairie GP Acquiror LLC … has the ability to control TGE's management and operations"), corroborated by the 2019-01-31 announcement 8-K (~44% economic interest, ~$3.3bn), the 2019-12-17 8-K exhibit (merger at $22.45/Class A share by BIP with Enagás, GIC, NPS and USS), the 2020-04-17 completion 8-K (merger with Prairie Private Acquiror LP) and Enagás's own 2024-07-10 release (its 30.2% sold to BIP for $1.1bn). blackstone.com serves a Cloudflare challenge to any client including on `/robots.txt`, so it was not fetched; the filings are the companies' own statements anyway. **Entity naming decided on measurement**: "Blackstone Infrastructure Partners" keys to `BLACKSTONE INFRASTRUCTURE PARTNER` under `pipeline/normalize.py::org_key` and collides with **nothing** in the 7,412-organisation load and **nothing** among GLEIF's 183,553 distinct legal names, whereas the plain "Blackstone" the request used keys to `BLACKSTONE` and would merge the fund into GLEIF's `BLACKSTONE INC.` node the day the Level 2 loader runs. No source read states an entity literally named "Blackstone Infrastructure Partners L.P.", so the rule carries the fund name the sources state and the registered Prairie vehicles and the 13D chain above them are recorded in the note rather than guessed at. **`parent_share_pct` stays NULL**: the ~43.7% at closing is what the Sponsor Entities held *collectively* (Enagás and GIC holding indirect LP interests in the same vehicles), the 100% is of the GP (control, not an economic share of TGE), the post-2020 consortium split is unstated and Enagás's 30.2% is the seller's stake, not the buyer's resulting total — four figures, none of them the one the column asks for. **Schema**: the curated file gains an optional `as_of` (`services/ingest/midstream.py`), written to the existing `organization.parent_as_of` (migration 0015 — no migration needed), never borrowed from `retrieved_at`, and included in the unchanged-comparison so re-runs stay no-ops; a malformed `as_of` raises during parse, before the session is touched. Deliberately **no** `share_pct` key until a rule can defend one. **Graph effect measured** on a copy of the 2026-09-21 load: 7,412 → 7,413 organisations, 298 → 299 parent links, 289 → 290 dated, 137 rooted trees unchanged (Tallgrass stops being a root as Blackstone becomes one), the Tallgrass tree 1 → 2 levels deep, `rules_without_match` empty and exactly one organisation matched — the owner's `Blackstone Infrastructure Partners → Tallgrass Energy (as_of 2019-03-11) → Trailblazer Pipeline Co` now resolves; re-run linked 0 / unchanged 10. **Found, not fixed** (out of this lane): `Trailblazer Infrastructure LLC`, an ERCOT queue sponsor, is a second unlinked Trailblazer organisation; `services/api/orgtree.py`'s docstring still says no Blackstone edge exists; `data/sources.yaml`'s `curated.organization_parents` `format:` field no longer lists every key. 5 new tests (`services/ingest/test_midstream.py`), 1,925 passing.
- 2026-09-21 data-engineer (coverage-and-vintage lane): **the product now says what its data is and is not.** (1) *Release versus fetch date.* The footer printed one date, "sources last fetched", and readers took it for the data's age; on the load it was measured against, the EIA-860M workbook was `july_generator2026.xlsx` — EIA's **July 2026** report — carrying a `retrieved_at` of 2026-09-13, and the Energy Atlas gas-processing layer is **2017** data fetched 2026-09-19. `services/ingest/vintage.py` resolves a release only from something the source published (the artefact filename, the Atlas shapefile member token) and has **no code path from a fetch date**, pinned by a test that fails if a fifth `from_*` extractor appears. Migration **0018** adds `source.vintage` (normalised `YYYY-MM`/`YYYY`, so `MIN()` answers "oldest loaded release" with no date parsing) and `source.vintage_basis` under a CHECK, and backfills from evidence already stored so an existing load renders without a re-ingest — measured on the 2026-09-13/19 dev load: **5 of 16 loaded sources state a release** (860M 2026-07; Atlas pipelines 2020-01, processing 2017, storage 2020-12, LNG 2020-04), **11 state none** and 5 are undetermined. *A source stating no release is written `not_stated` and rendered as "states none", never given the fetch date.* **Decision, argued rather than assumed:** the column sits on `source` (21 rows), not on `proposal_source`/`opportunity_source` (10,409 + 707) — `MIN(retrieved_at) == MAX(retrieved_at)` for every loaded source, so a per-link column would store ~11,000 copies of eight values, and the divergence it would capture is already expressible with `retrieved_at` + `gone_at`; it becomes meaningful the day an incremental loader lands and can be added then. Rendered in the footer beside the fetch date, per source on `/methodology` and `/attribution` (release column added, credit lines untouched), and on `GET /v1/health` (`source_vintage`) and `GET /v1/sources` (`vintage`). (2) *The status words are defined.* `data/vocabulary/lifecycle_states.yaml` carries the prose (definition, what the word excludes, where the mapping is a judgement) with the date it was written; `services/api/lifecycle.py` derives **what maps into each state from the 8 status-map files the pipeline itself keys off** at request time — 53 proposal-side and 45 opportunity-side mappings, shown verbatim, with `spp`/`isone` marked `rows_published: false` rather than hidden. `tests/test_lifecycle_definitions.py` fails when a state is in one and not the other, when a map emits a state the database would reject, and when the text scan of the connectors' `status_key`/`kind` disagrees with the real classes (verified to fail by adding a state). The four-state pre-construction split (`filed, studied, permitted, contracted`, **4,111 of 10,409 proposals**) is stated where trackers built from announcements publish one bucket. (3) *What is not included, derived.* `services/api/coverage.py` measures it: **86 sources registered, 16 with rows**; `load` is the **only** absent token of the 26 the normaliser can emit, so "no large-load or data-centre proposals" is a subtraction, not a sentence; **19 withheld registers, 8 of them supply** (PJM, MISO, SPP, ISO-NE, AESO, IESO, EirGrid, non-ISO OASIS) with the registry's own reason; **298 of 7,412 organisations have a recorded parent**; `unknown`/`cancelled` and `frozen`/`reinstated` have no rows. Only the *why* is prose (`data/vocabulary/coverage_notes.yaml`), each note dated and **dropped automatically when its derived fact stops being true**. **Placement decided against the "do not turn the product into a disclaimer" test:** one `/methodology` page carries the whole statement and is linked from every footer; in-place notes appear only in the three places an absence is actually misread — a company with no parent edge ("7,114 of 7,412 have none"), an empty proposal list (naming the absent technology filtered for and the withheld registers), and the footer's two dates. Nothing is printed beside a result that already has rows. New: `GET /v1/coverage`, `GET /v1/lifecycle-states` (spec valid at 147 operations, 44/44 stories, both contract-tested), `/methodology`. `services/api/visibility.py` has zero changes and no licence gate was touched; the coverage counts are deliberately tier-independent because "what is missing" is not a row. **Found on the way:** `source.last_success_at` is NULL for everything loaded through the file path, so the page falls back to the newest `retrieved_at` on the source's rows for its "we last fetched" column; `services/ingest/loader.py` is *not* the raw-status mapping (it passes `lifecycle_state` through) — `pipeline/status_map.yaml` plus the seven connector-local maps are; `starlette` keeps app state in `_state`, so the `app.state.__dict__.pop(...)` idiom in several `web/test_*.py` files is a no-op. **67 new core tests** (28 vocabulary-drift, 19 vintage, 16 coverage/API, 2 migration round-trip, 2 contract) and 22 new web tests; 2,065 core passing with 4 skipped, 348 web.

- 2026-09-21 backend-developer (ISO change-event delay removed): the last time-based tier is gone, and so is the knob that could have restored it (owner, 2026-09-21, answering the 2026-09-20 open question: option (a), "Let's go with A and build a new commercial function later"). **Removed, not zeroed:** `change_event_lag_days` from all eight `us.iso.*` entries in `data/sources.yaml` and from `SourceEntry`; `ISO_CHANGE_EVENT_LAG_DAYS`, `change_event_lag_days()` and `change_event_public_at()` from `services/ingest/lag.py`, whose docstring now states one rule for every row — the paywall is by shape (alerts, exports, API, watchlists are paid), nothing is time-delayed on any tier, `public_at == published_at` for records **and** events, `RECORD_LAG_DAYS = 0` and `record_public_at` is the identity; `source.lag_days` and `source.lag_overrides` by **migration `0019`**, which first sets `event.public_at = published_at` wherever `published_at` is not NULL so the events already withheld by 0016 are actually released (a taken-down row keeps its NULL), and whose `downgrade` re-adds both columns nullable and says in its docstring that it cannot restore a single value, because the manifest, the policy module and the admin surface lost them in the same change; the two fields from `PATCH /admin/v1/sources/{id}` and its `.md`, from `services/db/models.py`, from `services/ingest/loader.py::upsert_licence_and_source` and from `api/openapi.yaml` (3.1 union spellings throughout; `meta.lag_days` stays, documented as always `0`, and `HealthResponse.lag_days_default` loses its `iso_change_events` key). **Why the knob went too**, since that was the design call rather than the decision: a dormant per-source lag an admin `PATCH` could flip back on re-creates exactly the defeatable promise being removed and leaves a public page one audited write from claiming a delay the product does not apply — the argument `lag.py` already made for the record-level lag ("there is no knob"). **`services/api/visibility.py` has zero changes**, which is again the proof that no licence gate moved: `restricted`/`unknown` sources, PJM included, stay invisible on every non-admin surface, and its 100%-branch gate is untouched. Downstream, all consequences of the removal rather than new policy: the events list and detail report `lag_days: 0` on every tier (`services/api/app.py::_event_lag_days` deleted), the public feed title reads "Public feed, live — alerts and API in Pro", `services/social/__main__.py::_lag_days_for` returns `None` for every row so no draft can carry a lag clause, and the pricing page's delay copy, the masthead/footer/about clauses and the admin source-lag form field are gone rather than hidden behind a condition. `tests/test_iso_change_event_lag.py` inverted and renamed **`tests/test_publication_is_never_time_delayed.py`**: it writes the eight ids out explicitly and asserts none declares a lag, that all eight are still in the manifest, that no `lag`-shaped key exists on any entry, that `SourceEntry` and `source` have no lag field or column, that `lag.py` exposes only `record_public_at` (the uncalled `record_lag_days()` accessor went with the delay), and that a record and an ISO change event both load with `public_at == published_at`; `tests/test_licence_gate_survives_lag_removal.py` renamed `..._survives_removing_the_delay.py`; `tests/test_free_tier_change_reconstruction.py` kept as the evidence for the decision with its assertions flipped to "not withheld from anyone"; new `tests/test_migration_0019.py` round-trips the migration on SQLite. Docs amended so no document describes a time-based tier: `docs/00-PLAN.md` (decisions-log row, 2026-09-20 question marked answered), `docs/04` D-3/D-26/D-28/E-21 and conflict 1, `docs/10` (Lag glossary, US-101 AC3, US-201 AC4, US-503, US-601 AC1/AC2, US-604, US-904, A-7), `docs/11` §3, `docs/20` §5 and A-8, `docs/21` §3.19/§4.1/§5.4/§8/D-1/C-4 and the ERD, `docs/23` §1/§2/§3.1/§3.2/§9.2/§10/P-2, `docs/30-design-ia`, `docs/31` §5.4, `docs/32` §1.2/§3.2/§4.3, `docs/41`, `api/README.md`, `services/README.md`, `services/social/README.md`, and on coordinator review `docs/23` §6 (the `read:public` scope row still read "Delayed tier only"), `docs/03` pipeline step 6, `docs/02` §6 and `web/README.md` — whose §"Delayed tier, for real" still described fresh rows as invisible for 14/7 days and whose **scope line** still read "public delayed tier only — no auth, no Pro, no admin" while `web/auth.py`, `web/pricing.py` and `web/admin/` all exist; the scope line is rewritten against what the app actually mounts (public site, accounts, pricing and the billing hand-off, admin panel), "no Pro" is kept only in its narrow true sense (no Pro *product* surface in `web/`; saved searches and alerts live in `services/api/pro.py` and `services/alerts/`), and the spent Sprint-2 task sentence is struck through. `docs/00-PLAN.md` §Phases and `docs/01` §7 keep their "delayed tier" wording: both are dated records of what was planned before the decision, and the decisions log supersedes them in place. `services/social/editorial.py` records why the five `if event.lag_days` clauses are left dormant rather than deleted — descriptive and keyed on a runtime value, so inert while it is zero — against the pricing page's comparative "live events, unlike Free" flag, which was removed because it would be false the moment it rendered.

- 2026-09-22 data-scientist (asset-identity lane): **measured the asset-layer duplication and wrote the options memo — `docs/24-asset-identity-and-provenance.md`; no code, no migration, nothing shipped.** Measured read-only against `web/.data/dev.db`. (1) *Ethanol is worse than reported.* 388 `ethanol_plant` rows from two EIA sources resolve to **201 distinct plants — 187 cross-source links, 48.2% of rows redundant** (coordinator's estimate was 158 pairs / 41% / 227 plants). Method: blocking on state, scored on company-token similarity + parsed city + nameplate ratio, global-greedy assignment, then hand-adjudicated: **precision 183/187 = 97.9%, recall 183/187 = 97.9%** against a 187-link gold set; pessimistic floor 46% if all six judgement-call links are wrong. The named cause of the under-count is that name-normalisation systematically misses exactly the ownership-change pairs (Flint Hills→Poet, ADM→VCP, Green Plains→Sandhills) that are the reason to fuse the two sources. Nebraska alone: 49 rows, 29 plants — a 69% over-count. (2) *It is ethanol-only.* Five of the seven asset types have exactly one contributing source, so cross-source duplication is currently impossible for them; `power_plant` (14,659 rows, `us.eia.860m`) has **zero** — 14,659 distinct EIA plant codes, 14,657 distinct (name, state), and its 466 shared-coordinate rows are distinct registration phases at one site, not duplicates. `rng_project`'s two sources are disjoint populations (**0 of 498** AgSTAR rows match an LMOP row, with 220 of them in a county that has one), but LMOP disagrees with itself about row granularity: 1,353 rows → 778 landfill-level sites, **575 rows (42.5%) an expansion vintage of a site already present**, up to nine rows at one landfill — a normalisation question ADR 0008 does not settle, not a provenance one. Midstream types: 1.2–4.4% upper bound. (3) *Recommendation: option (a), `asset_source` mirroring `proposal_source`, but staged* — ship (d) coverage disclosure now, land the table with a one-to-one backfill before running any resolution. Corrections to the brief: `proposal` carries **no** `source_id`/`source_url`/`retrieved_at`/`licence_id` at all (the quartet lives on `proposal_source` with `min_reuse_class` as the gate), so multi-source provenance is already solved in this schema and merging need not destroy it; `_has_permitted_source` is a branch-free `exists(...)` already inside the 100% gate, so option (a)'s risk to `services/api/visibility.py` is semantic (the predicate widens to "some source is permitted") rather than a coverage problem; `resolution_decision`'s two FKs are `NOT NULL` to `proposal`, so it cannot hold an asset pair and must be widened or `docs/02` §5's reversibility is unmet; and `web/.data/dev.db` predates migration 0018 (no `alembic_version` table, no `source.vintage`), so no vintage claim is measurable from it. (4) *Safe without a schema change:* the two sources disagree on operator name on **40 of 183 confirmed pairs (21.9%)** and on nameplate by >5% on **79 (43.2%)**, but `asset_owner` **already holds both views** — 197 Atlas + 191 capacity-report operator edges, all 388 assets covered, each correctly attributed — so the fix is to stop reading the denormalised `asset.operator_name`; the blocker is that `asset_owner.as_of` is NULL on all 388 edges (3,217 of 6,336 overall), which is a loader change, not a migration. All 388 ethanol rows say `status='operating'`, including plants that never operated. (5) *Geocoding, assessed not built:* all 191 capacity-report names parse to a real city, and a US Census Places gazetteer would be about a day's work — but **187 of those 191 rows already have an exact coordinate from their Atlas twin**, so resolution delivers ~98% of the benefit at `exact` precision, and `asset` has no `location_id`, no `precision` and no `precision_reason`, so a city centroid written to `asset.geom` would be indistinguishable at every API boundary from an exact EIA point. Geocoding under the current schema cannot satisfy `docs/04` D-9; recommended not to do it until `asset` can record what a coordinate is. Four plants (incl. Green Plains York, NE, in the Trailblazer corridor) exist only in the capacity report and so have no coordinate at all — a second, independent cause of the wrong Trailblazer answer that de-duplication alone would not have fixed. docs/24, docs/02 §5, docs/04 D-9 and E-7, docs/21 §3.22–§3.23, ADR 0008.
- 2026-09-22 data-engineer (CCS lane): **EPA Class VI is built, and the route is not the PDF.** *Probe first (the brief's step 1).* The page (`https://www.epa.gov/uic/current-class-vi-projects-under-review-epa`, 200, 57,652 bytes, sha256 `846eb99b…d08cf`, footer "Last updated on May 26, 2026") offers exactly one download, `permit-tracker_5-22-26.pdf` (353,074 bytes, sha256 `b969e5bb…b8f3d`) — and it is a **no**: one page, zero tables, Producer "Adobe Acrobat … Paper Capture Plug-in" (OCR of a Gantt **image**), project labels truncated mid-word ("Carbon Storage Solutions, LLC: Front Range Storage .. ."), OCR corruption ("CTV Ill" for CTV III), and no per-project dates at all — only a shared date axis. It is recorded as evidence and never parsed. The dashboard **does** have a backing endpoint: it is a Qlik Sense app (appid `8c074297-7f9e-4217-82f0-fb05f54f28e7`) on EPA's EDAP, whose `/public/` virtual proxy answers **anonymously** (`OnAuthenticationInformation` → `mustAuthenticate: false`) over the Engine JSON API at `wss://awsedap.epa.gov/public/app/<appid>`. No credential, no login, no challenge bypass, no UA spoofing. `GetTablesAndKeys` names one table, `External`, 243 rows × 143 fields. *The connector* (`pipeline/connectors/us_epa_class_vi/`, `kind: proposal`): GET the page through the polite session (epa.gov robots.txt disallows nothing under `/uic`), scrape the appid, then one session hypercube over 67 named fields, paged. Live run 2026-09-22: **528,803 bytes, sha256 `f0dfa1b6d3b7ce7ddd71907be9087da4cf5f51f21504afff03a4219530d92151`, 243 hypercube rows → 68 project rows, 7 requests**, lifecycle `studied 39 / withdrawn 13 / permitted 11 / filed 5`, every row via `epa_class_vi.map`, 0 duplicate `record_id`. **Grain is per project, not per well**: the 68 projects carry **235 permit applications** between them — that sum, not a project count, is where the old manifest note's "175+ applications" came from. `source_record_id` is EPA's own GSDT project id (47 of 68) else a content hash over (company, project, state, county); `RowOrder` is requested **only** to stop Qlik's distinct-combination straight table from collapsing the 175 blank padding rows, and a test pins that it is never an identity. **`classify_tech` is deliberately not called**: its `\bstorage\b` rule labels a CO₂ sequestration project as *battery* storage, so `kind` is the constant `ccs` and `technology` `co2_geologic_sequestration`, with the trap asserted explicitly in a test. **Personal data**: the app carries `Primary Permit Writer` (named EPA staff, 21 values) — outside `FIELDS`, so never requested, never snapshotted, never stored, and a test proves it. Two tribal-land rows (EPA's `State` reads `Osage Nation` / `All Other Indian Tribes`) normalise to a **null** state rather than a guessed one. 18 tests, fixture `tests/fixtures/epa_class_vi_tracker.json` (11 real project rows + 3 real blank rows, nothing edited); `fetch()` is driven in-test by a scripted page and a scripted engine socket. *Manifest* (`data/sources.yaml`, 86 → 94 entries): `us.epa.class_vi` rewritten (`access: api`, the real format, `rate_limit_rps: 0.5`, verified 2026-09-22 with the numbers above, and the correction that the app's own `Last Updated` field reads **2026-09-11** on every row — fresher than the page footer, so the footer is not the data date). **Six** primacy states registered, not five — the old note omitted **Arizona**: `us.az.adeq.class_vi` (Cloudflare challenge, 403 on the page *and* on robots.txt — reported as a block, not worked around), `us.la.dce.class_vi` (EPA's link is stale: dnr.louisiana.gov → dce.louisiana.gov; the real list is SONRIS, whose `robots.txt` is **`Disallow: /`** for every agent but `siteimprove` — **do not fetch**, and one GET reached it before its robots was read, recorded in the entry), `us.nd.dmr.class_vi` (200, 186,730 bytes, 67 document links across 7 named storage facilities + per-facility injected-CO₂ volume PDFs; a document library, not a register), `us.tx.rrc.class_vi` (**the best of the six**: `class-vi-application-list-31126.pdf`, 143,679 bytes, sha256 `004f62bb…628f82`, a real vector table — pdfplumber extracts 14 columns × 18 application rows over 16 operators, statuses Pending RAD Response 5 / On Hold by Operator 5 / Under Review 4 / Approved 1 / Withdrawn 1 / Hearing 1 / Comment Period 1; RRC took primacy 2025-12-15), `us.wv.dep.class_vi` (200 but **no Class VI project list exists at the URL EPA links** — a blank application form only; registered as a measured gap), `us.wy.deq.class_vi` (list is behind an Elementor JS popup; served on 2 of 3 attempts, the third a 307 "Javascript is required" interstitial). All six are `reuse: unknown` / `publication: none`: **17 U.S.C. §105 does not reach state agencies**, so the federal public-domain rule stops at the primacy boundary. Plus `us.epa.ghgrp` (Envirofacts REST `pub_dim_facility`: **136,005** facility-year rows, **11,281** for RY2023, with lat/lon, FRS id, parent company, `co2_captured` and `rr_mrv_plan_url` — the emitter↔storage join; bulk zip 28,389,973 bytes confirmed by HEAD) and `global.carbonstorage_io` (**do not ingest, do not scrape** — private paid aggregator, no licence anywhere on the site; its permissive robots.txt is not a licence). Register rows added to `docs/13` §6 for all eight so `scripts/check_manifest_licences.py` stays green (PASS, 88 sources). *Competitor* recorded in `docs/11` §1.3 with their pricing verbatim, both tiers "Custom", Enterprise gated at "teams of 8 or more" + API, the FOIA feature called out as the one thing in their stack that is not a public feed, and their "Founded 2024 / Countries 50+ / Team 5+ / Coverage Global" footer marked as **marketing copy, not measurement** — the one check available from outside is their own "Real-Time CCS Activity" feed, whose newest item is dated **2025-11-03**, ~10 months stale. `requirements.txt` gains `websocket-client>=1.8` (lazily imported inside `fetch`; parser tests need no network stack). **Found, not fixed** (coordinator's call): `global.carbonstorage_io` currently reports `state: excluded` only by accident — `SourceEntry.never_ingest` matches the word "Cleanview" inside my own note text; the durable fix is one line, adding the id to `NEVER_INGEST` in `pipeline/connectors/registry.py`. Also: `pdfplumber` is named in ADR 0002 and `docs/20` §15 and is installed in `.venv`, but is absent from `requirements.txt`. 1,411 core tests passing, ruff and `mypy --strict` clean.
- 2026-09-25 data-engineer (CCS lane, Texas): **`us.tx.rrc.class_vi` connector built and gated to quarantine** (`pipeline/connectors/us_tx_rrc_class_vi/`, `kind: proposal`, `ccs` / `co2_geologic_sequestration`, `classify_tech` bypassed and the battery trap asserted). *The brief's "current PDF" was stale:* on 2026-09-25 the RRC page (200, 66,533 bytes, sha256 `62d8c53c…ec9a5`; robots.txt 200, empty) carries **three** application-list anchors, two with empty link text — the visible one is `/media/3ivkqhtb/class_vi_application-09222026.pdf` (131,163 bytes, sha256 `f4ee42dd18ddc14b930b96b87681caeedd7e972d34ee8f241392e76a9a8ad9b2`, "Last Updated: 09/22/2026", **20 rows over 16 operators**: Under Review 4 / On Hold by Operator 4 / Pending RAD Response 4 / Approved 2 / Withdrawn 2 / Hearing 2 / Draft Permit 1 / Draft Doc 1), beside `class_vi_application-5122026.pdf` (178,010 bytes, 19 rows, 2026-05-12) and the March `class-vi-application-list-31126.pdf` (143,679 bytes, sha256 `004f62bb…628f82`, 18 rows) the 2026-09-22 probe had recorded as current. *Discovery* (`find_list_links` / `choose_current`): every `class[-_]vi[-_]application*.pdf` anchor is collected with its `title`, the latest dotted date in the title (else file name) wins, falling back to the anchor with visible text, then page order; all candidates, the rule, page/PDF bytes and sha256 go into the run record; a non-PDF answer fails closed. *Parse* (pdfplumber 0.11, now in `requirements.txt` as `pdfplumber>=0.11`): 14 columns asserted by count and by a tolerant per-cell header pattern (the header text itself changed between the March and May builds), `ParseError` on drift, repeated headers skipped, title/`Last Updated`/legend rows recognised as preamble in both layouts, multi-line cells re-joined with hyphenation closed up (`OG-25-\n00029632`), footnote markers stripped from the mapped status (`Draft Doc6`, `Pending RAD Response**`) and kept verbatim in `raw`. *Identity* is the RRC's own **`RRC Tracking #`** — present, unique and unchanged across all three releases, including 57802 whose operator was renamed (White Energy → North Texas Carbon), so a refresh is 18 updates + 2 new, not 20 new; no content hash needed. *Status map* (`status_map.yaml`, key `tx_rrc_class_vi`): Approved → `permitted`, Withdrawn → `withdrawn`, everything else (Under Review, Pending RAD Response, On Hold by Operator, Hearing, Draft Doc, Draft Permit, "Comment Period for Draft Pemit" sic + the corrected spelling) → `studied`, each judgement argued in the map's caveats with the RRC's own legend quoted from the PDF; `tests/test_lifecycle_definitions.py` passes (28). *Gate:* `reuse: unknown` / `publication: none` kept; the registry reports the source `gated` (not `implemented`), refuses it without `allow_restricted=True`, and a real `run()` with the flag writes only quarantine paths — all asserted in the connector's tests. **Terms found, not classified:** the RRC Site Policies page (`https://www.rrc.texas.gov/site-policies/`) grants copying "for noncommercial use, as long as the content remains unaltered" — quoted verbatim in the manifest `license` field for an owner/counsel to classify; docs/13 §6 row untouched (`check_manifest_licences.py` PASS, 88 sources). *Personal data:* no column names a person; the PDFs' document metadata does name the RRC author (Info `Author`, XMP `dc:creator`, one copy inside a compressed object stream, so no clean byte-level scrub) — the connector never reads metadata and a test proves nothing from it reaches a row or `raw`. Fixtures `tests/fixtures/tx_rrc_class_vi_list_2026-09-22.pdf` and `..._2026-03-11.pdf` (both layouts, untrimmed — a PDF cannot be cut without re-serialising) were added on the coordinator's instruction and **removed the same day**: the owner first tabled commercial use, then superseded that with the posture decision (verbatim: "Let's move forward as a noncommercial platform for now for maximum and best data access. Then decide how to proceed once we're done." — `docs/00-PLAN.md` decisions log, 2026-09-25). The RRC's Site Policies grant is compatible with that posture; `reuse` stays `unknown` and the connector gated **only until** the vocabulary gains a `noncommercial` class (another lane, with a platform-posture setting; Texas is the first source it applies to). **No RRC document lives in the repository, by choice**; the fixtures README's rule stands. The parser is pinned on synthetic cell arrays in `test_connector.py` — the 2026-09-22 table transcribed cell for cell with pdfplumber's line breaks (20 rows, 20 unique tracking numbers asserted), a representative 10-row expected table (one row per status value, the rename case 57802, the amendment case 59502, the hyphen-join case 59737) pinned by equality, the March layout, the cosmetic header drift, the hyphen join, the footnote strip, the 14-cell invariant, the repeated header and the mis-alignment signature — plus one end-to-end test over a real PDF that reads the gitignored `tests/fixtures/local/` and skips when empty (`.gitignore` gains that path). The manifest `verified` block keeps the three releases' sizes and sha256s as evidence; `notes` records the posture decision, the compatibility, the pending reclassification and the gate. Gates after the rework, from the repo root: `ruff check .`, `ruff format --check .`, `mypy` (183 files), `mypy --strict --follow-imports=silent infra/` (17), `lint-imports` (2 kept), `python scripts/check_manifest_licences.py` (PASS, 88) all exit 0; `pytest pipeline/connectors/us_tx_rrc_class_vi tests/test_connector_gating.py tests/test_lifecycle_definitions.py -q -rs` exit 0, 84 passed, 1 skipped (the local-PDF test, no file present); `pytest pipeline/connectors -q` exit 0, 164 collected, 163 passed, 1 skipped. Not committed.
- 2026-09-25 backend-developer (coverage-note lane): **the ethanol two-source state is on the public methodology page, with both numbers** (owner, 2026-09-25: yes to surfacing it now, "two sources, not yet resolved" wording, both numbers shown). *Derived fact.* `services/api/coverage.py::asset_sources` -- rows, located rows and contributing sources per `asset_type`, and whether a resolution layer exists, read from the schema through the inspector (`asset_source`, docs/24 §5(a)) rather than asserted, so the fact flips the day the link table lands; on `GET /v1/coverage` as `assets` (`api/openapi.yaml`, 3.1 union spelling; `tests/test_api_contract.py` validates the real response). On the dev load: `ethanol_plant` 388 rows / 197 located from `us.eia.atlas.ethanol_plants` (197/197) and `us.eia.ethanol_capacity` (191/0); `rng_project` 1,851 / 1,339 from `us.epa.lmop` (1,353/1,339) and `us.epa.agstar` (498/0 -- AgSTAR carries no coordinates at all, which docs/24 did not note); every other type one source; `unresolved_multi_source = [ethanol_plant, rng_project]`. *Notes.* Two dated notes in `data/vocabulary/coverage_notes.yaml` keyed `applies_to: {unresolved_asset_type: X}`: `ethanol_two_sources` (about 201 distinct plants, 207 at the pessimistic floor, precision/recall 183/187, 46.6% floor, Atlas operator name the older one; measured 2026-09-22, docs/24) with its figures typed and dated in a `figures` block the API echoes; and `rng_two_sources` (0 cross-source links, disjoint populations), added because the derived table lists RNG as two-source-unresolved and a reader should not have to guess whether that is another ethanol. Both retire automatically through `_applicable_notes` when the type has one source or the resolution table exists; `services/api/test_coverage.py` proves each condition (the second by creating `asset_source` in the test schema and watching the note and `resolved` flip). Notes now echo `applies_to` and `figures` so a surface can find the note for the fact it renders. *Placement.* `/methodology` §2 gains an "Existing assets: sources per type" table (rows, located badge, per-source rows, resolved / not yet / one source) and each asset note prints the derived count of the current load beside its written estimate. On `/assets?asset_type=ethanol_plant` a single line under the chips: "This list holds 388 rows (about 201 ethanol plants). Two sources, not yet resolved to one record per asset ... measured 2026-09-22. The count above is of the 197 rows with a location." The PR #11 rule (no coverage line beside a populated list) is kept and the exception argued in the template: here the populated count *is* the wrong number, so the line is the corrected figure, not a disclaimer. It renders only for a single selected type with more than one source, no resolution, *and* a note carrying a measured distinct-count estimate (`web/viewmodels.py::asset_count_note`); RNG, single-source types, the unfiltered index and the proposals lists get nothing. Brief corrections: the ethanol list header count is 197 (the map's located-rows figure), not 388, because the capacity-report rows have no geometry -- the line says so; and `web/.data/dev.db` still predates migration 0018 (no `source.vintage`), so the full `coverage()` cannot run against it, only the new fact was measured there. `services/api/visibility.py`, `data/sources.yaml`, `pipeline/`, docs/02 and docs/13 untouched.
- 2026-09-25 data-engineer (CCS source-map lane): **the primary sources behind carbonstorage.io's nine asset classes and five feature types, mapped and registered — no carbonstorage.io fetch, 106 polite probes through `PoliteSession` (robots first, no retries, challenges honoured).** `docs/02` §11 holds one table per data type with measured/inferred marked per cell, the "no public primary source" list (lateral geometry, contracted-plant lists, hub detail, pre-application intent, closed primacy-state systems, per-project 45Q, most non-US coverage) and a rows-per-effort build order. *Manifest* (`data/sources.yaml`, 94 → 119 entries), all with URL/status/bytes/sha256 in `verified`: `us.epa.ghgrp.subpart_rr` (the annual GHGRP zip, 28,389,973 B, sha256 `895349c8…bf8345`, fetched: sheet "Geologic Sequestration of CO2" = **20 facilities RY2023** with mass sequestered, 12 of them EOR fields; Envirofacts `reported_subparts CONTAINING RR` = 63 facility-years; **no** `rr_`/`uu_subpart_level_information` tables exist — 404), `us.epa.ghgrp.subpart_uu_pp` (**81 CO₂ injection facilities**, **138 CO₂ suppliers** RY2023; RY2024 not yet published), `us.epa.uic.well_inventory` (FY2024 xlsx: 106,656 Class II recovery wells nationally, TX 36,852; 174 Class VI), `us.federalregister.api` (35 Class VI primacy rules — **Colorado proposed 2026-03-19**; 270 PHMSA CO₂-pipeline documents), `us.sec.edgar_fts` (48 CCS hits/12 months; SEC fair-access clauses quoted), `us.epa.foia.reading_room` (securefoia.epa.gov room + quarterly FOIA-log PDFs; derived-only, requester names never stored), `us.doe.foia.reading_room` (no CCS item), `us.doe.oced.portfolio` (client-rendered; DOE reorganised `oced` → `cmei/oced`, `fecm` → `hgeo` with **no carbon-management area**; 2025-10-01 termination of 321 awards, list unpublished), `us.doe.netl.carbon_storage_portfolio` (**unpublished** — Drupal 403 "not authorized" ×3), `us.phmsa.hazardous_liquid_annual` (unreached: origin 403 honoured, one archive attempt reset), `us.tx.rrc.datasets`, `us.nd.dmr.oilgas`, `us.wy.ogcc.data` (`pipeline.wyo.gov` robots `Disallow: /`), `us.ok.occ.well_data` (per-well UIC injection volumes 2011–2025), `us.nm.ocd.data`, `us.ks.kgs.wells`, `us.il.dnr.oilgas` (no data links), `us.sd.puc.co2_pipeline_dockets` (HP22-001 Summit, HP22-002 Navigator with filed/closed dates), `us.nd.psc.case_search`, `us.ia.iuc.efs` (robots `Disallow: /` — never fetch; IUB is now IUC), `us.il.icc.edocket` (SPA), `us.mn.puc.edockets` (bare 403), `no.sodir.factpages_co2_storage` (NLOD clause quoted), `gb.nsta.carbon_storage_licences` (terms not found), `global.iea.ccus_projects` (challenge). *Edited:* `us.tx.rrc.class_vi` — the RRC site policies **were readable and are quoted**: "grants permission to copy and distribute the information on its website for noncommercial use" → `restricted`, owner/counsel item; `us.epa.ghgrp` and `us.usaspending` (CarbonSAFE award rows measured: Four Corners Phase III $42.7M, Polk $55.8M, Illinois Basin West $20.5M …). `docs/13` §6 gains ten rows (TX split out of the six-state `unknown` row); `scripts/check_manifest_licences.py` PASS, 113 sources. **45Q**: no per-project public record (IRS credit pages 404; Form 8933 schedules are return attachments); the RR MRV-plan list is the public proxy. **FERC** does not regulate CO₂ pipelines — PHMSA safety, STB rates (49 U.S.C. §15301(a), read live), states site. Owner actions: Louisiana data-sharing request (SONRIS robots-disallowed), the Texas clause decision. *Posture pass, same day, after the owner's 2026-09-25 noncommercial decision:* `docs/02` §11.1–11.2 tables gain a posture column (`open` / `usable-NC` / `restricted-or-silent` / `blocked`, filled from the terms column and the manifest, nothing re-probed), §11.4 item 5 (Texas) is unblocked pending only the `noncommercial` reuse class, new §11.6 lists what the posture unlocks (RRC Class VI list and UIC/permit dumps, GEM TZ-ID rows, gem.wiki derived facts — nothing else among the 106 probes) and the three preconditions the coordinator put to the owner, and `us.tx.rrc.class_vi` `notes` carry the `noncommercial (pending class)` marker beside `us.tx.rrc.datasets`; no `reuse` value changed.
- 2026-09-25 backend-developer (posture lane): the platform posture as a flip, not a re-audit (`docs/26-platform-posture.md`; owner decision 2026-09-25). `services/posture.py`: `PLATFORM_POSTURE` ∈ {`commercial`, `noncommercial`}, read once per process the way `SENDER_LEGAL_NAME`/`MAP_TILE_URL` are, **default `commercial`**, garbage fails closed; `publishable_reuse_classes()`/`gated_reuse_classes()`/`posture_statement()` pure and at 100% (`services/test_posture.py`). `services/db/models.py`: `REUSE_CLASSES` gains `noncommercial`; `Licence` gains `CHECK (reuse_class <> 'noncommercial' OR NOT allows_commercial_use)`; `Licence.is_publishable_class`/`Source.gated` follow the posture. Migration `0020` swaps the three vocabulary CHECKs (`licence`, `proposal`, `opportunity`) inside `batch_alter_table`, adds the consistency CHECK, and its **downgrade refuses while any row carries the class**, naming counts per table; round-tripped on SQLite (`tests/test_migration_0020.py`) and on Postgres 16 (`upgrade head` from empty → `downgrade -1` → `upgrade head` → seeded one `noncommercial` licence → `downgrade -1` refused with `licence=1`, `alembic_version` still `0020`, constraints intact → purged → `downgrade -1` → `upgrade head`; the CHECK also refused `('noncommercial', allows_commercial_use=true)` at INSERT; note: the round trip needs a UTF-8 database — `createdb -E UTF8 -T template0 --locale=C.UTF-8` — because a `SQL_ASCII` cluster makes psycopg hand SQLAlchemy `version()` as bytes and the connection fails before any migration runs). `services/api/visibility.py`: `PUBLISHABLE_REUSE_CLASSES = publishable_reuse_classes(platform_posture())` at module level, one import and one docstring paragraph, **no branch added**; still 100% branch coverage; `tests/test_visibility_predicate.py` re-executes the module under `commercial`, `noncommercial`, unset and a typo, and pins the clause byte-identical across tiers under `noncommercial`. `pipeline/connectors/registry.py` `PUBLISHABLE_REUSE`/`GATED_REUSE` from the same helper; `services/ingest/loader.py` (affirmative membership, `noncommercial` licence rows written with attribution + link required, `allows_commercial_use`/`allows_bulk_export`/`allows_api_redistribution` false), `services/api/admin_sources.py` (`_licence_gate_missing` names the posture set; reclassifying to `noncommercial` clears the commercial flag instead of tripping the CHECK), `services/api/admin_records.py`, `services/api/coverage.py::WITHHELD_REUSE`, `services/social/editorial.py`, `services/resolve/report.py` all derive from it; `tests/test_platform_posture.py` asserts the gates cannot disagree under any setting, that a `noncommercial` row is invisible on `public`/`pro`/`api` by default and visible on all three under the widened set and gone again when narrowed, that PJM stays hidden under `noncommercial`, the CHECK, the loader row, the reclassification, and that no admin route writes `allows_commercial_use`. `GET /v1/health` gains `posture` + `posture_statement` (spec: `HealthResponse`, `ReuseClass` enum, coverage `withheld[].reuse` enum); `/about#tiers` and `/methodology` print the API's sentence (`web/test_platform_posture.py`: renders per value, absent when the API reports none — no prose in templates). `scripts/check_manifest_licences.py`: `noncommercial` in `REUSE_VOCAB`, register class `noncommercial` → manifest `noncommercial`, ranked below `attribution`; docs/13 §0 row + rule + §6 note; docs/21 §3.19/§8. `scripts/posture_report.py` (read-only, per source and table, `--json`). **Nothing reclassified, no pricing surface touched, `data/sources.yaml` untouched** (field-guide patch in the scratchpad). Measured: 24 `unknown` manifest entries, **0** with a `license` text reading noncommercial/non-commercial/NC/research. Finding: `Licence.allows_commercial_use` is not load-bearing anywhere and is `false` on every loader-written `attribution` licence, so the posture gates on the class, not the field.
- 2026-09-25 data-engineer (GHGRP lane): **EPA GHGRP built as a context source and an ownership source, and the route is not the zip.** The 28 MB Data Summary Spreadsheets zip (sha256 `895349c8…3fa8f`, RY2023, still the newest — RY2024 answers 0 rows) carries no `parent_company`, `co2_captured`, `rr_mrv_plan_url` or `year`; the connector (`pipeline/connectors/us_epa_ghgrp/`, `kind: document` as `us.eia.860`) reads Envirofacts `pub_dim_facility` as two 10,000-row CSV windows (3.1 MB + 0.4 MB; 11,281 RY2023 facilities, all with a point), discovers the newest year by count probe, and strips `address1/address2/comments` before the snapshot is stored (all 37 columns enumerated; no representative field). Parent strings parsed as stated (11,118 sum to 100, 33 do not, 93 `(%)`, 36 empty, 1 mixed, max 119 parents); Tallgrass is **22** facilities, not 23. Subpart RR/UU/PP membership from `reported_subparts` (`RR (RPT)`); RR mass and UU quantity joined from the zip's two sheets (`--summary-zip`; 80 of 81 UU values are `confidential`, flagged). **No GHGRP row enters `asset`** (docs/24): `pipeline/context/ghgrp.py` matches facilities to existing assets — EPA's GHGRP↔ORIS crosswalk first (vendored, 2,166 pairs, 1,620 land on dev `power_plant` rows), then state + ≤1 km + name + NAICS with solar/wind/hydro/storage plants excluded, accepted at 0.55 (blind vs crosswalk 99.0 % precision / 82.8 % recall; hand-checked sample 57/59, `data/eval/ghgrp_match_labels.csv`). Against a copy of dev.db: 2,679 matches (power 1,813 = 12.4 %, gas processing 264 = 55 %, ethanol 126 = 64 % of pointed, storage 63, LNG 4, RNG 401), 8,619 facilities held in the parquet (21 of 22 Tallgrass compressor stations have no asset type to land on); `services/ingest/ghgrp.py` wrote **3,010 `asset_owner` edges, 2,986 with `share_pct`, all with `as_of = 2023-12-31`** — the first dated, share-bearing owner source (docs/24 §7.1) — and `attributes["ghgrp"]` with inline provenance on 2,671 rows. Manifest and docs/02 §12 changes delivered as `ghgrp_manifest.patch` (another lane owns `data/sources.yaml`). Gates green; nothing committed.
- 2026-09-26 coordinator (back-end review): `docs/42-backend-review-2026-09-26.md` — the measured review the owner sequenced after the wiring: 187 modules / 55,748 physical lines / 1,900 functions on `c8c782a`; 49 functions >= 80 lines, 9 at nesting >= 5; **four** near-duplicate body groups in total (so the problem is not copy-paste); three route modules carrying 70/23/30 private helpers; `load_dataframe` 325 lines with 18 cross-block locals; `organization_detail` 224 lines with 34; layering edges measured (`web` imports `services.ingest/db` from exactly three composition modules; `services.ingest/resolve` -> `services.api` is zero). Seven lanes in order (L1 loader steps, L2 organisation pages, L3 asset pages + sitemaps, L4 intake approval, L5 public record routes, L6 import contracts, L7 optional customers/deletion), each with files, baseline -> target, gating tests. `scripts/backend_metrics.py` is the reproducible measure every lane reports its delta with. Measures differ from the plan row's code-line counts (2,554/1,896/1,574 physical vs 2,067/1,581/1,393 code lines) and the method is stated. Nothing refactored yet.
- 2026-09-26 backend-developer (refactor lane L1): `services/ingest/loader.py::load_dataframe` 325 lines / nesting 6 -> a 37-line orchestrator over named steps (`_prepare_load_context`, `_index_records`, `_upsert_records` with `_claim_source_record_key` / `_update_existing_entity` / `_create_entity_and_link`, `_load_events` / `_load_one_event`); a frozen `_LoadContext` for the per-load constants and a mutable `_UpsertState` for the loop's four containers, three call sites each; every new function <= 6 parameters; module nesting 6 -> 3, functions >= 80 lines 2 -> 3, lines 1,356 -> 1,480. Behaviour-preserving by the suite (302/302 in the nine files that import the loader; full suite core exit 0 at 88%, `loader.py` 89% branch). **Throughput measured, not assumed:** `bench_loader` interleaved before/after with a `/tmp` control worktree, 1,332-1,378 -> 1,282-1,310 rows/s (~3.5% slower); cProfile shows +28k calls in 18.7M and no new hot spot; cause not isolated, delta accepted for a batch path and recorded in `docs/42` §7. Three passes: hand-threaded parameters (15/13/10) -> context -> one duplicated per-row `fields` computation removed. `docs/42` §5 corrected: `public_at`/`published_at` are reassigned in the events block, not shared (16 crossing locals, not 18).
- 2026-09-26 backend-developer (refactor lane L2): the organisation pages leave `web/app.py` (2,554 -> 1,423 lines) for a `web/organizations.py` router, and the page plumbing every route shares (`templates` and its globals, `get_api`, canonical URLs, JSON-LD, `not_found_response`, the map/geometry presenters) gets one home in `web/page.py` instead of a third copy of the `legal.py`/`pricing.py` workaround. `organization_detail` 224 lines / nesting 6 -> fetch / compose (+ map, nearby) / render, largest piece 70 lines, two dataclasses for what 34 locals carried. Moved verbatim by AST check: 140 of 142 original definitions node-identical, the two routes changed as intended. Two test imports retargeted; `WEB_ROOT` deduplicated. `web` suite exit 0; full suite on the lane tree core exit 0 at 88%, `visibility.py` 100%, web exit 0, import contracts kept. Not touched: `_org_type_counts` (depth 6), `_org_summary_parts` (depth 5), `legal.py`/`pricing.py`.
- 2026-09-26 backend-developer (lane L4, back-end review refactor): intake approval moved out of `services/api/admin_people.py` (1,574 → 1,002 physical lines) into a new `services/api/admin_intake.py` (672 lines): the route `admin_approve_intake` (was 207 lines, one 159-line `if/elif` on `decision`) and the 13 helpers the call graph showed only it reached (measured by grep, reported in the lane's handback) — verified node-for-node AST-identical to `daf5e80` except the route itself. `_decode_public_id` stayed, confirmed still reached only by `admin_list_tasks`. The `if/elif` became three functions (`_apply_reject_decision` 16 lines, `_apply_link_decision` 49, `_apply_approve_decision` 43, plus `_create_intake_lead_signal` and `_record_approve_audit_events` split out of the approve path to clear 60 lines), sharing a frozen `_IntakeDecisionContext` (`db`, `request`, `task`, `reason`, `actor` — `ctx.user` pre-narrowed non-`None` so mypy needs no second check inside each function); none of the three takes more than four parameters. §2 item 3's duplicate `_normalise_domain` (`services/crm/router.py` L67, `services/api/admin_people.py` L219, AST-identical after stripping docstrings) is now one `normalise_domain` in `services/api/common.py`, imported by both plus `admin_intake.py`. `services/api/app.py` mounts the new router the same way as `admin_people`'s (one import, one `include_router`, same `/admin/v1/tasks/{task_id}/approve-intake` path — the review's `.../approve` was shorthand). `tests/test_api_admin_people.py`'s standalone-app fixture now also mounts `admin_intake_router` (its ~15 approve-intake tests exercise a route that physically moved) and three unit-test import lines updated (`_parse_iso_date`/`_parse_iso_datetime` from `admin_intake`, `normalise_domain` from `common`); no assertion changed. Gates: `ruff check`/`ruff format --check` clean, `mypy` clean (190 files), the five named test files green (`tests/test_api_crm.py` does not exist in this tree, skipped), `check_story_coverage.py` 44/44, `lint-imports` both contracts kept. `scripts/backend_metrics.py` before/after: `services.api.admin_people` 1,574→1,002 lines (42→27 funcs, longest 207→98), new `services.api.admin_intake` 672 lines/19 funcs/longest 68, repo summary 187 modules/55,748 lines/1,900 funcs/49 big/9 deep → 188/55,854/1,904/48/8 (one more module, net +106 lines from the new module docstring and the split's parameter/return-value plumbing outweighing the removed `if/elif`). Duplicate-body groups 4 → 3 (the `_normalise_domain` pair gone). Nothing committed.
- 2026-09-26 backend-developer (refactor lane L3): the asset pages and the sitemap cluster leave `web/app.py` (1,421 -> 733 lines; 2,554 at the review) for `web/assets_pages.py` and `web/sitemaps.py`; the asset presenter cluster under `_asset_extras` goes to `web/page.py` because `search` calls it too (measured). `asset_detail_proxy` deliberately stays: moved with the router it would register `/api/assets/{public_id}` ahead of the literal `/api/assets/geo` and swallow it. All 118 moved definitions AST-identical to the originals; one test import retargeted. `web` suite exit 0; full suite on the lane tree core exit 0 at 88%, `visibility.py` 100%, web exit 0.
- 2026-09-26 backend-developer (refactor lane L5): the public proposal/opportunity routes and their filter stack leave `services/api/app.py` (1,896 -> 1,072 lines) for `services/api/records.py`; the mirrored `list_proposal_events`/`list_opportunity_events` become one `_list_subject_events` over a five-field config (duplicate groups 3 -> 2); `LIST_COMMON`, `sort_spec`, `int_param` live once in `services/api/params.py`, and `assets.py`/`admin_sources.py` drop their identical copies. Generated OpenAPI byte-identical before/after (127 operations); `api/openapi.yaml` untouched. 67 of 69 original definitions AST-identical after the two renames; two test targets retargeted; `records.py` added to the per-file B008 exemption. Found, not fixed: `admin_records._int_param` raises 400 on a bad integer where the other three did not (`docs/42` §8). Full suite on the lane tree core exit 0 at 88%, `visibility.py` 100%, web exit 0, 44/44 stories.
